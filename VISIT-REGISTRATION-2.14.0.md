# ویزیتور — نسخهٔ اندروید ۲٫۱۴٫۰: «ثبت ویزیت» + فعال‌شدن همهٔ بخش‌ها بعد از ورود

**تاریخ:** ۲۰۲۶-۰۹-۲۱ · **شاخهٔ این تحویل:** `arena/01a0c36c-vizitor` · **پکیج:** `ir.atiran.vizitor` (شمارهٔ ساخت **۲۱۸۱۰**، `2.14.0-direct`)

این سند پاسخ به درخواست «چک‌کردن پروژه + ساخت نسخهٔ اندرویدی که با اطلاعات دقیق sys متصل شود،
بعد از وارد کردن اطلاعات ویزیتور بخش‌ها فعال شوند و امکان ثبت ویزیت و کلیه عملیات باشد» است.

---

## ۱) نتیجهٔ بررسی پروژه (چک)

| جزء | وضعیت که پیدا شد |
|---|---|
| `vizitor-app/` (سورس کامل اندروید) | ✅ کامل‌ترین سورس موجود در مخزن (۲٫۱۳٫۷ ساخته‌شده) — **ولی آخرین بیلد (۲٫۱۳٫۸) شکسته بود** |
| خطای بیلد ۲٫۱۳٫۸ | ❌ ۶ خطای کلمپایل: `Luxury2.kt` (۳ تابع با `@Composable` ناقص + خواندن `vizitorPalette` داخل لامبای Canvas) و `Premium.kt` (ارجاع به `short` قبل از تعریف) — از `vizitor-app/build-failure.txt` |
| مسیر اتصال | ✅ اتصال مستقیم SQL Server (پورت ۱۴۳۳) با mssql-jdbc + jTDS؛ ورود با جدول واقعی `dbo.sys_users`؛ بعد از ورود، همگام‌سازی (`SqldirectSync`) کالا/مشتری/فاکتور را می‌پراند → **بخش‌ها فعال می‌شوند** |
| جدول `dbo.Visit` | ⚠️ در اسکیم وجود دارد (۱۵ ستون: `VisRdf`, `Shmo`, `Duration`, `Created`, `Sent`, `Description`, `SaveLat/SaveLng`, `DateCreated`, `TimeCreated`, …) ولی **هیچ کدی در اپ روی آن نمی‌نوشت** |
| دسترسی کاربر محدود | ⚠️ نصب‌کننده `INSERT` روی `subsailtemp_pish`/`subsailtemp` و `EXECUTE add_sail_pish` می‌داد ولی **نه `INSERT ON dbo.Visit`** |
| ریلیز فعلی | آخرین ریلیز سالم: `vizitor-app-direct-2.13.7` (۲۰۲۶-۰۹-۲۰) |

**نتیجه:** پروژه سالم و منسجم است؛ مشکل اصلی (الف) بیلد شکستهٔ شاخه و (ب) نبود «ثبت ویزیت» بود. هر دو رفع شد.

---

## ۲) چه چیزی ساخته شد

### الف) رفع بیلد شکسته (شرط اول همه‌چیز)
* `Luxury2.kt`: `@Composable` به `luxFrame` و `ornaments` اضافه شد (پارامتر پیش‌فرض هر دو از
  `vizitorPalette` — یک `CompositionLocal` — می‌خواند) و در `GoldFlourish` رنگ `goldDark`
  **پیش** از بلوک Canvas خوانده می‌شود (خواندن `CompositionLocal` داخل DrawScope مجاز نیست).
* `Premium.kt`: تعریف `short` (و `tall`) به بالای `dense` منتقل شد تا ارجاع پیش از تعریف نباشد.

### ب) بخش تازهٔ «ثبت ویزیت» (تب ویزیت)
* **`sqldirect/VisitRepository.kt`** — ثبت در جدول واقعی `dbo.Visit`:
  * درج با پارامتر کامل؛ اگر `VisitID` در نسخهٔ ERP شما `identity` باشد خود سرور شماره می‌دهد
    (SCOPE_IDENTITY) وگرنه `MAX(VisitID)+1` — هر دو نسخه کار می‌کند.
  * `Created` = ثانیهٔ UNIX زمان ثبت · `Sent` = ۰ · `DateCreated` = تاریخ شمسی قالب ERP
    (مثل `1405/06/27`) · `TimeCreated` = `HH:mm:ss` · `SaveLat/SaveLng` = موقعیت دستگاه.
  * `recentVisits` (با نام مشتری از `dbo.CUSTOMERS`) و `countToday`.
* **`sqldirect/VisitDate.kt`** — تاریخ شمسی با تقویم رسمی خود پلتفرم اندروید
  (`android.icu.util.PersianCalendar`)؛ بدون الگوریتم دست‌نویس و با جایگزین امن.
* **`sqldirect/VisitLocation.kt`** — GPS با `LocationManager` خالص (بدون Play Services):
  آخرین موقعیت + یک درخواست واحد با تایم‌اوت ۱۲ ثانیه؛ اجازه در سطح UI.
* **`sqldirect/VisitViewModel.kt`** — وضعیت بخش‌ها: وقتی ویزیتور وارد شود (`VizitorSession.loggedIn`)
  بخش‌ها فعال و مشتریان مجاز از `dbo.sys_cus` خوانده می‌شوند؛ خطای «دسترسی ندارد» به فارسی
  با راه‌حل دقیق ترجمه می‌شود.
* **`ui/screens/VisitScreen.kt`** — صفحهٔ فارسی/RTL: موقعیت GPS (دریافت/پاک‌کردن)، انتخاب مشتری
  با جست‌وجو، مدت (دقیقه)، توضیح، دکمهٔ ثبت، ویزیت‌های اخیر + شمارش «امروز».
* **نوار پایین**: تب تازهٔ «ویزیت» (آیکن سه‌بعدی `tab_visit.png`)؛ «گفتگو» به کارت
  «دسترسی سریع» پیشخوان منتقل شد (با کاشیِ ثبت‌ویزیت).

### ج) ست کردن سرور
* `api/provision_android_sql.py`: `dbo.Visit` به فهرست `INSERT_OBJECTS` اضافه شد →
  نصب‌کنندهٔ ویندوز (بخش «آماده‌سازی اتصال مستقیم اندروید») حالا `GRANT INSERT ON dbo.Visit`
  را هم می‌دهد (فقط اگر جدول موجود باشد؛ غیرتلفیقی، قابل اجرای تکراری).
  اگر روی سرور قبلاً نصب کرده‌اید: یا یک‌بار دیگر نصب‌کننده را اجرا کنید، یا دستی:
  `GRANT INSERT ON dbo.Visit TO [vizitor_android];`

### د) «کلیه عملیات» بعد از ورود ویزیتور
با ورود موفق از `dbo.sys_users` و همگام‌سازی، همهٔ این بخش‌ها با دادهٔ واقعی سرور فعال‌اند:

| بخش | منبع دادهٔ واقعی |
|---|---|
| پیشخوان | داده‌های همگام‌شده + فاکتورهای سرور |
| ویترین کالا (۵ سطح قیمت، موجودی) | `dbo.inventory` + `forosh_price` + `VW_InventoryAnbars` |
| **ویزیت (تازه)** | ثبت/خواندن `dbo.Visit` + مشتریان از `dbo.sys_cus` |
| مشتری | `dbo.sys_cus` + `dbo.CUSTOMERS` |
| سبد → پیش‌فاکتور | `EXEC dbo.add_sail_pish` + درج `dbo.subsailtemp_pish` (تریگر) |
| گزارشات | `dbo.sailfact` / `dbo.sailfact_pish` (فاکتورهای واقعی ویزیتور) |
| تنظیمات | اتصال/ورود/همگام‌سازی/خروج |

---

## ۳) فایل نصب

بیلد با CI گیت‌هاب (`build-vizitor-app.yml`) انجام می‌شود و ریلیز زیر ساخته می‌شود:

```
https://github.com/Companymeelano/Vizitor/releases/tag/vizitor-app-direct-2.14.0
```

| فایل | توضیح |
|---|---|
| `Vizitor-2.14.0-direct.apk` | نسخهٔ امضاشده (کلید ثابت `CN=Vizitor App Direct, OU=Meelano Studio Design` — روی نصب‌های قبلی **به‌روزرسانی** می‌شود) |
| `Vizitor-2.14.0-direct-debug.apk` | نسخهٔ آزمون (کنار نسخهٔ اصلی نصب می‌شود) |

### نصب روی گوشی
۱. اگر نسخهٔ ۲.۱۷.۱ آپلودی (`ir.atiran.vizitor` با کلید Debug) را دارید، آن را حذف کنید
(امضای متفاوت). نسخه‌های `…-direct` قبلی (۲.۱۳.۶/۲.۱۳.۷) **نیاز به حذف ندارند**.
۲. `Vizitor-2.14.0-direct.apk` را نصب کنید.
۳. صفحهٔ اول → **تنظیم اتصال** → کارت اتصال نصب‌کننده را بچسبانید (یا آی‌پی/پورت/دیتابیس/کاربر
محدود را دستی وارد کنید) → **تست اتصال** → **اتصال به دیتابیس** → نام کاربری/رمز خودتان در
سامانهٔ آتیران → **ورود و همگام‌سازی** → **ورود به پنل**.
۴. حالا تب **ویزیت** فعال است: موقعیت را بگیرید، مشتری را انتخاب کنید و ویزیت را ثبت کنید.

---

## ۴) نکات صادقانه
* ثبت ویزیت مستقیماً در جدول `dbo.Visit` می‌نویسد — روی دیتابیس واقعی مشتری اثر دارد
(همان‌طور که درخواست شد). ستون‌های `Sent/DateSent/TimeSent` خالی می‌مانند تا ERP خودش پر کند.
* اگر پیام «اجازهٔ نوشتن در جدول Visit را ندارد» دیدید: نصب‌کننده را دوباره اجرا کنید (مبحث ۲ج).
* اجرا روی گوشی واقعی هنوز در این محیط قابل آزمون نیست (دسترس به دستگاه نداریم) — اولین
اجرا را با نسخهٔ `-debug` انجام دهید و هر خطا را بفرستید تا همان‌جا اصلاح شود.

طراحی و برنامه‌نویسی: **میلاد یقوبی** — گروه نرم‌افزاری **Meelano Studio Design**
