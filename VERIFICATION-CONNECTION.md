# بازبینی اتصال ویزیتور — سرور، نصب‌کننده و برنامهٔ اندروید

**تاریخ:** ۲۰۲۶-۰۹-۱۸ · **شاخه:** `arena/01a0a7c6-vizitor` · **PR:** [#1](https://github.com/Companymeelano/Vizitor/pull/1)

خواستهٔ کارفرما: «هر دو فایل راه‌اندازی سرور را برای اتصال و نسخهٔ اندروید ویزیتور بررسی کن تا
مطمئن شوی اتصال بدون مشکل اتفاق می‌افتد.»

این بررسی «متنی» نیست: سه ورک‌فلوی خودکار یک **SQL Server واقعی** بالا می‌آورند، **خودِ کد
برنامه** را روی آن اجرا می‌کنند، `install.ps1` را با **خودِ PowerShell** تحلیل نحوی می‌کنند و
`exe` نصب‌کننده را می‌سازند و محتوایش را رمزنگاری‌شده (sha256) مقابله می‌کنند. نتیجهٔ همهٔ این
اجراها در خودِ مخزن ثبت می‌شود:

| گزارش در مخزن | ورک‌فلو | چه چیزی را ثابت می‌کند |
|---|---|---|
| [`android-app/sql-connection-report.txt`](android-app/sql-connection-report.txt) | `verify-sql-connection.yml` | اتصال واقعی اندروید → SQL Server با کاربر محدود + مسیر کامل نوشتن پیش‌فاکتور |
| [`android-app/server-setup-report.txt`](android-app/server-setup-report.txt) | `verify-installer.yml` | تحلیل نحوی PowerShell + بررسی قرارداد سرور↔اندروید (کارت، دسترسی‌ها، پورت ۱۴۳۳، BOM) |
| [`installer/exe-build-report.txt`](installer/exe-build-report.txt) | `verify-installer.yml` | ساخت exe + اثبات اینکه `install.ps1` داخل exe **همان** فایل این کامیت است |
| [`android-app/apk-report.txt`](android-app/apk-report.txt) | `build-android-direct.yml` | ساخت APK، امضا، درایورها و کلاس‌های داخل بسته |

---

## ۱) نتیجهٔ کوتاه

| موضوع | وضعیت |
|---|---|
| اتصال اندروید به SQL Server (کد خود برنامه، SQL Server واقعی) | ✅ **۸ از ۸ آزمون موفق** (`8 آزمون، 0 ناکام، 0 خطا، 0 پرش`) |
| پورت ۱۴۳۳ از بیرون (همان مسیر گوشی → سرور) | ✅ باز است؛ شنونده روی `0.0.0.0` و `::` |
| ورود ویزیتور از `dbo.sys_users` (رمز `varbinary`) | ✅ موفق؛ رمز غلط/حساب غیرفعال/قفل رد می‌شوند |
| دسترسی‌های کاربر محدود | ✅ خواندن با `db_datareader`، `EXECUTE dbo.add_sail_pish`، `INSERT` جدول میانی، `DELETE` ممنوع |
| مسیر نوشتن پیش‌فاکتور (EXEC + INSERT + تریگر) | ✅ با همان کاربر محدود، واقعاً ثبت و بازخوانی شد |
| `install.ps1` از نظر نحو | ✅ ۱۴۲۷ خط، ۴۲ تابع، **صفر خطای نحوی** (با خودِ PowerShell) |
| exe نصب‌کننده | ✅ بازسازی‌شده و محتوایش با 7-Zip مقابله شد؛ ریلیز `vizitor-setup-v1.0.0` |
| APK اندروید | ✅ ریلیز `vizitor-direct-v1.0.0` |
| اجرا روی گوشی واقعی و ویندوز واقعی | ⚠️ دستی باقی است (CI گوشی/ویندوز واقعی ندارد) |

دانلودها:

* نصب‌کنندهٔ ویندوز: <https://github.com/Companymeelano/Vizitor/releases/tag/vizitor-setup-v1.0.0>
* برنامهٔ اندروید: <https://github.com/Companymeelano/Vizitor/releases/tag/vizitor-direct-v1.0.0>

> پیش‌نمایش‌های طراحی، نصب‌کننده نیستند. **نصب واقعی فقط با `Vizitor-Setup-1.0.0.exe`** انجام می‌شود.

---

## ۲) دو اشکال واقعی که همین بازبینی پیدا کرد (و رفع شد)

### ۲-۱) سه خط با نحو نامعتبر در `install.ps1` — نصب‌کننده عملاً اجرا نمی‌شد

تحلیل نحوی با خودِ PowerShell این پیام‌ها را داد:

```
install.ps1:1201  Variable reference is not valid. ':' was not followed by ...
install.ps1:1268  Variable reference is not valid. ...
install.ps1:1271  Variable reference is not valid. ...
```

سه خط مقصر بودند:

```powershell
$ext = "$ext:$($script:Port)"        # خط ۱۲۰۱
"بازرسی دور $round: همه موارد، موفق"    # خطوط ۱۲۶۸ و ۱۲۷۱
```

در PowerShell، `:` بلافاصله بعد از یک متغیر داخل رشتهٔ دوکوتیشن‌دار «پیشوند دامنه/درایو»
خوانده می‌شود (`$env:PATH` و مانند آن). پس این خط‌ها **خطای نحو** می‌دهند و کل فایل
**پیش از اجرای هر دستوری** رد می‌شود؛ یعنی نصب‌کننده روی ویندوز پیام خطا می‌داد و هیچ کاری
نمی‌کرد. این خط‌ها در نسخهٔ منتشرشدهٔ قبلی exe هم بودند.

اصلاح: همه با `${...}` نوشته شدند (`${ext}`، `${round}`، `${script:StepN}`،
`${script:ApiUrl}`، `${script:DbHost}`) و از این پس CI با خودِ PowerShell نحو را بررسی می‌کند.

### ۲-۲) نبودِ BOM در فایل‌های PowerShell — فارسی در ویندوز به‌هم می‌ریخت

exe این اسکریپت‌ها را با **Windows PowerShell 5.1** اجرا می‌کند
(`$SYSDIR\WindowsPowerShell\v1.0\powershell.exe -File install.ps1`). آن نسخه فایل
UTF-8 **بدون BOM** را با ANSI می‌خواند و تمام پیام‌های فارسی خراب می‌شدند.

اصلاح: BOM به `install.ps1` و `installer/nsi/preflight.ps1` اضافه شد و `installer/build.sh`
در هر ساخت آن را تضمین می‌کند؛ بررسی قرارداد هم نبودِ BOM را «شکست» می‌داند.

---

## ۳) آزمون‌های واقعی اتصال (۸ مورد)

| # | آزمون | نتیجه |
|---|---|---|
| ۱ | بار شدن درایورها (`mssql-jdbc` و jTDS) و پذیرش نشانی‌های JDBC | ✅ |
| ۲ | اتصال با کاربر محدود به `master` و گرفتن فهرست `sys.databases` | ✅ |
| ۳ | اتصال به دیتابیس انتخاب‌شده + نسخهٔ سرور + تعداد مشتری | ✅ |
| ۴ | ورود از `dbo.sys_users` (رمز `varbinary`، مقایسهٔ `CONVERT`) و رد رمز غلط/غیرفعال/قفل | ✅ |
| ۵ | مشتریان مجاز، گروه و تیر قیمت، کالا، قیمت، موجودی انبار، هویت ویزیتور | ✅ |
| ۶ | سلامت مسیر پیش‌فاکتور (۵ شیء + ۲۵ ستون) بدون هیچ نوشتنی | ✅ |
| ۷ | کاربر محدود نمی‌تواند `DELETE` کند | ✅ |
| ۸ | **مسیر نوشتن**: `EXEC dbo.add_sail_pish` + `INSERT` در جدول میانی + اجرای تریگر و بازخوانی سطر | ✅ |

آزمون‌ها در `android-app/vizitor-direct/src/test/java/ir/atiran/vizitor/data/RealSqlConnectionTest.kt`
هستند و فقط وقتی متغیرهای `VIZ_TEST_SQL_*` تنظیم باشند اجرا می‌شوند (بیلد عادی گوشی را خراب نمی‌کنند).

---

## ۴) بررسی «قرارداد» سرور ↔ اندروید

`api/check_connection_contract.py` (خروجی: `android-app/server-setup-report.txt`) از روی سورس
و با حذف توضیحات بررسی می‌کند:

* **کارت اتصال:** کلیدهای `h/p/d/u/H` که `api/android_connect.py` می‌سازد، همان‌هایی است که
  `ConnectCards.kt` می‌خواند؛ کلیدهای JSON هم (`host_lan/host_public/port/database/login`)؛
  پورت ۱۴۳۳ در سرور، در اپ و در قاعدهٔ فایروال.
* **دسترسی‌ها:** ۱۳ شیء خواندنی (مشتری، گروه، کالا، قیمت، موجودی، کاربران، ویزیتورها، جداول
  پیش‌فاکتور) با نقش `db_datareader`؛ `EXECUTE` روی `dbo.add_sail_pish`؛ `INSERT` روی
  `dbo.subsailtemp_pish`؛ هیچ مجوز نوشتن دیگری داده نمی‌شود.
* **نصب‌کننده:** بررسی شنوندهٔ TCP/IP ۱۴۳۳ و فعال‌سازی خودکار آن، قاعدهٔ فایروال، ساخت کاربر
  محدود، کارت QR، پیام «آماده است» فقط بعد از موفقیت بررسی شنونده، و نبودِ بازماندهٔ IIS.

---

## ۵) چیزهایی که هنوز دستی می‌مانند (صادقانه)

1. **اجرای واقعی روی گوشی** (نصب APK و یک ورود با کاربر واقعی) — CI گوشی ندارد.
2. **اجرای واقعی روی ویندوز مشتری** (اجرای `Vizitor-Setup-1.0.0.exe` و دیدن ویزارد) — CI
   ویندوز ندارد. اصلاح‌های این دور (نحو و BOM) دقیقاً همین مسیر را هدف گرفته‌اند.
3. **`retirePreInvoice`**: روی `dbo.sailfact_pish` دستور `UPDATE` می‌زند و کاربر محدود عمداً
   این مجوز را ندارد. نیاز به تصمیم کارفرما: دادن مجوز `UPDATE` یا انجام همان کار با
   امضای تأییدشدهٔ `dbo.Edit_sail_pish` (که `EXECUTE` آن همین حالا داده شده است).
4. **دکمهٔ ثبت پیش‌فاکتور در رابط کاربری**: رابط فعلاً فقط «بررسی سلامت» را صدا می‌زند؛
   `createPreInvoice` آماده و آزمون‌شده است ولی به دکمه وصل نشده (طبق درخواست «رابط را عوض نکن»،
   منتظر تأیید جریان ثبت).
5. **دادهٔ واقعی سرور Meelano**: مقادیری مثل آی‌پی (‎.110 یا ‎.150) و مقادیر سربرگ پیش‌فاکتور
   باید از سمت سرور تأیید شود (پرسش‌های بازِ همان ممیزی).

---

## ۶) بازتولید محلی

```bash
python3 api/check_connection_contract.py        # بررسی قرارداد (سبز)
python3 -m py_compile api/*.py                  # نحو اسکریپت‌های سرور
bash installer/build.sh                         # ساخت exe (نیازمند NSIS)
```

CI ها: `verify-sql-connection.yml` · `verify-installer.yml` · `build-android-direct.yml`

طراحی و برنامه‌نویسی: **میلاد یقوبی** — گروه نرم‌افزاری **Meelano Studio Design**
