# تحلیل کدبه‌کد پروژهٔ آتیران ویزیتور و رفع ایرادهای «اتصال» و «فراخوانی»

**تاریخ:** ۲۰۲۶‑۰۹‑۲۲ · **نسخهٔ تحلیل‌شده:** `2.17.0-direct` (شمارهٔ ساخت ۲۱۸۱۳) · **نسخهٔ اصلاح‌شده:** `2.18.0-direct` (۲۱۸۱۴)
**دامنهٔ تحلیل:** کل پوشهٔ `vizitor-app/app/src/main/java/ir/atiran/vizitor` — **۵۹ فایل، ۲۴٫۳۴۶ خط کد Kotlin**
**روش:** خواندن کامل فایل‌های لایهٔ اتصال/داده و گرپ‌کردن نقطه‌های فراخوانی در کل پروژه (هر ادعای این سند با نام تابع و خط نشان داده شده است).

---

## ۱) معماری واقعی برنامه (آن‌گونه که هست، نه آن‌گونه که در مستندات توصیف شده)

```
UI (Compose, ۱۲ صفحه + ناوبری)
      │
      ├── VizitorViewModel (۳۵۹ خط) ──► VizitorRepository (۴۱۷ خط) ──► Room + Retrofit(API PHP)
      │                                   ↑ مسیر قدیمی: api.php روی پورت ۸۷۳۱ (در نصب فعلی وجود ندارد)
      │
      └── DirectSqlViewModel (۷۷۳ خط) ──► SqldirectSync / CustomerSync / VisitRepository
                                             │
                                             ├── MeelanoDataSource (۹۴۸ خط)  ← کوئری‌های واقعی ERP
                                             ├── VisitorRepository / VisitorLogin
                                             └── SqlConnectionManager (۴۶۰ خط) ← استخر اتصال + درایورها
                                                        │
                                                        ▼
                                          SQL Server 192.168.1.150:1433 / دیتابیس Meelano
                                          (jTDS → Microsoft JDBC به‌عنوان درایور جایگزین)
```

**نکتهٔ کلیدی:** برنامه دو مسیر فراخوانی موازی دارد؛ مسیر «مستقیم SQL» کامل پیاده شده است، ولی
شماری از دکمه‌های کاربر همچنان به مسیر قدیمی `api.php` می‌رفتند. از همین‌جا ایراد «فراخوانی» متولد می‌شود.

---

## ۲) یافته‌ها، به‌ترتیب شدت

| # | شدت | یافته | محل دقیق | وضعیت در ۲٫۱۸٫۰ |
|---|---|---|---|---|
| ۱ | 🔴 بحرانی | **مسیر نوشتن پیش‌فاکتور در ERP هرگز فراخوانی نمی‌شد.** `createPreInvoice` / `preInvoiceLines` / `retirePreInvoice` در `MeelanoDataSource` کامل پیاده شده‌اند اما **صفر نقطهٔ فراخوانی** داشتند؛ فقط `preInvoiceHealth()` صدا زده می‌شد. | تعریف: `MeelanoDataSource.kt:664 / 691 / 734` · فراخوانی‌ها: `grep` در کل پروژه ⇒ فقط `DirectSqlViewModel.kt:308` و `:407` (فقط health) | ✅ رفع شد — از طریق `VizitorGateway.submitCart()` |
| ۲ | 🔴 بحرانی | **دکمهٔ «صدور و ارسال به آتیران» هیچ‌وقت به ERP نمی‌رسید.** `VizitorViewModel.issueInvoice()` فقط فاکتور محلی می‌ساخت و سپس `repo.syncAll()` (مسیر Retrofit/api.php) را صدا می‌زد که در نصب فعلی سروری ندارد. | `VizitorViewModel.kt:260‑280` (۲٫۱۷) · فراخوانی از `CartScreen.kt:315` | ✅ رفع شد — ثبت محلی + فراخوانی واقعی پیش‌فاکتور |
| ۳ | 🔴 بحرانی | **«همگام‌سازی» و «تست اتصال» به API قدیمی می‌رفتند.** `syncNow()` = `repo.syncAll()` و `testConnection()` = `repo.testConnection()` — هر دو Retrofit. | `VizitorViewModel.kt:313‑349` (۲٫۱۷) · `VizitorRepository.kt:239/272/286/306/376` | ✅ رفع شد — `VizitorGateway.syncAll()` و `VizitorGateway.probe()` |
| ۴ | 🟠 مهم | **اتصال بعد از بسته‌شدن برنامه از دست می‌رفت.** `SqlConnectionManager.settings` فقط در حافظه بود؛ `withConnection` با `requireNotNull(settings)` خطای «قبل از هر کوئری باید اتصال برقرار باشد» می‌داد و کاربر مجبور بود به صفحهٔ اتصال برگردد. | `SqlConnectionManager.kt:217` (۲٫۱۷) · `runWithRetry` | ✅ رفع شد — `ensureConnected()` + خواندن تنظیمات رمزنگاری‌شدهٔ گوشی |
| ۵ | 🟠 مهم | **فراخوانی هم‌زمان اتصال، «خطای نامشخص» می‌داد.** `connect()` اگر `connecting` بود، بی‌صدا `false` برمی‌گرداند ⇒ پیام گنگ «خطای نامشخص» در UI. | `SqlConnectionManager.kt:126‑128` (۲٫۱۷) | ✅ رفع شد — انتظار برای نتیجهٔ همان اتصال (تا ۴ ثانیه) |
| ۶ | 🟠 مهم | **هر فراخوانی، اتصال را از نو می‌ساخت.** بدون مسیر سریع، `connect()` حتی وقتی اتصال سالم بود، تنظیمات را جایگزین می‌کرد (کندی + احتمال شکستن کوئری جاری). | `SqlConnectionManager.kt:118‑155` (۲٫۱۷) | ✅ رفع شد — مسیر سریع + بستن استخر فقط هنگام تغییر سرور/دیتابیس |
| ۷ | 🟡 متوسط | **همگام‌سازی، «همهٔ فعالیت‌های ویزیتور» را پوشش نمی‌داد.** فقط کالا (۲۰۰۰)، مشتری (۳۰۰۰) و فاکتور. ویزیت‌های امروز، مسیرها (`dbo.masir`)، سهمیهٔ فاکتور و اعتبار همگام نمی‌شد. | `SqldirectSync.kt:55‑184` (۲٫۱۷) | ✅ رفع شد — ویزیت، مسیر، سهمیه، اعتبار اضافه شد |
| ۸ | 🟡 متوسط | **«ثبت مشتری جدید» ادعای نادرست داشت.** پیام «به حسابداری آتیران ارسال شد» در حالی نمایش داده می‌شد که مسیر نوشتن مشتری در ERP تأیید نشده است. | `VizitorViewModel.kt:242‑250` + `VizitorRepository.kt:150‑175` (۲٫۱۷) | ✅ رفع شد — پیام شفاف «ثبت محلی در انتظار تأیید» |
| ۹ | 🟡 متوسط | **کارت وضعیت، مسیر اشتباه را گزارش می‌کرد.** `HealthReport.baseUrl` از `api.php` ساخته می‌شد؛ دربارهٔ اتصال مستقیم SQL چیزی نمی‌گفت. | `SettingsScreen.kt:503‑519` | ✅ رفع شد — گزارش از `direct-sql://…` + نسخهٔ SQL Server |
| ۱۰ | ⚪ پایین | `DbInvoiceRow.customerName` تنها با کلید «کد مشتری» نگاشت می‌شد (فاکتورها `shmo` را نگه می‌دارند) ⇒ نام مشتری خالی می‌ماند. | `SqldirectSync.kt` (کلید دوگانه) | ✅ قبلاً در ۲٫۱۶٫۰ رفع شده بود (کلید دوگانه SHMO + code) |

### ۲‑۱) جدول نقطه‌های فراخوانی اتصال (خروجی گرپ ۲٫۱۷)

```
SqlConnectionManager.connect ⇒ DirectSqlViewModel.kt:294, 400, 453, 498, 583, 690
                               VizitorViewModel.kt:74
SqlConnectionManager.withConnection ⇒ MeelanoDataSource (همهٔ کوئری‌ها)، VisitorRepository،
                                      VisitRepository، SqldirectSync، CustomerSync
VizitorRepository (Retrofit) ⇒ فقط از VizitorViewModel (syncAll / testConnection /
                               addPendingCustomer / issueInvoice → syncAll)
```

**نتیجهٔ تحلیل:** لایهٔ اتصال مستقیم سالم و دقیق است (استخر ۴ اتصال، `isValid` هر ۵ ثانیه،
دو تلاش با backoff ۷۰۰ms، تراکنش کامل، نقشهٔ خطاهای فارسی، ترتیب درایور jTDS→Microsoft)،
اما **دروازهٔ ورودی دکمه‌ها به آن وصل نبود**. اصلاح ۲٫۱۸٫۰ همین «وصل‌کردن» است، نه بازنویسی.

### ۲‑۲) بررسی «درستی فراخوانی» پیش‌فاکتور (مسیر نوشتن)

تحلیل مسیر نوشتن بر پایهٔ مستندات خودِ پروژه (`android-sql-direct/docs/write-path/`) و کد فعلی:

```
۱) EXEC dbo.add_sail_pish (۲۶ پارامتر موضعی) → @id_en = شمارهٔ پیش‌فاکتور (shfacfo)
      • پروسیجر خودش «begin/commit transaction forosh» دارد ⇒ برنامه نباید تراکنش بیرونی باز کند
۲) برای هر قلم: INSERT در dbo.subsailtemp_pish با mod = 1 (۲۵ ستون، همه پارامتر)
      • تریگر INSTEAD OF (dbo.trig_sst_pish) آن را در dbo.subsailfact_pish می‌نویسد و
        rdf__ سربرگ زنده را روی سطر می‌گذارد (خودِ جدول میانی خالی می‌ماند — رفتار خود ERP)
۳) قرائت تأییدی: subsailfact_pish ⋈ sailfact_pish روی (shfacfo, rdf__)
۴) در شکست نیمه‌کاره: کنار گذاشتن با UPDATE sailfact_pish SET active='f', ismodify='t'
      (دقیقاً همان کاری که Edit_sail_pish با نسخهٔ قبلی می‌کند — سند حذف نمی‌شود)
```

**قاعدهٔ مستندشدهٔ پروژه:** پنج مقدار سربرگ (`@ted_rooz`, `@ph_kh`, `@mod_darsad_vis`,
`@rdf_sarbarg`, `@gainall`) در هیچ‌کدام از دو دیتابیس نمونه ندارند (جدول `sailfact_pish` خالی است)،
پس **نباید حدس زده شوند**. در ۲٫۱۸٫۰ این قاعده به‌صورت مکانیزم اجرا شده است:

* برنامه آخرین پیش‌فاکتور واقعی ERP را می‌خواند (`preInvoiceReference()`).
* اگر چنین ردیفی **نباشد** ⇒ دکمهٔ ثبت واقعی غیرفعال می‌ماند و تنها «پیش‌نمایش سند» ساخته می‌شود.
* به‌محض اینکه مدیر یک‌بار در خودِ ERP پیش‌فاکتور ثبت کند، مقادیر مرجع خوانده و ثبت واقعی فعال می‌شود.

---

## ۳) آنچه در نسخهٔ ۲٫۱۸٫۰ اضافه/اصلاح شد (کد به کد)

| فایل | تغییر |
|---|---|
| `sqldirect/VizitorGateway.kt` **(جدید، ۵۶۴ خط)** | دروازهٔ واحد اتصال و فراخوانی: `ensureConnection()`, `probe()`, `limits()`, `todayVisits()`, `recentVisits()`, `routesFor()`, `syncAll()`, `briefing()`, `preInvoiceReference()`, `submitCart()` (پیش‌نمایش/ثبت واقعی با تأیید بازخوانی و کنار گذاشتن سند نیمه‌کاره) |
| `sqldirect/SqlConnectionManager.kt` | مسیر سریع اتصال، انتظار برای اتصال در جریان، بستن استخر هنگام تغییر سرور، `ensureConnected()`، خودترمیمی در `withConnection`/`borrow()`، `refresh()` با اتصال مجدد |
| `sqldirect/SqldirectSync.kt` | تضمین اتصال پیش از هر کوئری + دامنهٔ تازه: ویزیت‌های امروز، مسیرها، سهمیه و اعتبار در گزارش پایان کار |
| `VizitorViewModel.kt` | `syncNow`/`testConnection`/`issueInvoice` روی دروازهٔ واحد؛ حالت‌های تازهٔ `briefing`, `activities`, `invoiceOutcome`, `liveInvoiceWrite`, `briefingSeen`؛ پیام شفاف ثبت مشتری |
| `ui/components/Lux3D.kt` **(جدید، ۹۴۳ خط)** | کیت سه‌بعدی: `Lux3DStage`, `lux3dPanel`, `Lux3DSlab`, `Lux3DTile`, `Lux3DMenu`, `Lux3DTable`, `Lux3DBarChart`, `Lux3DDonut`, `Lux3DStatOrb`, `Lux3DProgress`, `Lux3DPill`, `Lux3DSectionTitle`, `Lux3DNote` |
| `ui/screens/BriefingScreen.kt` **(جدید، ۵۷۶ خط)** | «اطلاع‌رسانی اولیه به ویزیتور» — وضعیت اتصال، اختیارات، سهمیه، اعتبار، ترکیب مشتریان، مسیرها، وضعیت مسیر ثبت سند و منوی سه‌بعدی شروع سریع |
| `ui/screens/ActivitiesScreen.kt` **(جدید، ۴۷۶ خط)** | «همهٔ فعالیت‌های ویزیتور» — منوی سه‌بعدی ۱۲ فعالیت، نمودار ویزیت‌های هفته، آخرین ویزیت‌ها، مطالبات در خطر، مسیرها و آخرین سند |
| `ui/navigation/VizitorNavigation.kt` | دو مسیر تازه (`BRIEFING`, `ACTIVITIES`) + ورود خودکار به «اطلاع‌رسانی اولیه» در نخستین ورود |
| `ui/screens/DashboardScreen.kt` | دو کاشی تازه در «دسترسی سریع»: اطلاع‌رسانی اولیه، همهٔ فعالیت‌ها |

---

## ۴) چیزهایی که عمداً تغییر نکردند (و چرا)

1. **مسیر `Retrofit/api.php` حذف نشد.** کد آن دست‌نخورده باقی مانده تا اگر روزی سرور PHP راه‌اندازی شد،
   بدون بازنویسی قابل استفاده باشد؛ فقط دکمه‌های برنامه دیگر به آن وابسته نیستند.
2. **`dbo.new_cust` پیاده‌سازی نشد.** امضای پروسیجر ثبت مشتری در ممیزی سرور تأیید نشده است
   (همان سیاست پروژه: هیچ کوئری‌ای با نام حدسی نوشته نمی‌شود). برنامه فعلاً محلی ثبت می‌کند.
3. **صفحه‌های Reports/Customers/Catalog/Cart/Visit/Chat/Settings/DirectSql** با همان طراحی ۲٫۱۷ کار می‌کنند
   و از سیستم طراحی مشترک (`MaReport` + `Lux3D`) رنگ و قاب می‌گیرند؛ بازنویسی کامل آن‌ها در این دور انجام نشد
   تا ریسک رگرسیون روی بخش‌های سالم گرفته نشود.

---

## ۵) کاری که برای تأیید صفرشدن ایرادها لازم است (سمت شما)

1. یک پیش‌فاکتور در **خودِ ERP** برای یکی از ویزیتورها ثبت کنید (بدون صدور فاکتور).
2. یک‌بار در برنامه «اطلاع‌رسانی اولیه» را باز کنید؛ اگر کاشی «مسیر ثبت سند» برچسب **آماده** گرفت،
   سوئیچ «فعال‌کردن ثبت واقعی» قابل روشن‌شدن می‌شود.
3. سپس از سبد سفارش، «صدور و ارسال به آتیران» را بزنید؛ شمارهٔ پیش‌فاکتور و تعداد اقلام
   بازخوانی‌شده از سرور در همان صفحه نشان داده می‌شود.

Meelano Studio Design — Milad Yaghoobi
