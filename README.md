# Vizitor — سامانه مدیریت بازدیدکنندگان

**Vizitor** یک سامانهٔ سروری برای ثبت و مدیریت بازدیدکنندگان است که با **برنامهٔ اندروید** همراه کار می‌کند. این مخزن شامل نصب‌کنندهٔ هوشمند سرور، API، و دیتابیس است.

## اجزای پروژه

| مسیر | توضیح |
|---|---|
| `install.sh` | **نصب‌کنندهٔ هوشمند (Linux)** — تشخیص خودکار سرور، پرسش اطلاعات ضروری، نصب، بازرسی و تعمیر خودکار |
| `install.ps1` | **نصب‌کنندهٔ هوشمند (Windows)** — همان رویه برای ویندوز با پشتیبانی SQL Server |
| `api/server.py` | سرور API (فقط کتابخانهٔ استاندارد Python 3) |
| `api/db.py` | لایهٔ دیتابیس (SQLite یا MySQL/MariaDB یا **Microsoft SQL Server**) |
| `api/seed.py` | درج اولیهٔ داده‌ها (ادمین، کد فعال‌سازی، تنظیمات) |
| `api/provision_sqlserver.py` | آماده‌سازی **غیرتلفیقی** SQL Server (فقط CREATE در صورت نبود؛ بدون DROP/ALTER) |
| `database/schema_mysql.sql` | اسکیم دیتابیس MySQL/MariaDB |
| `database/schema_sqlite.sql` | اسکیم دیتابیس SQLite |
| `database/schema_sqlserver.sql` | اسکیم دیتابیس Microsoft SQL Server (T-SQL، ای‌دی‌ام‌پتنت) |
| `ANDROID_INTEGRATION.md` | راهنمای ساخت **بخش اتصال هوشمند اندروید** (چند کلیک + لحظه‌ای) با کد آمادهٔ Kotlin |
| `android-app/` | **برنامهٔ اندروید «ویزیتور مستقیم»** — اتصال مستقیم به SQL Server روی پورت ۱۴۳۳. **فایل APK آمادهٔ نصب:** [ریلیز v1.0.0](https://github.com/Companymeelano/Vizitor/releases/tag/vizitor-direct-v1.0.0) ([راهنما](android-app/README.md)) |
| `vizitor-app/` | **پروژهٔ کامل اندروید آتیران ویزیتور (۲٫۱۳٫۶) + اتصال مستقیم SQL Server (۱۴۳۳)**: پنل اتصال روی **صفحهٔ اول**، ورود از `dbo.sys_users`، همگام‌سازی و گزارش‌های سرور، فونت فارسی وزیرمتن و آیکن سه‌بعدی تازه — [راهنما](vizitor-app/README-DIRECT-SQL.md) |
| `ANDROID-DIRECT-HANDOVER.md` | **تحویل نسخهٔ نهایی اندروید با اتصال مستقیم SQL (پورت ۱۴۳۳)** — فایل نصب، هش، امضا، گام‌های ست‌کردن با نصب‌کننده و نکته‌های صادقانه |
| `UI-REDESIGN-2.13.6.md` | **بازطراحی لاکچری اپ اندروید (۲٫۱۳٫۶)**: صفحهٔ اول و کاشی‌های نقش، صفحهٔ اتصال و تنظیمات ورود، فونت فارسی وزیرمتن، آیکن سه‌بعدی تازه + هش و لینک فایل نهایی |
| `CONNECTION-FIX-FINAL.md` | **رفع مشکل اتصال اپ ویزیتور به سرور (۱۴۳۳)**: علت دقیق (سه اشکال نصب‌کننده + یک اشکال اپ)، صفحهٔ اتصال جدید روی صفحهٔ اول، ذخیرهٔ ورود، همگام‌سازی و گزارش‌ها + هش و لینک فایل‌های نهایی |
| `DIRECT-SQL-FIX-REPORT.md` | **گزارش رفع اشکال اتصال مستقیم (پورت ۱۴۳۳)**: ۱۰ اشکال کد‌به‌کد در برنامه و نصب‌کننده + فایل‌های نهایی + گام‌های اجرا و عیب‌یابی |
| `android-sql-direct/` | لایهٔ داده و ممیزی SQL (جایگزین‌شدنی در برنامهٔ موجود) + مستندات طرح و اسکریپت‌های فقط‌خواندنی سرور |
| `INSTALL.md` | راهنمای کامل نصب و پیکربندی (Linux + Windows) |

## نصب سریع

**Linux:**

```bash
# روی سرور، این پوشه را کپی کنید و:
sudo bash install.sh
```

**Windows (با Microsoft SQL Server):**

```powershell
# روی سرور ویندوزی، در PowerShell با دسترسی Administrator:
powershell -ExecutionPolicy Bypass -File install.ps1
```

نصب‌کننده به‌طور خودکار:
- ✅ سیستم‌عامل، پکیج‌منیجر، Python و دیتابیس را تشخیص می‌دهد و کمبودها را نصب می‌کند
- ✅ **آی‌پی سرور (عمومی + داخلی)** را شناسایی و آدرس API را بر اساس آن می‌سازد
- ✅ از شما می‌پرسد: **آدرس/IP، پورت، مشخصات دیتابیس، حساب ادمین، کد فعال‌سازی**
- ✅ همهٔ موارد را در `config.json` جایگذاری و سرویس را راه‌اندازی می‌کند
- ✅ سپس **همه‌چیز را چک** می‌کند (API، دیتابیس، فعال‌سازی، لاگین) و در صورت مشکل،
  **تنظیمات را بازبینی/تعمیر کرده و دوباره چک** می‌کند (تا ۴ دور)
- ✅ در پایان، **آدرس دقیق API برای ورود در برنامهٔ اندروید** را نمایش می‌دهد

## اتصال برنامهٔ اندروید (چند کلیک + لحظه‌ای)

در بخش «اتصال API» برنامهٔ اندروید، آدرسی که نصب‌کننده در انتها نمایش داده را وارد کنید:

```
http://آدرس-سرور:پورت/api
```

اتصال **هوشمند** است: برنامه با دریافت `http://آدرس-سرور:پورت/api/config` آدرس دقیق، نسخه، وضعیت
فعال‌سازی و پشتیبانی لحظه‌ای را خودکار می‌خواند و بقیهٔ مراحل (فعال‌سازی، لاگین) را با حداقل سؤال جلو می‌برد.

**دریافت اطلاعات لحظه‌ای:** سرور جریان **Server-Sent Events** در `GET /api/events` دارد
(ثبت هر بازدیدکننده بلافاصله به برنامه push می‌شود) + همگام‌سازی افزونه‌ای در
`GET /api/visitors/since?after_id=N`. (این‌ها بخش پنل/سرور هستند؛ خودِ برنامهٔ ویزیتور مستقیم با دیتابیس کار می‌کند.)
جزئیات کامل + کد آمادهٔ Kotlin در [ANDROID_INTEGRATION.md](ANDROID_INTEGRATION.md).

**سرور:** سامانه روی پورت ۹۵۹۵ بالا می‌آید و نیازی به IIS ندارد (IIS در این پروژه استفاده نمی‌شود) —
بدون لمس هیچ سایت/پیکربندی موجود.

**پنل مدیریت (در خودِ همین سرویس):** با باز کردن ریشهٔ آدرس سرویس، صفحهٔ پنل فارسی باز می‌شود:

```
http://127.0.0.1:9595/          (روی همان سرور)
http://آدرس-سرور:9595/          (از داخل شبکه)
```

سرویس، خودش فایل‌های پنل را از پوشهٔ `panel/` سرو می‌کند (بدون IIS، بدون وب‌سرور اضافه، بدون اینترنت):
`panel/index.html` + `panel/fonts/Vazirmatn-*.woff2` (فونت وزیرمتن، مجوز SIL OFL — فایل مجوز در `panel/fonts/OFL.txt`).
رنگ‌بندی یکدست سرمه‌ای، فارسی و راست‌به‌چپ. کارت‌های پنل: وضعیت سرویس و آدرس‌ها، سلامت سرویس و
دیتابیس، **تنظیمات اتصال SQL** (سرور/پورت/کاربر/رمز/دیتابیس)، تست اتصال، فهرست دیتابیس‌های سرور،
فعال‌سازی، ورود مدیر، ویزیتورها و شناسنامه. رمز SQL هرگز خوانده/نمایش داده نمی‌شود (کارت فقط
«تنظیم شده/نشده» را نشان می‌دهد) و نوشتن تنظیمات فقط با توکن مدیر انجام می‌شود.

## API (خلاصه)

| روش | مسیر | توضیح |
|---|---|---|
| GET | `/api/ping` | زنده بودن (بدون احراز) |
| GET | `/api/config` | تنظیمات عمومی برای اپ (آدرس، وضعیت فعال‌سازی) |
| GET | `/api/health` | گزارش کامل سلامت (دیتابیس، فعال‌سازی، uptime) |
| GET | `/panel/*` | همان فایل‌های پنل (نشانی جایگزین، برای لینک‌دهی راحت) |
| POST | `/api/activate` | فعال‌سازی با کد `{"code": "..."}` |
| POST | `/api/login` | ورود ادمین `{"username","password"}` → توکن |
| GET/POST | `/api/visitors` | فهرست / ثبت بازدیدکننده (با توکن `Authorization: Bearer ...`) |
| GET | `/api/visitors/since?after_id=N` | همگام‌سازی افزونه‌ای (فقط رکوردهای جدید) |
| GET | `/api/events` | **جریان لحظه‌ای SSE** (ثبت بازدیدکننده/تغییر فعال‌سازی → push آنی) |
| GET | `/` و `/fonts/*` | **صفحهٔ پنل مدیریت** و فونت‌هایش (سرو از پوشهٔ `panel/`) |
| GET | `/api/db/info` | تنظیمات فعلی اتصال — **بدون رمز** (رمز فقط «تنظیم شده/نشده») |
| GET | `/api/db/databases` | فهرست **فقط‌خواندنی** دیتابیس‌های همان SQL Server (`sys.databases`) |
| POST | `/api/db/test` | تست اتصال با `{host, port, user, password}` بدون ذخیرهٔ چیزی |
| POST | `/api/db/save` | ذخیرهٔ اتصال/دیتابیس در `config.json` (نیازمند توکن مدیر) |

## حالت‌های نصب‌کننده

**Linux:**

```bash
sudo bash install.sh                 # نصب تعاملی (پیشنهادی)
sudo bash install.sh --auto          # بدون سؤال، با مقادیر هوشمند
sudo bash install.sh --recheck       # فقط بازرسی و تعمیر خودکار نصب موجود
sudo bash install.sh --port 9000     # اجبار پورت
sudo bash install.sh --ip 1.2.3.4    # اجبار آدرس
```

**Windows:**

```powershell
powershell -ExecutionPolicy Bypass -File install.ps1           # نصب تعاملی (پیشنهادی)
powershell -ExecutionPolicy Bypass -File install.ps1 -Auto     # بدون سؤال، با مقادیر هوشمند
powershell -ExecutionPolicy Bypass -File install.ps1 -Recheck  # فقط بازرسی و تعمیر خودکار
powershell -ExecutionPolicy Bypass -File install.ps1 -Port 9000 -Ip 1.2.3.4
```

## پشتیبانی Microsoft SQL Server (بدون آسیب)

نصب‌کنندهٔ ویندوز می‌تواند موتور دیتابیس را روی **SQL Server** قرار دهد. دسترسی به دیتابیس
**کاملاً دقیق و غیرتلفیقی** است:

- 🛡️ فقط `CREATE` در صورت **نبود** شی (دیتابیس، لاگین، یوزر، جداول) — هیچ `DROP` یا `ALTER` روی شی موجود
- 🛡️ اگر لاگین قبلاً وجود داشته باشد، **گذرواژه‌اش دست‌نخورده می‌ماند**
- 🛡️ اختیارات فقط روی همان دیتابیس هدف: `db_datareader` + `db_datawriter` + `db_ddladmin`
- 🛡️ تغییر **هیچ** تنظیم سراسری SQL Server (پورت، sa، دیتابیس‌های دیگر، ...) انجام نمی‌شود
- 🔄 اسکریپت ای‌دی‌ام‌پتنت است: اجرای مکرر آن بی‌خطر است

جزئیات کامل، عیب‌یابی و مدیریت سرویس در [INSTALL.md](INSTALL.md) آمده است.

## گزارش‌های تأیید (ساختهٔ CI، داخل مخزن)

هر تغییر روی این شاخه، سه بررسی خودکار را اجرا می‌کند و نتیجه را در مخزن ثبت می‌کند:

| گزارش | چه چیزی را ثابت می‌کند |
|---|---|
| [`android-app/sql-connection-report.txt`](android-app/sql-connection-report.txt) | یک SQL Server واقعی در CI بالا می‌آید و **خودِ کد اندروید** با کاربر محدود به آن وصل می‌شود: ورود ویزیتور، مشتریان، تیر قیمت، موجودی، سلامت پیش‌فاکتور و مسیر نوشتن (EXEC + INSERT + تریگر) |
| [`android-app/server-setup-report.txt`](android-app/server-setup-report.txt) | تحلیل نحوی `install.ps1` با خودِ PowerShell + بررسی قرارداد سرور↔اندروید (کارت اتصال، دسترسی‌ها، پورت ۱۴۳۳، BOM) |
| [`installer/exe-build-report.txt`](installer/exe-build-report.txt) | `Vizitor-Setup-1.0.0.exe` از نو ساخته می‌شود و با 7-Zip ثابت می‌شود **همان `install.ps1` این کامیت** داخل exe است (مقایسهٔ sha256) |

## فایل نصب ویندوز (Vizitor-Setup.exe) — آیکن، فعال‌سازی، انتخاب بخش‌ها

برای نصب روی **ویندوز ۷ / ۸ / ۱۰ / ۱۱** یک فایل نصب گرافیکی تک‌فایلی هم آماده است:

```
release/Vizitor-Setup-1.0.0.exe   ~ ۹۷۴ کیلوبایت (≈ ۹۵۲ KiB) — عدد دقیق و sha256 در
`release/Vizitor-Setup-1.0.0.exe.sha256` و در `installer/exe-build-report.txt` ثبت می‌شود

**دانلود مستقیم:** [ریلیز vizitor-setup-v1.0.0](https://github.com/Companymeelano/Vizitor/releases/tag/vizitor-setup-v1.0.0)
```

- 🎨 رابط فارسی راست‌به‌چپ با آیکن و تصاویر اختصاصی ویزیتور
- ☑️ **انتخاب بخش‌ها با تیک**: سرویس/پنل، دیتابیس، فایروال (۱۴۳۳ و ۹۵۹۵)،
  اتصال مستقیم اندروید به SQL Server، راهنماها و بستهٔ SQL، میان‌برها، بازرسی و تعمیر
- 🔑 **کد فعال‌سازی** در صفحهٔ مخصوص (یا گزینهٔ «بعداً»)
- 📷 **کارت اتصال + کد QR اندروید**: با یک اسکن، سرور/دیتابیس/کاربر تنظیم می‌شود و ویزیتور فقط
  نام کاربری و کلمهٔ عبور خودش را می‌زند (فایل‌های `setup\android-connect.png` / `.txt` / `.json`)
- 🖧 صفحهٔ تنظیمات سرور/دیتابیس با تشخیص خودکار آی‌پی و پیش‌نمایش زندهٔ آدرس API
- 🔌 **بررسی شنوندهٔ SQL Server روی پورت ۱۴۳۳ و فعال‌سازی خودکار TCP/IP** — قبلاً می‌شد سرور
  کاملاً سالم باشد ولی گوشی هرگز وصل نشود؛ حالا نصب‌کننده قبل از گفتن «آماده است» این را چک
  (و در صورت نیاز با یک تأیید اصلاح) می‌کند
- 🖥️ **پنل مدیریت زیبا و واقعی** (همان طرح تأییدشده؛ دیگر پیش‌نمایش نیست): صفحهٔ HTML فارسی روی پورت
  ۹۵۹۵، سرمه‌ای یکدست، فونت وزیرمتن، کاشی‌های آماری، کارت‌های SQL/دیتابیس/سلامت/فعال‌سازی/ورود/ویزیتورها
- 🔒 رمز SQL Server هرگز نمایش داده نمی‌شود؛ رفتار دیتابیس همچنان کاملاً غیرتلفیقی
- ♻️ همان `install.ps1` هوشمند زیر پوسته اجرا می‌شود (بدون پارامتر، مثل قبل کاملاً پرسشی کار می‌کند)
- 🧰 نسخهٔ بدون پارامتر/گرافیکی: میان‌برهای «بازرسی و تعمیر»، «اتصال مستقیم SQL»، شروع/توقف سرویس

ساخت دوباره:

```
installer\build.bat      (ویندوز، نیازمند NSIS 3 از nsis.sourceforge.io)
./installer/build.sh     (لینوکس/CI)
```

جزئیات کامل (فهرست بخش‌ها، مسیرها، امنیت، حذف نصب و چک‌لیست تست):
[installer/README.md](installer/README.md)
