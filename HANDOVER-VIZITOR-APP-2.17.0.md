# تحویل نسخهٔ ۲٫۱۷٫۰ — آتیران ویزیتور (بازطراحی کامل گرافیکی: «گزارش طلایی»)

**تاریخ:** ۲۰۲۶‑۰۹‑۲۲ · **نسخه:** `2.17.0-direct` · **شمارهٔ ساخت (versionCode):** ۲۱۸۱۳
**صفحهٔ ریلیز:** <https://github.com/Companymeelano/Vizitor-Android/releases/tag/vizitor-app-direct-2.17.0>

> در این دور، هیچ منطق کاری تغییر نکرده است؛ تمام کار روی **ظاهر** برنامه بوده تا
> با نمونهٔ مرجعی که فرستادید (تصاویر M•A Report در مخزن) هم‌زبان شود.

---

## ۱) فایل نصب

| فایل | حجم | SHA‑256 |
|---|---|---|
| `vizitor-app/dist/Vizitor-2.17.0-direct.apk` (امضاشده) | ۴۱٫۸۳۰٫۰۵۴ بایت (≈۳۹٫۹ مبی‌بایت) | `24ef2f4540f867d54aa8685660fbc966d1e95b8d5e4441ac2328a9685d40c02a` |
| `Vizitor-2.17.0-direct-debug.apk` (نسخهٔ آزمون) | ۵۲٫۸۷۰٫۷۵۳ بایت | — |

- **کلید امضا تغییر نکرده:** `SHA-256 = 5ccad3b601496e698bae055a0729e1ef0be51fba86c5db0754cad055d4a6ac5a`
  (CN=Vizitor App Direct, O=Meelano, C=IR) → **به‌روزرسانی درجا** روی نسخهٔ نصب‌شده.
- گزارش کامل بیلد و امضا در `vizitor-app/apk-report.txt`. کامیت بیلد (CI): `57eab29`.
- بازرسی محتوای همین APK در CI انجام شد: نام بسته `ir.atiran.vizitor`، شمارهٔ ساخت ۲۱۸۱۳،
  نام نسخه `2.17.0-direct`، و همهٔ رشته‌های تازه («گزارش طلایی»، «نبض کسب‌وکار»،
  «روند فروش و تخفیف»، «نبض امروز»، «دسترسی سریع مدیریت»، «MEELANO STUDIO DESIGN») و
  کلاس‌های تازه (MaReport/MetalBarChart/Ring3DChart/LineTrendChart/MaPulseCard/MaToolGrid)
  داخل APK موجودند.

---

## ۲) زبانِ طراحی تازه («گزارش طلایی»)

از تصاویر مرجع، این زبان بصری استخراج و در کل برنامه پیاده شد:

| عنصر مرجع | معادل در برنامه |
|---|---|
| پس‌زمینهٔ مشکی گرم + طلای فلزی | پالت تازهٔ **«گزارش طلایی»** (پیش‌فرض) + تم روشن **«شامپاین روشن»** |
| کارت‌های تیره با حاشیهٔ باریک طلایی | `Modifier.metalPanel` — سطح فلزی + حاشیهٔ طلایی + خط نور بالای کارت |
| تیتر درشت طلایی + زیرنویس لاتین | `MaHeroTitle` (شیمر طلایی + زیرنویس «آتیران ویزیتور — ATIRAN • INTELLIGENT SALES EXPERIENCE») |
| کارت‌های شاخص (گردش مالی/مشتریان/چک‌ها/کالاها) | `KpiCard` و `MaMetricCard` بازنویسی‌شده: **نشان آیکن طلایی گوشه‌گرد** + خط کوتاه طلایی + مقدار درشت |
| جدول «نبض کسب‌وکار» با نوار وضعیت | `MaPulseCard` + `MaPulseRow` با رنگ‌های **پایدار (سبز) / نیازمند توجه (کهربایی) / بحران (قرمز)** |
| نمودار ستونی استوانه‌ای طلایی با بازتاب | `MetalBarChart` — گرادیان استوانه‌ای، درخشش نوک، سایهٔ عمق، **بازتاب کف** |
| نمودار خطی روند با پرشدگی | `LineTrendChart` — دو سری (فروش/تخفیف)، خطوط نرم، نقاط درخشان، راهنمای رنگ |
| حلقهٔ سه‌بعدی درخشان با نگین | `Ring3DChart` — عمق، باریکهٔ نور، ۴ نگین الماسی، درصد فارسی در مرکز |
| کاشی‌های Word/Excel/PDF/چاپ | `MaToolGrid` + `MaToolTile` (کاشی‌های رنگی با برچسب) |
| تنها نشان انتهای صفحه‌ها | `MeelanoPill` («MEELANO STUDIO DESIGN» + نشان M) و خط «برنامه‌نویس • میلاد یعقوبی» |
| حالت خالی با حلقهٔ تیک طلایی | `MaEmptyState` |
| آیکن‌های تب پایین در کادر طلایی | نوار پایین: کاشی طلایی گوشه‌گرد پشت آیکن تب فعال + برچسب طلایی |

همهٔ این‌ها **فقط با Canvas خودِ Compose** رسم می‌شوند — هیچ کتابخانهٔ نموداری اضافه نشد
(حجم APK سنگین نشد) و در حالت «گرافیک سبک» (`VizitorPerf`) انیمیشن‌ها خودکار خاموش می‌شوند.

---

## ۳) چه صفحه‌هایی تغییر کردند؟

| صفحه | تغییر |
|---|---|
| **پیشخوان** | تیتر قهرمان + نوار خلاصهٔ عددی (فروش امروز/هدف/ارسال‌شده/در انتظار) + کارت «نبض امروز» + کاشی‌های دسترسی سریع فلزی |
| **پنل مدیریت** | نوار خلاصه، «نبض کسب‌وکار» (وصول مطالبات، فاکتور رسمی، سلامت انبار، مشتریان خوش‌حساب، اتصال سرور)، نمودار ستونی فلزی، **روند فروش و تخفیف**، حلقهٔ سه‌بعدی وصول، جدول‌ها، و شبکهٔ «دسترسی سریع مدیریت» |
| **گزارشات / مشتری / ویترین / سبد / ویزیت / گفتگو / تنظیمات / ورود** | سربرگ بخش‌ها، کارت‌ها، کاشی‌های آماری و فوتر به زبان تازه بازرنگ و بازقاب شدند |
| **صفحهٔ اول (نقش‌ها)** | رنگ‌ها از پالت تازه می‌آیند (طلایی) — نقش «مدیریت» همان‌طور فعال است |

---

## ۴) صادقانه — نکات مهم

1. **آزمون روی گوشی واقعی انجام نشده است.** ساخت و امضا و بازرسی APK در CI انجام و تأیید می‌شود،
   ولی رفتار روی دستگاه را باید یک‌بار خودتان ببینید.
2. اگر تم قبلی (بنفش/زمرد/مروارید/رز) را دوست دارید، هیچ‌چیز از دست نرفته: **هر پنج تم قبلی
   در انتخابگر تم باقی مانده‌اند** و تم تازه فقط **پیش‌فرض** شده است (از دکمهٔ سوییچر تم در پیشخوان
   یا از تنظیمات قابل تغییر است).
3. اعداد «نبض کسب‌وکار» از همان دادهٔ واقعی همگام‌شده محاسبه می‌شوند (نسبت‌ها، بدون عدد ساختگی)؛
   اگر داده‌ای نباشد، کارت خالی/صفر نشان می‌دهد.
4. در `dist/` فقط APK همین نسخه نگه داشته می‌شود؛ نسخه‌های قبلی روی صفحه‌های ریلیز خودشان
   دست‌نخورده هستند (۲٫۱۴٫۰ / ۲٫۱۵٫۰ / ۲٫۱۶٫۰ / ۱٫۰٫۰).

---

## ۵) مسیر به‌روزرسانی

۱) `Vizitor-2.17.0-direct.apk` را روی همان گوشیِ نسخهٔ قبلی نصب کنید (بدون حذف برنامه).
۲) چون کلید امضا یکی است، اندروید به‌روزرسانی درجا را می‌پذیرد و داده‌های محلی می‌مانند.
۳) اولین اجرا: نقش «مامور فروش/ویزیتور» یا «مدیریت» را بزنید و نام کاربری و کلمهٔ عبور خود را
   در سامانهٔ آتیران وارد کنید.

---

Meelano Studio Design — Milad Yaghoobi · آتیران ویزیتور ۲٫۱۷٫۰
