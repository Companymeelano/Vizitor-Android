# تحویل نسخهٔ ۲٫۱۶٫۰ — آتیران ویزیتور (اتصال مستقیم SQL Server)

**تاریخ:** ۲۰۲۶‑۰۹‑۲۲ · **نسخه:** `2.16.0-direct` · **شمارهٔ ساخت (versionCode):** ۲۱۸۱۲
**صفحهٔ ریلیز:** <https://github.com/Companymeelano/Vizitor-Android/releases/tag/vizitor-app-direct-2.16.0>

---

## ۱) فایل‌های آمادهٔ نصب

| فایل | حجم | SHA‑256 |
|---|---|---|
| `vizitor-app/dist/Vizitor-2.16.0-direct.apk` (امضاشده، برای گوشی) | ۴۱٫۷۹۷٫۲۸۶ بایت | `ca40f68571dd2b01d5e911928fae78b108b7ec4334244e22a159b3de5e523cbc` |
| `Vizitor-2.16.0-direct-debug.apk` (روی صفحهٔ ریلیز) | ۵۲٫۸۰۵٫۲۱۷ بایت | `88d0a807378d04e4a80f48fdebe5e910f1b8db5ac07b0fad89511a2d32dad170` |

- **کلید امضا تغییر نکرده است:** اثر انگشت گواهی `SHA-256 = 5ccad3b601496e698bae055a0729e1ef0be51fba86c5db0754cad055d4a6ac5a`
  (CN=Vizitor App Direct, O=Meelano, C=IR) — **دقیقاً همان کلید ۲٫۱۵٫۰ و ۲٫۱۴٫۰**.
  👉 بنابراین نسخهٔ ۲٫۱۶٫۰ روی همان برنامهٔ نصب‌شدهٔ قبلی **به‌روزرسانی درجای** (بدون حذف و نصب مجدد) می‌شود.
- امضا با APK Signature Scheme v2 تأیید شد (`apksigner verify` → Verifies).
- بیلد موفق: اجرای CI شمارهٔ `35708841075` (کامیت `624b71e`) — گزارش کامل در `vizitor-app/apk-report.txt`.

---

## ۲) خواسته‌های این دور و آنچه انجام شد

### ۱. «لیست مشتریان همگام‌سازی نمی‌شود» — رفع شد
- ماژول تازهٔ `sqldirect/CustomerSync.kt` با **سه مسیر پله‌ای**:
  ۱) مجوز واقعی کاربر `dbo.sys_cus` ← ۲) مشتریان تخصیص‌یافته به ویزیتور (`CUSTOMERS.vis_rdf`)
  ← ۳) مسیر پشتیبان «همهٔ مشتریان فعال سامانه» (تا فهرست هرگز خالی نماند).
- **کوئری با ستون‌های واقعی همان سرور ساخته می‌شود** (`sys.columns` و در نبود دسترسی، فهرست ستون‌های
  تأییدشده): کم‌بودن/تغییرنام یک ستون دیگر کل فهرست را خالی نمی‌کند.
- دکمهٔ بزرگ **«همگام‌سازی مشتریان از سرور»** در تب «مشتری» + پیام صریح:
  تعداد مشتری، مسیرِ خواندن داده، و علت دقیق خطا.
- در همگام‌سازی کلی برنامه هم همین ماژول استفاده می‌شود و هشدار گویا در گزارش همگام‌سازی می‌آید.

### ۲. «منو خیلی شلوغ است؛ فقط با نام کاربری و رمز وارد شود» — ساده شد
- نوار پایین از **۶ تب به ۴ تب** رسید: «خانه | ویترین | مشتری | گزارشات» + دکمهٔ مرکزی سبد.
- بخش‌های «ثبت ویزیت»، «گفتگو»، «گزارشات»، «پنل مدیریت» و «تنظیمات و اتصال» به
  **کارت دسترسی سریع پیشخوان (۶ کاشی)** منتقل شدند.
- **کارت ورود بازنویسی شد:** فقط «نام کاربری» + «کلمهٔ عبور» + دکمهٔ «ورود به سامانه».
  «به‌خاطر سپردن» برای ورود سریع اجرای بعدی، و انتخاب ویزیتور از `dbo.sys_vis` به‌صورت
  مسیر جایگزین (اختیاری) باقی مانده است.

### ۳. «آیکن‌های نوار پایین بزرگ‌تر شوند» — بزرگ‌تر شدند
- ارتفاع نوار ۷۴ → **۸۶**، دایرهٔ آیکن تب ۳۶ → **۵۰**، تصویر داخل آن ۳۴ → **۴۶**،
  دکمهٔ مرکزی سبد ۶۲ → **۷۴** با آیکن ۳۴.
- برچسب تب‌ها با `AutoFitText` تا **۱۲٫۵** بزرگ‌تر شد (هرگز نمی‌بُرد)، نشانگر تب فعال
  ۳۰×۴ با حاشیهٔ طلایی ۲dp و هالهٔ رنگی ملایم.

### ۴. «پنل مدیریت با گزارش کامل و نمودار» — فعال شد
- در صفحهٔ اول، نقش **«مدیریت»** فعال شد؛ با آن، ورود **با نام کاربری و کلمهٔ عبور شخصی مدیر
  در آتیران** (جدول واقعی `dbo.sys_users`) انجام می‌شود و پنل مدیریت باز می‌شود.
- `ui/manager/ManagerAnalytics.kt` شاخص‌ها را از **دادهٔ واقعی همگام‌شده** محاسبه می‌کند
  (`server_invoices`, `customers`, `products`) — هیچ عدد ساختگی وجود ندارد.
- `ui/screens/ManagerScreen.kt` شامل:
  - ردیف KPI: فروش کل، تعداد فاکتور (رسمی/پیش)، میانگین فاکتور، جمع تخفیف، مشتریان،
    جمع بدهی، ارزش انبار و تعداد ناموجود.
  - **نمودار ستونی** فروش به تفکیک تاریخ + **دو نمودار حلقه‌ای** (سهم فاکتور رسمی/پیش‌فاکتور،
    و سهم دسته‌های کالا از ارزش انبار).
  - **جدول‌ها:** مشتریان بدهکار، برترین مشتریان، برترین کالاها (به ارزش موجودی)،
    آخرین فاکتورهای سامانه، و پیش‌فاکتورهای صادرشده از همین برنامه.
  - اگر با نقش ویزیتور وارد شده باشید، بنر راهنما + دکمهٔ طلایی «ورود با حساب مدیر» نشان داده می‌شود.

### ۵. «ارتقای گرافیکی همهٔ بخش‌ها با دکمه‌های جذاب» — انجام شد
- `ui/components/Glamour.kt`: **GlamourButton** (سه‌بعدی + گرادیان + درخشش عبوری + فشرده‌شدن هنگام
  لمس + متن خودتنظیم)، **KpiCard** (کاشی شاخص با قاب طلایی)، **GlamourCard** (قاب شیشه‌ای گزارش‌ها).
- `ui/components/Charts.kt`: بخش تازهٔ ۲٫۱۶ — **BarChart**، **DonutChart**، **ReportRow**
  (رسم کامل با Canvas خودِ Compose؛ بدون هیچ کتابخانهٔ بیرونی و بدون سنگین‌شدن APK)
  و در همان فایل، نمودارهای لاکچری ۲٫۱۵ (`NeonDonutChart`, `RoyalBarChart`, `TwinDonutChart`,
  `StatusDot`, `RankBadge3D`, `DepthBar`, `RoyalTable`) دست‌نخورده حفظ شدند.
- `ui/screens/ReportsScreen.kt`: دو بخش تازهٔ «گزارش مشتریان (بدهی و خرید)» و
  «خلاصهٔ انبار و کالا» با جدول و شاخص.
- تمام صفحه‌های ورود/تنظیمات هم به دکمه‌های GlamourButton مجهز شدند.

---

## ۳) نقشهٔ فایل‌های تغییریافته در این دور

```
sqldirect/CustomerSync.kt            (تازه) همگام‌سازی سه‌مسیرهٔ مشتریان + سازگاری ستون‌ها
sqldirect/VizitorRoleIntent.kt       (تازه) نقش ورود: ویزیتور یا مدیریت
sqldirect/SqlConnectionManager.kt    connected() برای تشخیص سریع وضعیت اتصال
sqldirect/SqldirectSync.kt           بخش مشتریان بازنویسی شد (CustomerSync)
VizitorViewModel.kt                  syncCustomersNow() + CustomerSyncState
data/repository/VizitorRepository.kt replaceCustomers()
ui/components/Glamour.kt             (تازه) دکمه/کاشی/قاب جذاب
ui/components/Charts.kt              بخش ۲٫۱۶: BarChart، DonutChart، ReportRow
ui/manager/ManagerAnalytics.kt       (تازه) محاسبهٔ شاخص‌ها از دادهٔ واقعی
ui/screens/ManagerScreen.kt          (تازه) پنل مدیریت (KPI + نمودار + جدول)
ui/screens/DashboardScreen.kt        کارت دسترسی سریع ۶ کاشی + چهار فراخوان تازه
ui/screens/CustomersScreen.kt        دکمهٔ همگام‌سازی مشتریان + پیام وضعیت
ui/screens/ReportsScreen.kt          گزارش مشتریان + خلاصهٔ انبار
ui/screens/DirectSqlScreen.kt        کارت ورود ساده (نام کاربری + کلمهٔ عبور)
ui/screens/SplashScreen.kt           فعال شدن نقش «مدیریت» + مسیر onManager
ui/navigation/VizitorNavigation.kt   مسیر MANAGER + نوار پایین ۴ تبی و بزرگ‌تر
app/build.gradle.kts                 versionCode 21812 / versionName 2.16.0-direct
.github/workflows/build-vizitor-app.yml  APP_VERSION=2.16.0 + متن ریلیز فارسی
```

---

## ۴) شفافیت — آنچه باید بدانید (بدون بزرگ‌نمایی)

1. **آزمون روی گوشی واقعی انجام نشده است.** محیط ساخت ما فقط بیلد می‌گیرد؛ رفتار برنامه روی
   دستگاه و شبکهٔ داخلی شما باید یک‌بار آزمایش شود. (بیلد و امضا و بازرسی APK با CI انجام و تأیید شد.)
2. **همگام‌سازی مشتریان به دسترسی شبکه/کاربر دیتابیس نیاز دارد.** اگر هیچ‌یک از سه مسیر داده
   برنگرداند، پیام دقیق (مسیر + علت خطا) در همان صفحه نمایش داده می‌شود تا عیب‌یابی سرِ جایی شود.
3. **ورود مدیر** با همان کاربران واقعی `dbo.sys_users` انجام می‌شود؛ پس اگر حساب مدیر در آتیران
   برای کاربر دیتابیس محدود مجاز نباشد، اتصال با پیام خطای سرور برمی‌گردد.
4. تب‌های «ویزیت» و «تنظیمات» از نوار پایین برداشته شدند و از **کارت دسترسی سریع پیشخوان**
   (و نوار بالای صفحه‌ها) باز می‌شوند — همین باعث ساده‌شدن منو شده است.
5. تصاویر دو کالای «شیرینی» و «نوشیدنی» به‌صورت جایگزین (انجیر/عسل) نگاشت شده‌اند؛ تولید تصویر
   در محیط ما محدودیت دارد. بقیهٔ ۹ تصویر کالا واقعی و اختصاصی هستند.
6. در `dist/` فقط APK همین نسخه نگه داشته می‌شود؛ نسخه‌های ۲٫۱۴٫۰، ۲٫۱۵٫۰ و ۱٫۰٫۰ روی
   صفحه‌های ریلیز خودشان دست‌نخورده باقی هستند.

---

## ۵) مسیر به‌روزرسانی روی گوشی‌ها

۱) روی همان گوشیِ نسخهٔ ۲٫۱۵٫۰، فایل `Vizitor-2.16.0-direct.apk` را نصب کنید (بدون حذف برنامهٔ قبلی).
۲) چون کلید امضا یکی است، اندروید به‌روزرسانی درجا را می‌پذیرد و داده‌های محلی (سبد، تنظیمات اتصال) می‌مانند.
۳) اولین اجرا: اگر نام کاربری/رمز قبلاً ذخیره شده باشد، دکمهٔ «ورود سریع» کافی است؛ در غیر این صورت
   نام کاربری و کلمهٔ عبور خودتان در آتیران را وارد کنید. برای پنل مدیریت، از صفحهٔ اول نقش
   «مدیریت» را انتخاب کنید (ورود با حساب شخصی مدیر).

---

Meelano Studio Design — Milad Yaghoobi · آتیران ویزیتور ۲٫۱۶٫۰
