# تحویل نسخهٔ نهایی اندروید «آتیران ویزیتور» با اتصال مستقیم SQL Server (پورت ۱۴۳۳)

این سند، همان چیزی است که خواستید: **بررسی فایل اندروید اضافه‌شده به پروژه + آماده‌سازی نسخهٔ اندروید
برای اتصال مستقیم روی پورت ۱۴۳۳ + خواندن جدول و ستون‌های ویزیتورها + هماهنگی کامل با نصب‌کنندهٔ
ویندوز + نسخهٔ نهایی برای نصب**.

مخزن → شاخهٔ `arena/01a0a7c6-vizitor` — پروژهٔ کامل سورس در پوشهٔ **`vizitor-app/`**.

---

> 🔧 برای مشکل «وصل نشدن به سرور» و شرح اشکالات پیدا/رفع‌شده، سند
> [`CONNECTION-FIX-FINAL.md`](CONNECTION-FIX-FINAL.md) را ببینید.
> 🎨 برای بازطراحی لاکچری (صفحهٔ اول، صفحهٔ اتصال، فونت فارسی، آیکن سه‌بعدی) سند
> [`UI-REDESIGN-2.13.6.md`](UI-REDESIGN-2.13.6.md) را ببینید.

## ۱) خروجی نهایی (فایل نصب)

| مورد | مقدار |
|---|---|
| صفحهٔ دانلود | https://github.com/Companymeelano/Vizitor/releases/tag/vizitor-app-direct-2.13.6 |
| فایل نصب | `Vizitor-2.13.6-direct.apk` |
| حجم | **39,492,745 بایت** |
| SHA-256 | `8acc2eab8cb18b371730d2bffa96b6526f739d6323c7761e3ee6e4e15704dfe8` |
| فایل آزمون (کنار نسخهٔ اصلی نصب می‌شود) | `Vizitor-2.13.6-direct-debug.apk` |
| نام/شمارهٔ نسخه | `2.13.6-direct` / شمارهٔ ساخت **۲۱۸۰۱** |
| پکیج | `ir.atiran.vizitor` (همان پکیج برنامهٔ خودتان) |
| کلید امضا | `CN=Vizitor App Direct, OU=Meelano Studio Design` — اثر انگشت گواهی (SHA-256): `5CCAD3B601496E698BAE055A0729E1EF0BE51FBA86C5DB0754CAD055D4A6AC5A` |

> این کلید **ثابت** است (تا ۲۰۵۶ معتبر). یعنی از این پس نسخه‌های بعدی روی همین نصب
> **به‌روزرسانی** می‌شوند و نیازی به حذف/نصب دوباره نیست.

### نصب گام‌به‌گام روی گوشی

۱. اگر فایل `Vizitor-v2.17.1-release.apk` (فایلی که به `main` آپلود کردید) روی گوشی نصب است،
   **اول آن را حذف کنید.** دلیل: آن APK با کلید «Android Debug» امضا شده و امضایش با این نسخه
   یکی نیست؛ اندروید اجازهٔ جایگزینی نمی‌دهد. (تنظیمات ذخیره‌شدهٔ اتصال هم یک‌بار پاک می‌شود.)
۲. فایل `Vizitor-2.13.6-direct.apk` را نصب کنید و اگر پیام آمد، «نصب از منابع نامشخص» را اجازه دهید.
۳. از این پس، هر نسخهٔ جدید را می‌توانید روی همین نصب به‌روز کنید.

---

## ۱.۵) نسخهٔ اصلاح‌شده پس از گزارش «وصل نمی‌شود»

پس از گزارش شما («پورت ۱۴۳۳ سبز است ولی برنامه وصل نمی‌شود»)، کل مسیر اتصال کد به کد بررسی شد و
**۱۰ اشکال واقعی** پیدا و رفع شد (ترتیب درایور روی اندروید، گرفته‌نشدن `Error`، نبود desugaring،
پاک‌سازی نشانی، انتخاب خودکار نشانی داخلی/اختصاصی، TLS، پیام‌های خطا، و در نصب‌کننده: باگ بررسی
شنوندهٔ ۱۴۳۳، حالت احراز هویت سرور، فعال‌سازی کاربر و `VIEW ANY DATABASE`، و خودآزمایی ورود).
جزئیات کامل + فایل نصبی سرور: [`DIRECT-SQL-FIX-REPORT.md`](DIRECT-SQL-FIX-REPORT.md)

در همین نسخه، دکمهٔ **«عیب‌یابی اتصال (۶ بررسی)»** به صفحهٔ اتصال اضافه شد که مرحله‌به‌مرحله می‌گوید
کجا گیر است و یک گزارش **بدون رمز** برای ارسال به پشتیبانی می‌سازد.

## ۲) ست کردن با نصب‌کنندهٔ ویندوز (همان چیزهایی که نصب‌کننده آماده می‌کند)

نصب‌کننده روی سرور، کارت اتصال را می‌سازد:

```
C:\Vizitor\setup\android-connect.txt      ← کارت متنی (همین را بچسبانید)
C:\Vizitor\setup\android-connect.json     ← فایل JSON (محتوایش را بچسبانید)
C:\Vizitor\setup\android-connect.png      ← تصویر QR (با دوربین بخوانید)
```

مسیر در برنامه:

**تنظیمات → «اتصال مستقیم به SQL Server» → «ورود به صفحهٔ اتصال مستقیم SQL»**

۱. متن کارت را در کادر اول بچسبانید و **«خواندن کارت اتصال»** را بزنید.
   با این کار: نشانی سرور (کلید `h` = آی‌پی داخلی)، آی‌پی اختصاصی (کلید `H`= بیرون شبکه)،
   **پورت ۱۴۳۳** (`p`)، نام دیتابیس (`d`) و **نام کاربر محدود** (`u`) خودکار پر می‌شود.
۲. اگر بیرون از فروشگاه هستید، کلید **«اتصال از بیرون شبکه»** را روشن کنید (از آی‌پی اختصاصی استفاده می‌کند).
۳. **رمز کاربر محدود دیتابیس** را وارد کنید (رمز در کارت نیست — همان رمزی که نصب‌کننده در فایل
   `C:\Vizitor\api\...` نگه می‌دارد و فقط مدیر سامانه دارد).
۴. **«تست اتصال و فهرست دیتابیس‌ها»** → پیام «اتصال برقرار است ✅ — N دیتابیس پیدا شد».
۵. نام دیتابیس حسابداری را از **«فهرست»** انتخاب کنید یا **دستی تایپ** کنید (اجباری نیست از فهرست باشد).
۶. **«اتصال به دیتابیس»** → تنظیمات با AES-GCM + Android Keystore روی گوشی ذخیره می‌شود و
   تیک‌های سلامت مسیر پیش‌فاکتور (`add_sail_pish`, `sailfact_pish`, …) نمایش داده می‌شود.

### سپس ورود ویزیتور (مرحلهٔ دوم اعتبارنامه)

۷. در کارت سوم: **نام کاربری و کلمهٔ عبور خودتان** در سامانهٔ آتیران را بزنید → **«ورود و بارگذاری ویزیتورها»**.
   * این رمز روی گوشی ذخیره **نمی‌شود** (فقط رمز مرحلهٔ ۱ رمزنگاری‌شده ذخیره می‌شود).
   * ورود فقط با جدول واقعی `dbo.sys_users` انجام می‌شود؛ حسابِ غیرفعال (`active=0`) و حساب قفل رد می‌شود.

> **چرا دو مرحله؟** چون کاربر محدود دیتابیس (`vizitor_android`) که نصب‌کننده می‌سازد، یک
> کاربر SQL است و ردیفی در `dbo.sys_users` ندارد؛ و کاربر ویزیتور در سامانه هم کاربر SQL نیست.
> برنامهٔ آزمایش‌شدهٔ قبلی («ویزیتور مستقیم») هم دقیقاً همین دو مرحله را داشت.

---

## ۳) بعد از ورود، چه چیزی خوانده و نمایش داده می‌شود

* **ویزیتورهای زیرمجموعهٔ شما** — با ستون‌های واقعی جدول `dbo.visitors`:
  `vis_rdf`, `vis_name`, `vis_cell`, `vis_tell1`, `vis_addre`, `VIs_region`, `vis_city`,
  `active`, `is_supervisor`, `eteb`, `per_p_d_naghd`, `per_p_d_check`, `TedadFactorMojazMande`
  و نقشهٔ درست دسترسی: `sys_users.user_id → sys_vis.UserID → sys_vis.shvis → visitors.vis_rdf`.
* **دامنهٔ دسترسی هر ویزیتور** — شمارش مجازهای واقعی از `sys_cus` (مشتری)، `sys_kal` (کالا)،
  `sys_anb` (انبار) با فیلتر شرکت (`SysID`).
* **فهرست کامل ستون‌های جدول `dbo.visitors`** — مستقیم از `sys.columns` + `sys.types`
  (نام، نوع، طول، تهی‌پذیری). ستون رمز **خوانده و نمایش داده نمی‌شود**.
* **تعداد کل ویزیتورهای سامانه** و **تیک‌های سلامت مسیر پیش‌فاکتور**.
* اتصال همیشه روی `Dispatchers.IO` است (هیچ کوئری روی ترد رابط کاربری اجرا نمی‌شود)،
  همهٔ کوئری‌ها **پارامتری**اند و کانکشن از pool برمی‌گردد (بدون نشتی).

### دسترسی‌ها (همان‌طور که نصب‌کننده می‌سازد)

`db_datareader` برای خواندن · `EXECUTE dbo.add_sail_pish` · `INSERT dbo.subsailtemp_pish` ·
`DELETE` داده نشده (عمداً). هیچ تنظیمی از IIS یا API لازم نیست؛ همه‌چیز مستقیم روی **۱۴۳۳** است.

---

## ۴) چه چیزی در این نسخه تغییر کرد (خلاصهٔ فنی)

* پوشهٔ جدید `vizitor-app/` = سورس کامل برنامهٔ اندروید + بستهٔ `sqldirect`:
  `SqlConnectionManager` (JDBC: mssql-jdbc + jTDS، pool، تایم‌اوت، پیام خطای فارسی)،
  `MeelanoDataSource` (ورود از `dbo.sys_users`، هویت ویزیتور، اطلاعات دیتابیس، سلامت پیش‌فاکتور)،
  `SecureDbStore` (AES-GCM + Keystore)، `ConnectCards` (خواندن کارت `vizitor://c?…` و JSON)،
  `VisitorRepository` (**جدول و ستون‌های ویزیتورها**)، `DirectSqlViewModel` و صفحهٔ `DirectSqlScreen`.
* صفحهٔ جدید دقیقاً از **تنظیمات** خودِ برنامه باز می‌شود؛ طرح و فونت و بقیهٔ صفحات دست‌نخورده است.
* بیلد خودکار: `.github/workflows/build-vizitor-app.yml` (JDK 17 + SDK 35) → ساخت debug و release،
  **بازرسی APK** (حضور درایورهای SQL، امضا، مشخصات نسخه) و ثبت گزارش در `vizitor-app/apk-report.txt`.

## ۵) نکته‌های صادقانه

* فایل `Vizitor-v2.17.1-release.zip` که آپلود شد **فقط APK** داشت (بدون سورس کد؛ کلاینت REST و بدون درایور SQL).
  بنابراین قابل تغییر نبود؛ نسخهٔ نهایی از **آخرین سورس موجود در مخزن (۲٫۱۳٫۵)** ساخته شد و کل مسیر
  اتصال مستقیم به آن اضافه شد. شمارهٔ ساخت را بالاتر از ۲۱۷۰۱ گذاشتم (۲۱۸۰۰) تا نصب/به‌روزرسانی راحت باشد،
  اما نام نمایشی صادقانه می‌گوید سورس پایه ۲٫۱۳٫۵ است.
* پیام «رمزنگاری TLS» به‌صورت پیش‌فرض روشن است (گواهی سرور اعتبارسنجی نمی‌شود). اگر سرور قدیمی
  اجازه نداد، همان کلید را خاموش کنید.
* **روی گوشی واقعی هنوز آزمایش نشده** — من به دستگاه دسترسی ندارم. اولین اجرا را با نسخهٔ
  `-debug` انجام دهید و اگر خطایی آمد، متن وضعیت (بدون رمز) را بفرستید تا سریع اصلاح کنم.
* آرشیو `Vizitor-2.14.2-AtiranSQL-Final.zip` را هم بررسی کردم: **هیچ برنامهٔ اندرویدی در آن نیست**
  (۵ فایل متنی: دو اسکریپت فقط‌خواندنی PowerShell و سه README)؛ معماری توصیف‌شده در آن IIS/PHP است
  که طبق دستور شما کنار گذاشته شده است.

---

طراحی و برنامه‌نویسی: **میلاد یقوبی** — گروه نرم‌افزاری **Meelano Studio Design**
