# گزارش رفع اشکال «اتصال مستقیم برنامهٔ ویزیتور به SQL Server روی پورت ۱۴۳۳»

**موضوعی که گزارش کردید:** پورت ۱۴۳۳ در `ping.eu` سبز است، تنظیمات سرور هم درست وارد شده،
ولی برنامه به دیتابیس وصل نمی‌شود و ورود/فراخوانی اطلاعات انجام نمی‌شود.

این سند نتیجهٔ بررسی **کد به کد کل مسیر اتصال** است: از دکمهٔ «تست اتصال» در برنامه تا درایور
JDBC، تا اسکریپت‌های نصب‌کنندهٔ ویندوز. **۱۰ اشکال واقعی** پیدا و رفع شد.

> نکتهٔ کلیدی که همه‌چیز را توضیح می‌دهد: سبز بودن پورت در `ping.eu` فقط یعنی «یک برنامه روی پورت
> ۱۴۳۳ گوش می‌دهد». از آن نقطه تا «ورودِ موفقِ برنامه» چند شرط دیگر لازم است که هیچ‌کدام با آن تست
> دیده نمی‌شوند — حالت احراز هویت سرور، فعال بودن کاربر SQL، دسترسی او به همان دیتابیس، درایور روی
> گوشی، و درست بودن نشانیِ استفاده‌شده (داخلی یا اختصاصی). شش مورد از ده اشکال زیر دقیقاً از همین
> خانواده‌اند و اکنون هم در برنامه و هم در نصب‌کننده کنترل می‌شوند.

---

## الف) اشکالاتی که در «برنامهٔ اندروید» پیدا و رفع شد

| # | اشکال (کد به کد) | نتیجه‌اش روی گوشی | رفع |
|---|---|---|---|
| ۱ | ترتیب انتخاب درایور اشتباه بود: `DirectSql.detect()` **اول `mssql-jdbc`** را امتحان می‌کرد که روی اندروید به `javax.naming` و `java.time` نیاز دارد (روی اندروید ۷/۸ وجود ندارند) | حتی با پورت باز، ساخت اتصال شکست می‌خورد | ترتیب برعکس شد: **jTDS اول** (روی اندروید آزمایش‌شده و بدون آن وابستگی‌ها)، مایکروسافت دوم |
| ۲ | هیچ‌جا `Throwable` گرفته نمی‌شد؛ فقط `Exception`. اما `NoClassDefFoundError` از خانوادهٔ **Error** است | اگر درایور بار نشود، پیام «وصل نشد» نمی‌دید؛ **برنامه می‌بست** | همهٔ مسیر اتصال `Throwable` را می‌گیرد و پیام فارسی می‌دهد |
| ۳ | بازگشت خودکار به درایور دوم وجود نداشت | اگر یک درایور روی گوشی کار نکند، هیچ جایگزینی امتحان نمی‌شد | اگر خطا «سطح درایور» باشد، درایور دوم خودکار امتحان می‌شود؛ **رمزِ اشتباه دوباره تکرار نمی‌شود** (تا حساب SQL قفل نشود) |
| ۴ | `java.time` روی اندروید ۷/۸ وجود ندارد و درایور مایکروسافت آن را می‌خواهد | روی گوشی‌های قدیمی خطای بارگذاری | `core library desugaring` فعال شد (`desugar_jdk_libs 2.1.2`) |
| ۵ | نشانی سرور پاک‌سازی نمی‌شد؛ کاربر ممکن بود `http://`، `host:1433`، `host,1433` یا فاصله وارد کند | ساخت اتصال به نشانی نامعتبر → عدم اتصال | `sanitizeHost()` + `cleanHost`/`cleanPort` اضافه شد |
| ۶ | اگر نشانی فعال (داخلی/اختصاصی) از شبکهٔ فعلی در دسترس نبود، هیچ تلاش دیگری نمی‌شد | حالت بسیار شایع: «پورت بیرون سبز است ولی آی‌پی داخلی در فرم است» → عدم اتصال | ابتدا دسترسی به نشانی فعال آزمایش می‌شود؛ اگر بسته بود، **نشانی دیگر خودکار** امتحان و انتخاب می‌شود (و در فرم ذخیره می‌شود) |
| ۷ | TLS در نشانی jTDS اعمال نمی‌شد (`ssl=…` نبود) و کلید «رمزنگاری TLS» فقط روی درایور مایکروسافت اثر داشت | روی سرورهای قدیمی یا جدید، یکی از دو حالت بی‌اثر بود | هر دو درایور همان کلید را رعایت می‌کنند (`ssl=require/off` + `socketTimeout`) |
| ۸ | هیچ ابزار عیب‌یابی داخل برنامه نبود | کاربر فقط «وصل نشد» می‌دید، بدون علت | **صفحهٔ عیب‌یابی گام‌به‌گام** اضافه شد (پایین توضیح داده شده) |
| ۹ | پیام‌های خطا علت‌های واقعی را تفکیک نمی‌کردند | «خطای دیتابیس» بدون راهنمایی | پیام‌های اختصاصی برای: سرورِ فقط‌ویندوزی (18456 + Windows Authentication)، رمز اشتباه، کاربر غیرفعال، دیتابیس ناموجود، نبود دسترسی به دیتابیس، تناقض TLS، Connection refused، timeout و نبود درایور |

## ب) اشکالاتی که در «نصب‌کننده/راه‌اندازی سرور» پیدا و رفع شد

| # | اشکال | نتیجه‌اش روی سرور | رفع |
|---|---|---|---|
| ۱۰ | **زیرفرمان `listener` در `api/sql_admin_tools.py` هرگز صدا زده نمی‌شد** (هندلر وجود داشت ولی در `main` هیچ شاخهٔ `if args.cmd == "listener"` نبود) | بررسی «آیا SQL روی ۱۴۳۳ گوش می‌دهد؟» در نصب‌کننده **همیشه به‌طور خاموش «سالم» رد می‌شد**؛ یعنی حتی اگر TCP/IP خاموش بود، نصب می‌گفت همه‌چیز خوب است | شاخهٔ dispatch اضافه شد و زیرفرمان‌های دیگر هم بازبینی شدند (`authmode`, `databases`, `health`, `probe` همه متصل‌اند) |
| ۱۱ | **حالت احراز هویت سرور هیچ‌جا بررسی نمی‌شد** | اگر سرور روی «فقط احراز هویت ویندوز» باشد، **هیچ کاربر SQL نمی‌تواند از گوشی وارد شود** — حتی با پورت باز و کاربر درست. برنامه فقط خطای 18456 می‌داد | زیرفرمان جدید و فقط‌خواندنی `authmode` + بررسی در `install.ps1` + (با تأیید) تغییر به حالت «SQL + ویندوز» (`LoginMode=2`) و یک‌بار ری‌استارت سرویس، سپس بازبینی |
| ۱۲ | کاربر ساخته‌شده تضمینِ **فعال بودن** و `CONNECT SQL` نداشت | کاربری که نصب‌کننده می‌ساخت، محتمل بود غیرفعال بماند → ورود ناموفق | `ALTER LOGIN … ENABLE` + `GRANT CONNECT SQL` اضافه شد |
| ۱۳ | بدون `VIEW ANY DATABASE`، برنامه در مرحلهٔ «فهرست دیتابیس‌ها» فقط دیتابیس‌های خودش را می‌دید | مرحلهٔ انتخاب دیتابیس ناقص یا خالی | `GRANT VIEW ANY DATABASE` (فقط اجازهٔ دیدن نام دیتابیس‌ها؛ هیچ دسترسی داده‌ای نمی‌دهد) |
| ۱۴ | هیچ **خودآزماییِ ورودِ واقعی** با کاربر برنامه وجود نداشت | نصب «موفق» تمام می‌شد ولی معلوم نبود از بیرون می‌شود وارد شد یا نه | گزینهٔ `--self-test` در `provision_android_sql.py`: بعد از اعطای دسترسی‌ها، با همان کاربر/رمز **واقعاً وارد دیتابیس می‌شود** و نتیجه را در گزارش می‌نویسد (`self_test: ok / no-access / failed`) |
| ۱۵ | متن کارت اتصال، «یک رمز» را القا می‌کرد | ویزیتور رمز اشتباه را در جای اشتباه می‌زد | کارت اکنون **دو اعتبارنامه** را شفاف توضیح می‌دهد: الف) کاربر محدود دیتابیس و محل فایل رمز آن، ب) خودِ ویزیتور در `sys_users` + یادآوری کلید «اتصال از بیرون» |

---

## ج) «عیب‌یابی اتصال» داخل برنامه — دیگر حدس نمی‌زنیم

در صفحهٔ **تنظیمات → اتصال مستقیم به SQL Server → ورود به صفحهٔ اتصال مستقیم SQL**،
دکمهٔ **«عیب‌یابی اتصال (۶ بررسی)»** اضافه شده است. هر گام نتیجهٔ ✔/✖ و راهنمای فارسی می‌دهد:

۱. **نشانی سرور** (و پاک‌سازی آن) ۲. **تبدیل نام دامنه** (اگر دامنه وارد شده باشد)
۳. **دسترسی شبکه به پورت ۱۴۳۳** — همان کاری که `ping.eu` می‌کند، ولی از همین گوشی
۴. **هر دو نشانی** (داخلی و اختصاصی) جداگانه — معلوم می‌شود الان کدام در دسترس است
۵. **بارگذاری هر دو درایور** روی همین گوشی (jTDS و مایکروسافت) + نام درایور فعال
۶. **ورود واقعی با کاربر SQL** + خواندن `SERVERPROPERTY('IsIntegratedSecurityOnly')` و
`SUSER_SNAME()` (متوجه می‌شود سرور فقط‌ویندوزی است یا نه) ۷. **دسترسی به دیتابیس حسابداری**
(`HAS_DBACCESS` + `DB_NAME`)

و دکمهٔ **«کپی گزارش عیب‌یابی (بدون رمز)»** متن کامل گزارش را برای ارسال به پشتیبانی کپی می‌کند.
**هیچ رمزی در گزارش نیست** (نه رمز دیتابیس، نه رمز ویزیتور).

---

## د) فایل‌های نهایی (نسخهٔ اصلاح‌شده)

### ۱. برنامهٔ اندروید (APK)

| مورد | مقدار |
|---|---|
| صفحهٔ دانلود | https://github.com/Companymeelano/Vizitor/releases/tag/vizitor-app-direct-2.13.5 |
| فایل نصب | `Vizitor-2.13.5-direct.apk` |
| حجم | **۳۹٬۱۷۱٬۳۹۷ بایت** |
| SHA-256 | `3b6b261d8c0767f5658c0cf9c5209ef1945da5c47f2e945c81c204c8a8c6548d` |
| نسخه / پکیج | `2.13.5-direct` (شمارهٔ ساخت ۲۱۸۰۰) / `ir.atiran.vizitor` |
| امضا (ثابت) | `CN=Vizitor App Direct, OU=Meelano Studio Design` — اثر انگشت `5CCAD3B6…AC5A` |
| نسخهٔ آزمون | `Vizitor-2.13.5-direct-debug.apk` (۴۷٬۳۸۴٬۵۹۲ بایت) |
| شاهد حضور کد در APK | گزارش CI: `SqlDiagnostics`، `SqlConnectionManager`، `DirectSql`، jTDS و `mssql-jdbc` همه داخل dex هستند (`vizitor-app/apk-report.txt`) |

### ۲. فایل نصبی سرور (راه‌اندازی بخش اتصال به ۱۴۳۳)

| مورد | مقدار |
|---|---|
| صفحهٔ دانلود | https://github.com/Companymeelano/Vizitor/releases/tag/vizitor-setup-v1.0.0 |
| فایل | `Vizitor-Setup-1.0.0.exe` |
| حجم | **۹۷۷٬۳۲۵ بایت** (نسخهٔ قبلی ۹۷۴٬۸۳۰ بود) |
| SHA-256 | `7a961c1bda291c812d7a6f9041d4a7151ec376ea2c67c95e4e576f2d86f3f59d` |
| شاهد محتوا | گزارش CI: `install.ps1` داخل exe بایت‌به‌بایت همان فایل مخزن است (`d5ef3c97…`) — یعنی اصلاح‌ها **واقعاً داخل نصب‌کننده‌اند** (`installer/exe-build-report.txt`) |

---

## ه) کارهایی که باید انجام دهید (به همین ترتیب)

### گام ۱ — روی گوشی
۱. اگر `Vizitor-2.13.5-direct.apk` قبلی نصب است: **همان را نصب کنید (به‌روزرسانی)** — کلید امضا ثابت است
و حذف/نصب لازم نیست. (اگر نسخهٔ `2.17.1` آپلودی روی گوشی است، اول آن را حذف کنید.)
۲. برنامه → **تنظیمات → اتصال مستقیم به SQL Server → ورود به صفحهٔ اتصال مستقیم SQL**
۳. کارت اتصال را بچسبانید (`vizitor://c?…`) → **خواندن کارت اتصال**
۴. اگر بیرون از فروشگاه هستید، کلید **«اتصال از بیرون شبکه»** را روشن کنید
۵. رمز **کاربر محدود دیتابیس** را وارد کنید → **«تست اتصال و فهرست دیتابیس‌ها»**
۶. دیتابیس را انتخاب یا دستی تایپ کنید → **«اتصال به دیتابیس»**
۷. نام کاربری و کلمهٔ عبور خودتان → **«ورود و بارگذاری ویزیتورها»**

### گام ۲ — اگر هنوز وصل نشد (این گام را رد نکنید)
در همان صفحه، **«عیب‌یابی اتصال (۶ بررسی)»** را بزنید. با یک نگاه معلوم می‌شود کجا گیر است:

| اگر این سطر ✖ شد | یعنی | کار |
|---|---|---|
| دسترسی شبکه به پورت ۱۴۳۳ | از این شبکه، سرور روی آن نشانی/پورت پاسخ نمی‌دهد | نشانی درست (داخلی در فروشگاه / اختصاصی بیرون) و فوروارد پورت در روتر |
| ورود به SQL Server + پیام «فقط احراز هویت ویندوز» | سرور فقط ورود ویندوزی را می‌پذیرد | گام ۳ پایین (نصب‌کننده خودش پیشنهاد می‌دهد) |
| ورود + پیام «رمز اشتباه است» | رمزِ کاربر محدود دیتابیس غلط است | رمز در سرور: `C:\ProgramData\Vizitor\data\android_app_password.txt` |
| دسترسی به دیتابیس حسابداری | کاربر به آن دیتابیس دسترسی ندارد | گام ۳ پایین (نصب‌کننده دوباره اعطا می‌کند) |
| درایورها | هیچ درایوری بار نشد | نسخهٔ `-debug` را نصب کنید و همین گزارش را بفرستید |

### گام ۳ — روی سرور (نصب‌کنندهٔ جدید)
۱. `Vizitor-Setup-1.0.0.exe` **جدید** (۹۷۷٬۳۲۵ بایت) را روی سرور اجرا کنید — این نسخه فایل‌های
   `C:\Vizitor` را به‌روز می‌کند و **هیچ تغییری در دیتابیس‌ها/داده‌ها نمی‌دهد**.
۲. از منوی استارت: **«اتصال مستقیم SQL»** را اجرا کنید (همان `install.ps1 -AndroidPrepOnly`).
   این مرحله: حالت احراز هویت را (اگر فقط‌ویندوزی باشد) با تأیید به «SQL + ویندوز» تغییر می‌دهد،
   سرویس SQL را یک‌بار ری‌استارت می‌کند، کاربر محدود را فعال و دسترسی‌ها را اعطا می‌کند،
   **با همان کاربر واقعاً وارد دیتابیس می‌شود** و در پایان می‌گوید `خودآزمایی ورود: … ✓`.
۳. کارت اتصال تازه: `C:\Vizitor\setup\android-connect.txt` (و `.json` و QR).

> اگر می‌خواهید بدون اجرای کامل نصب‌کننده فقط همین بخش را اجرا کنید، همان میان‌بر
> «اتصال مستقیم SQL» کافی است؛ ولی فایل‌های `C:\Vizitor` باید با نصب‌کنندهٔ جدید به‌روز شده باشند.

---

## و) شفاف و صادقانه

* شش علتِ بالا (TLS، حالت احراز هویت، دسترسی دیتابیس، نشانی داخلی/اختصاصی، درایور، سرورِ فقط‌ویندوزی)
  همه از یک جنس‌اند: **با pinging پورت دیده نمی‌شوند**. به همین دلیل هم تشخیص از راه دور سخت بود،
  و هم اکنون «عیب‌یابی» داخل برنامه اضافه شده تا بدون حدس، علت مشخص شود.
* این نسخه روی **گوشی واقعی آزمایش نشده** (به دستگاه دسترسی ندارم). برای همین:
  اگر خطایی دیدید، فقط دکمهٔ «کپی گزارش عیب‌یابی» را بزنید و همان متن را بفرستید — گزارش بدون رمز است.
* تغییر حالت احراز هویت سرور (`LoginMode=2`) یک تنظیم سروری است، نه تغییر داده؛ در نصب‌کننده
  پیشنهاد داده می‌شود و مسیر دستی (SSMS) هم چاپ می‌شود. ری‌استارت سرویس SQL هم یک‌بار و
  با همان رویه‌ای است که پیش‌تر برای فعال‌سازی TCP/IP انجام می‌شد.

طراحی و برنامه‌نویسی: **میلاد یقوبی** — گروه نرم‌افزاری **Meelano Studio Design**
