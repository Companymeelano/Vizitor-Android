> **آخرین به‌روزرسانی (۲۰۲۶-۰۹-۲۲):** نسخهٔ ۲٫۱۵٫۰ آماده و منتشر شد —
> «ورود ویزیتور از `dbo.sys_vis`»، ویترین تصویری با قیمت مصرف‌کنندهٔ پیش‌فرض و میانگین قیمت هر قلم،
> رفع بارگذاری نام مشتریان/گزارشات، و ارتقای همهٔ بخش‌ها.
> سند تحویل کامل: **`HANDOVER-VIZITOR-APP-2.15.0.md`**
> ریلیز: https://github.com/Companymeelano/Vizitor-Android/releases/tag/vizitor-app-direct-2.15.0
>
# بررسی پروژهٔ ویزیتور و ساخت نسخهٔ قابل نصب اندروید

> ℹ️ **به‌روزرسانی (۲۰۲۶-۰۹-۲۲):** این سند به برنامهٔ کوچک‌تر «ویزیتور مستقیم» (`android-app/`) مربوط است.
> نسخهٔ درخواستی شما — **آتیران ویزیتور ۲٫۱۴٫۰** با «انتخاب نقش» در صفحهٔ اول (`vizitor-app/`) — جداگانه ساخته شد:
> فایل نصب [`vizitor-app/dist/Vizitor-2.14.0-direct.apk`](vizitor-app/dist/Vizitor-2.14.0-direct.apk) و
> راهنمای کامل در [`HANDOVER-VIZITOR-APP-2.14.0.md`](HANDOVER-VIZITOR-APP-2.14.0.md).

**تاریخ:** ۲۰۲۶-۰۹-۲۲ • **شاخه:** `arena/01a0c76f-vizitor-android` • **مخزن:** `Companymeelano/Vizitor-Android`
**نتیجه:** ✅ برنامهٔ اندروید **«ویزیتور مستقیم» ۱٫۰٫۰** ساخته، امضا و منتشر شد — فایل نصب آماده است.

---

## ۱) خلاصه در یک نگاه

| مورد | مقدار |
|---|---|
| فایل نصب (نسخهٔ امضاشدهٔ اصلی) | `VizitorDirect-1.0.0.apk` — **۷٬۶۲۳٬۴۱۵ بایت** (۷٫۳ مگابایت) |
| SHA-256 | `44019a2ea9bc539e131f9a6bfbff2c2db521e544a356e86a8bc25a96638d11ea` |
| امضا | `apksigner verify` → **Verifies** • طرح امضای **v2** • گواهی `CN=Vizitor Direct, OU=Meelano Studio Design, O=Meelano, L=Tehran, C=IR` |
| اثر انگشت گواهی (SHA-256) | `1b47b00dc3f393a3cfe7814c65b99ed8539ab05d017887482179b0a1ba67f84a` |
| کمینه/هدف اندروید | `minSdk 24` (اندروید ۷ و بالاتر) • `targetSdk 34` |
| بستهٔ برنامه | `ir.atiran.vizitor.direct` (نسخهٔ debug: `…direct.debug` — در کنار نسخهٔ اصلی نصب می‌شود) |
| تاریخ ساخت / کامیت | 2026-09-22 05:07:01 UTC • `d00f5a9` |
| ورک‌فلوی موفق | `Build Vizitor Direct APK` — اجرای `35689176742` (۴ دقیقه و ۲۲ ثانیه، موفق) |

**دانلود فایل نصب (۲ راه):**
1. صفحهٔ ریلیز: <https://github.com/Companymeelano/Vizitor-Android/releases/tag/vizitor-direct-v1.0.0>
2. داخل خودِ مخزن (اگر دسترسی به بخش ریلیز/Artifacts بسته باشد): [`android-app/dist/VizitorDirect-1.0.0.apk`](android-app/dist/VizitorDirect-1.0.0.apk)
   — همراه `BUILD-INFO.txt` و `VizitorDirect-1.0.0.apk.sha256`

> نسخهٔ آزمون (`VizitorDirect-1.0.0-debug.apk` — ۱۰٬۷۱۴٬۳۵۸ بایت) هم در همان ریلیز هست و
> **کنار** نسخهٔ اصلی نصب می‌شود (شناسهٔ بستهٔ جدا دارد)، پس می‌توانید هر دو را هم‌زمان روی یک گوشی داشته باشید.

---

## ۲) نقشهٔ پروژه — چه چیزهایی در این بایگانی‌ها هست؟

| مسیر | خطوط/حجم | نقش |
|---|---|---|
| `android-app/` | ۱۶ فایل Kotlin — **۳٬۶۶۷ خط** | برنامهٔ کامل اندروید «ویزیتور مستقیم» (Gradle + Android Studio) |
| `api/` | **۲٬۷۶۹ خط** پایتون | سرور API فقط با کتابخانهٔ استاندارد: `server.py` (۷۶۸)، `sql_admin_tools.py` (۵۴۵)، `check_connection_contract.py` (۳۸۲)، `provision_android_sql.py` (۳۲۷)، `android_connect.py` (۲۷۴)، `db.py` (۲۴۵)، `provision_sqlserver.py` (۱۵۹)، `seed.py` (۶۹) + `vendor/segno` (ساخت QR) |
| `database/` | ۳ فایل SQL | اسکیمای SQLite / MySQL-MariaDB / Microsoft SQL Server |
| `panel/` | HTML + فونت وزیرمتن | پنل مدیریت فارسی (سرو می‌شود از خودِ سرویس روی پورت ۹۵۹۵، بدون IIS) |
| `install.sh` / `install.ps1` | ۳۸ / ۸۶ کیلوبایت | نصب‌کنندهٔ هوشمند لینوکس/ویندوز: تشخیص خودکار سرور، پرسش اطلاعات، نصب، بازرسی و تعمیر خودکار |
| `android-sql-direct/` | ۸۲۴ کیلوبایت | ممیزی و مستندسازی لایهٔ SQL (اسکیمای استخراج‌شدهٔ ATIRAN، گزارش‌های audit، پیش‌فاکتور) |
| `.github/workflows/` | ۴ ورک‌فلو | ساخت APK • ساخت اپ کامل (`vizitor-app`) • بررسی/ساخت نصب‌کنندهٔ ویندوز • **آزمون واقعی اتصال با SQL Server زنده** |
| `.toolchain/nsis` | ۵ مگابایت | ابزار NSIS برای ساخت نصب‌کنندهٔ ویندوز |
| مستندات | ۱۳ فایل md | `README`, `INSTALL`, `CONNECTION-FIX-FINAL`, `DIRECT-SQL-FIX-REPORT`, `UI-REDESIGN-2.13.6`, `VISIT-REGISTRATION-2.14.0`, `ANDROID-DIRECT-HANDOVER`, … |

---

## ۳) معماری برنامهٔ اندروید (فایل‌به‌فایل)

مسیر داده: **UI (Compose) → `DirectViewModel` → `MeelanoDataSource` → `SqlConnectionManager` → JDBC → SQL Server:1433**

| فایل | خطوط | کار |
|---|---|---|
| `data/sql/MeelanoDataSource.kt` | ۸۰۸ | تمام کوئری‌های واقعی: ورود `dbo.sys_users` (رمز `varbinary`)، مشتریان و گروه‌ها، کالا/قیمت/تیر قیمت، موجودی انبار، شناسنامهٔ ویزیتور، مسیر **پیش‌فاکتور** (`dbo.add_sail_pish` + جدول میانی + تریگر)، سلامت پیش‌فاکتور |
| `data/sql/SqlConnectionManager.kt` | ۳۴۳ | استخر اتصال (۴)، اعتبارسنجی `isValid`، **۲ بار retry** با backoff ۷۰۰ms برای خطاهای گذرا، تراکنش ایمن (commit/rollback)، خطاهای فارسی، `ConnectionState` زنده |
| `direct/DirectViewModel.kt` | ۳۶۳ | وضعیت برنامه: `SetupForm` → فهرست دیتابیس‌ها → اتصال → ورود → داده‌ها (همه روی `Dispatchers.IO`) |
| `direct/MainActivity.kt` | ۳۳۱ | چهار صفحه: **راه‌اندازی اتصال / ورود / خانه / شناسنامه** + چیدمان RTL + دریافت کارت اتصال `vizitor://` از بیرون برنامه |
| `direct/ui/screens/SetupScreen.kt` | ۲۸۷ | آی‌پی داخلی و اختصاصی، پورت، کاربر/رمز، دکمهٔ «فهرست دیتابیس‌ها»، انتخاب دیتابیس، تست اتصال، اسکن QR |
| `direct/ui/components/Widgets.kt` | ۲۹۵ | اجزای ظاهری (کارت، دکمهٔ طلایی، چیپ وضعیت، …) |
| `direct/ui/screens/HomeScreen.kt` | ۲۵۲ | مشتریان مجاز، کالا و موجودی، جست‌وجو |
| `data/local/SecureDbStore.kt` | ۱۶۵ | ذخیرهٔ تنظیمات اتصال با **AES-GCM + Android Keystore** (کلید غیرقابل‌خروج) — رمز به‌صورت متن هرگز ذخیره نمی‌شود |
| `direct/ui/screens/LoginScreen.kt` | ۱۱۵ | ورود ویزیتور با حساب خودش در `dbo.sys_users` |
| `direct/util/ConnectCards.kt` | ۱۱۲ | خواندن کارت اتصال نصب‌کننده: QR / فایل `android-connect.json` / متن |
| `direct/ui/screens/AboutScreen.kt` | ۱۰۴ | شناسنامه و امضای سازنده |
| `direct/ui/theme/Theme.kt` | ۷۳ | تم سرمه‌ای/طلایی، فونت وزیرمتن |
| `data/repository/ServerConfig.kt` | ۶۴ | نگاشت تنظیمات ⇄ لایهٔ داده |
| `data/sql/DirectSql.kt` | ۶۳ | کمکی‌های SQL |
| `direct/DirectApplication.kt` | ۱۵ | راه‌اندازی `SecureDbStore` |
| `src/test/…/RealSqlConnectionTest.kt` | ۲۷۷ | **۸ گروه آزمون روی SQL Server واقعی** در CI (ورود، دادهٔ کاری، امنیت کاربر محدود، مسیر نوشتن پیش‌فاکتور) |

**زنجیرهٔ ابزار بیلد:** Gradle 8.7 (wrapper داخل مخزن) • AGP 8.5.2 • Kotlin 1.9.24 • Compose BOM 2024.06.00 (کامپایلر 1.5.14) • JDK 17 • `mssql-jdbc 9.4.1.jre8` (درایور اصلی) + `jtds 1.3.1` (پشتیبان) + `zxing-android-embedded 4.3.0` (اسکن QR).

---

## ۴) نقطه‌قوت‌های پروژه (چیزهایی که واقعاً خوب انجام شده)

1. **تفکیک لایه‌ها:** هیچ کوئری‌ای در UI یا ViewModel نیست؛ همهٔ SQL در `MeelanoDataSource` جمع شده. تغییر جدول/ستون فقط یک فایل را لمس می‌کند.
2. **امنیت رمز:** رمز SQL با AES-GCM و کلید Android Keystore رمزنگاری می‌شود، هرگز در UI نمایش داده نمی‌شود، در لاگ نمی‌آید و `android:allowBackup="false"` است. هیچ رمز پیش‌فرضی در کد نیست.
3. **کاربر محدود دیتابیس:** نصب‌کننده/API کاربر `vizitor_android` با `db_datareader` و فقط دسترسی لازم برای پیش‌فاکتور می‌سازد — نه `sa`.
4. **اثبات اتصال، نه ادعا:** ورک‌فلوی `verify-sql-connection.yml` یک SQL Server ۲۰۱۹ واقعی بالا می‌آورد و همان کد لایهٔ داده را با ۸ گروه آزمون اجرا می‌کند؛ خروجی‌اش در `sql-connection-report.txt` ثبت می‌شود.
5. **گزارش ماشین‌خوان در مخزن:** `android-app/apk-report.txt` (حجم، SHA-256، تأیید امضا با apksigner، شمار dex، وجود کلاس‌های کلیدی در dex) — بدون نیاز به دسترسی به لاگ CI قابل بازبینی است.
6. **زیبایی و یکدستی:** فونت وزیرمتن (با مجوز SIL OFL داخل APK)، راست‌به‌چپ، تم سرمه‌ای، هم‌رنگ پنل و نصب‌کنندهٔ ویندوز؛ کارت اتصال QR هماهنگ با نصب‌کننده.
7. **هیچ وابستگی سنگین سمت سرور:** API با کتابخانهٔ استاندارد پایتون و بدون IIS؛ پنل و فونت هم داخل خودِ پروژه.

---

## ۵) ریسک‌ها و نکته‌های صادقانه (چیزهایی که باید بدانید)

1. **`mssql-jdbc` رسمی برای اندروید نیست.** پروژه این را با انتخاب `9.4.1.jre8` + درایور پشتیبان **jTDS** و انتخاب خودکار درایور مدیریت کرده است؛ روی بعضی گوشی‌ها درایور اصلی بار نمی‌شود و برنامه به jTDS سوییچ می‌کند (کندتر، ولی کار می‌کند). این را خودِ پروژه هم صادقانه نوشته است.
2. **پورت ۱۴۳۳ روی اینترنت = سطح حمله.** توصیهٔ جدی: یا از VPN/شبکهٔ داخلی استفاده کنید، یا روی فایروال سرور فقط آی‌پی‌های مجاز را باز کنید. کاربر دیتابیس باید همان `vizitor_android` محدود بماند (نه `sa`) و رمز آن قوی باشد.
3. **کلید امضا.** برای این نسخه کلید **پایدار** ساختیم و در مخزن گذاشتیم: `android-app/vizitor-ci.jks` (رمز `vizitorci`، alias `vizitor`) — همین کلید برای بیلدهای بعدی استفاده می‌شود، پس نسخه‌های بعدی **روی همین نصب به‌روزرسانی می‌شوند** و رمز SQL ذخیره‌شدهٔ کاربران از بین نمی‌رود.
   ⚠️ صادقانه: چون این کلید در مخزن (و مخزن عمومی) است، هر کسی که دسترسی نوشتن به مخزن داشته باشد می‌تواند بیلد امضاشده بسازد. برای انتشار رسمی/فروشگاهی، کلید خصوصی خودتان را نگه دارید و `android-app/keystore.properties` را بدهید (نمونه‌اش در `android-app/README.md` آمده).
4. **حجم ۷٫۳ مگابایتی:** `isMinifyEnabled = false` عمداً خاموش است تا R8 هوک‌های رفلکشن درایور JDBC و Compose را نشکند. اولویت درست: پایداری، نه کوچک‌ترین حجم.
5. **یک مقدار سخت‌کدشده برای تمیزکاری:** `MeelanoDataSource.kt` خط ۸۰۱ آی‌پی پیش‌فرض `192.168.1.150` (آی‌پی شبکهٔ همان مشتری) را به‌عنوان مقدار پیش‌فرض نگه داشته است. کار برنامه را خراب نمی‌کند (کاربر در صفحهٔ راه‌اندازی همیشه می‌تواند عوضش کند) ولی برای نسخه‌های بعدی بهتر است به مقدار خالی/`0.0.0.0` یا «آخرین مقدار ذخیره‌شده» تغییر کند.
6. **`vizitor-app/` در این بایگانی‌ها نبود.** مستندات به پروژهٔ کامل «آتیران ویزیتور ۲٫۱۳٫۶ تا ۲٫۱۴٫۰» (با ثبت ویزیت `dbo.Visit`) اشاره می‌کنند، اما در بایگانی‌های تحویلی فقط `android-app` (همین «ویزیتور مستقیم ۱٫۰٫۰») هست. به همین دلیل دو ورک‌فلو (`build-vizitor-app.yml` و `verify-installer.yml`) به پوشه‌هایی اشاره می‌کنند (`vizitor-app/`, `installer/`) که در این مخزن وجود ندارند و اجرا نمی‌شوند. اگر آن نسخهٔ کامل‌تر را می‌خواهید، بگویید تا از مخزن `Companymeelano/Vizitor` بیاورم و برای همان هم APK بسازم.
7. **آزمون‌های غیرقابل‌خودکار:** نصب روی گوشی واقعی و اجرای `Vizitor-Setup-1.0.0.exe` روی ویندوز مشتری را CI نمی‌تواند انجام دهد؛ دستی باقی می‌ماند (خود پروژه هم همین را نوشته). آزمون‌های «اتصال به SQL Server واقعی» خودکار است.
8. **حداقل اندروید ۷ (API 24)** — چون درایور JDBC به API 24 نیاز دارد.

---

## ۶) شواهد ساخت (چطور ساخته شد)

```
ورک‌فلو : .github/workflows/build-android-direct.yml  (Build Vizitor Direct APK)
اجرا    : 35689176742 — موفق در ۴ دقیقه و ۲۲ ثانیه، ورودی: کامیت d00f5a9
گام‌ها   : بررسی کد → JDK 17 → کلید پایدار → gradlew assembleDebug assembleRelease
          → آماده‌سازی dist/ → گزارش apk-report.txt → ثبت APK در مخزن → artifact → ریلیز
```

| فایل | حجم | SHA-256 |
|---|---|---|
| `VizitorDirect-1.0.0.apk` (اصلی) | 7,623,415 بایت | `44019a2ea9bc539e131f9a6bfbff2c2db521e544a356e86a8bc25a96638d11ea` |
| `VizitorDirect-1.0.0-debug.apk` (آزمون) | 10,714,358 بایت | `3336b5060de31c6e7bca32f554c15bdae76968129773b5d8ff216a979ce39dbb` |

**بازرسی داخل APK (توسط `apksigner` و `unzip` روی رانر):** تأیید امضا ✅ • ۲ فایل dex • وجود
`com/microsoft/sqlserver/jdbc/SQLServerDriver`، `net/sourceforge/jtds/jdbc/Driver`،
`ir/atiran/vizitor/direct/MainActivity`، `ir/atiran/vizitor/data/sql/MeelanoDataSource`،
`com/journeyapps/barcodescanner` و طرح `vizitor://` • مجوز فونت `assets/licenses/Vazirmatn-OFL.txt` ✅

---

## ۷) نصب روی گوشی (گام‌به‌گام)

1. فایل `VizitorDirect-1.0.0.apk` را دانلود کنید (از صفحهٔ ریلیز یا از `android-app/dist/`).
2. فایل را با کابل/بلوتوث/تلگرام به گوشی منتقل کنید — یا اگر لینک ریلیز را روی خودِ گوشی باز کنید، مستقیم دانلود می‌شود.
3. فایل را باز کنید؛ اگر گوشی اجازه نداد: **تنظیمات → نصب برنامه‌های ناشناس** برای همان برنامهٔ فایل‌منیجر/مرورگر را روشن کنید.
4. «نصب» را بزنید. نام برنامه روی گوشی: **ویزیتور مستقیم**.
5. اگر پیام «برنامهٔ ناسازگار» یا نصب‌نشد دیدید: گوشی باید اندروید **۷ یا بالاتر** باشد.

> نکته: نسخهٔ `debug` را فقط برای آزمون نصب کنید؛ **نسخهٔ اصلی (`VizitorDirect-1.0.0.apk`) همان است که باید روی گوشی‌های ویزیتورها نصب شود.**

---

## ۸) راه‌اندازی اتصال در برنامه (یک‌بار برای هر گوشی)

**راه سریع (پیشنهادی):** در سرور، نصب‌کننده `Vizitor-Setup-1.0.0.exe` (یا نسخهٔ لینوکسی) **کارت اتصال** می‌سازد (QR / فایل `android-connect.json` / متن).
در برنامه: صفحهٔ «راه‌اندازی اتصال» → **اسکن کارت اتصال** → آدرس/پورت/دیتابیس از کارت پر می‌شود →
نام کاربری و رمز دیتابیس را یک‌بار وارد کنید → **«دریافت فهرست دیتابیس‌ها»** → دیتابیس حسابداری را انتخاب کنید → **«اعمال تنظیمات و تست اتصال»**.

**راه دستی:** آی‌پی داخلی شبکه (`hostLan`) و آی‌پی اختصاصی/اینترنتی (`hostPublic`) را وارد کنید، پورت `1433`، کاربر `vizitor_android` و رمزش، سپس همان مراحل بالا.

پس از آن:
* ورود هر ویزیتور با **نام کاربری و رمز خودش** در `dbo.sys_users` انجام می‌شود.
* تنظیمات به‌صورت **رمزنگاری‌شده** روی گوشی می‌ماند و اجرای بعدی برنامه، خودش وصل می‌شود.
* دکمه‌های «فراموش کردن همه‌چیز»/«خروج» برای پاک‌کردن اطلاعات روی گوشی موجودند.

---

## ۹) عیب‌یابی سریع

| نشانه | علت محتمل و راه‌حل |
|---|---|
| «نصب نشد / برنامه نصب نمی‌شود» | اجازهٔ «نصب از منابع ناشناس» را بدهید؛ اگر نسخهٔ قدیمی با کلید دیگری نصب بوده، اول آن را حذف کنید |
| «اتصال برقرار نشد (timeout)» | پورت ۱۴۳۳ روی فایروال سرور باز نیست، یا TCP/IP در SQL Server غیرفعال است. با ابزار نصب‌کننده `Test-SqlTcpListener`/`Enable-SqlTcp` فعال کنید |
| «لیست دیتابیس‌ها خالی است» | کاربر دیتابیس دسترسی `db_datareader` به `sys.databases` ندارد یا رمز/سرور اشتباه است |
| «ورود ناموفق» | حساب ویزیتور در `dbo.sys_users` غیرفعال/قفل است یا رمز اشتباه است |
| «همه‌چیز درست بود ولی بعداً قطع شد» | برنامه خودش ۲ بار retry می‌کند؛ اگر ادامه داشت، VPN/شبکه و آدرس (داخلی ↔ اختصاصی) را بررسی کنید |

---

## ۱۰) ساخت مجدد و ادامهٔ کار

* هر تغییر در کد/پیکربندی `android-app/vizitor-direct/**` (یا فایل‌های Gradle) روی شاخه‌های `main` یا `arena/**` **خودکار APK می‌سازد** و در صورت موفقیت، ریلیز `vizitor-direct-v1.0.0` و فایل `android-app/dist/` را به‌روز می‌کند.
* نکتهٔ مهم دربارهٔ هش فایل: هر بیلد، کامیت خودش را داخل APK ثبت می‌کند
  (`META-INF/version-control-info.textproto`) و بیلد اندروید بیت‌به‌بیت تکرارپذیر نیست؛ پس **SHA-256 هر بیلد عوض می‌شود ولی گواهی/امضا ثابت می‌ماند**
  (اثر انگشت گواهی کلید پایدار: `1b47b00d…`). همیشه تازه‌ترین فایلِ صفحهٔ ریلیز یا `android-app/dist/` معتبر است.
* برای نسخهٔ بعدی: در `android-app/vizitor-direct/build.gradle.kts` مقدارهای `versionCode` و `versionName` را بالا ببرید؛ با **همان کلید پایدار** به‌روزرسانی روی نسخهٔ نصب‌شده انجام می‌شود (بدون حذف برنامه).
* برای امضای اختصاصی خودتان: یک کلید بسازید و `android-app/keystore.properties` را کنار پروژه بگذارید (روش کامل در `android-app/README.md`).

---

## پیوست: کارهایی که در همین نوبت انجام شد

1. **استخراج کل پروژه از بایگانی‌های zip** به ساختار واقعی مخزن (پیش‌تر فقط پنج فایل zip در مخزن بود).
2. **اصلاح ورک‌فلوی ساخت:** بیلد فقط روی شاخهٔ یک جلسهٔ قدیمی (`arena/01a0a7c6-vizitor`) اجرا می‌شد؛ حالا روی `main` و `arena/**` اجرا می‌شود.
3. **تشخیص خودکار `apksigner`** از نسخهٔ build-tools موجود روی رانر (مسیر ثابت `34.0.0` روی رانرهای تازه وجود ندارد و مرحلهٔ بازرسی بی‌صدا رد می‌شد).
4. **کلید امضای پایدار** (`android-app/vizitor-ci.jks`) به‌جای کلید تازه در هر بیلد → به‌روزرسانی بدون حذف برنامه.
5. **محدودکردن محرّک بیلد به کد/پیکربندی** (تغییر مستندات دیگر APK را بازسازی نمی‌کند).
6. **ثبت خودِ APK در مخزن** (`android-app/dist/`) + `BUILD-INFO.txt`، تا اگر دانلود از بخش Artifacts/ریلیز در شبکه‌ای بسته بود، فایل از خودِ مخزن هم قابل گرفتن باشد.
7. **ساخت و امضا و انتشار** نسخهٔ ۱٫۰٫۰ و ثبت گزارش بازرسی در `android-app/apk-report.txt`.
8. **همین سند بررسی** (این فایل).

---

*طراحی و برنامه‌نویسی برنامه: میلاد یقوبی — گروه نرم‌افزاری Meelano Studio Design.*
