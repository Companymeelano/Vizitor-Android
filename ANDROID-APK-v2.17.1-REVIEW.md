# بررسی فایل اندروید تازه‌افزوده‌شده — `Vizitor-v2.17.1-release.zip`

**تاریخ بررسی:** ۲۰۲۶-۰۹-۱۹ · **محل فایل:** شاخهٔ `main`، کامیت `d033ba6` («Add files via upload» — Meelano Studio Design)
**روش بررسی:** باز کردن کامل بسته و APK، پارس `AndroidManifest.xml` دودویی، خواندن جدول رشته‌های `dex`،
`resources.arsc`، بلوک امضای APK و گواهی X.509 — بدون نیاز به aapt/apksigner.

---

## ۱) خودِ فایل

| مورد | مقدار |
|---|---|
| نام فایل | `Vizitor-v2.17.1-release.zip` (شاخهٔ `main`) |
| حجم | ۱۳٬۹۷۸٬۹۸۸ بایت (≈ ۱۳.۳ مگابایت) |
| sha256 (زیپ) | `cb29fa3284c672252dadd2c55463c4e8fb595faecaa23bc755b0c6f1d49b660a` |
| محتوای زیپ | **فقط یک فایل**: `Vizitor-v2.17.1-release.apk` |
| حجم APK | ۲۶٬۲۷۵٬۰۱۱ بایت (≈ ۲۵.۱ مگابایت) |
| sha256 (APK) | `f6496b8b526327867323c021bd5aa8f13aa7d1440f691d1171ab8e3dfc7bf945` |
| تاریخ بسته‌بندی | ۲۰۲۶-۰۹-۱۶ ۱۲:۳۳:۴۰ |
| سلامت بسته | هر ۴۷۵ ورودی CRC سالم؛ زیپ سالم ✓ |

## ۲) شناسنامهٔ برنامه

| مورد | مقدار |
|---|---|
| `package` | **`ir.atiran.vizitor`** |
| نسخه | `versionName = 2.17.1` · `versionCode = 21701` |
| کمینهٔ اندروید | `minSdkVersion = 24` (اندروید ۷.۰ و بالاتر) |
| هدف | `targetSdkVersion = 35` · `compileSdkVersion = 35` |
| کلاس برنامه | `ir.atiran.vizitor.VizitorApp` · `MainActivity` |
| مجوزها | INTERNET، ACCESS_NETWORK_STATE، CAMERA، موقعیت دقیق/تقریبی، POST_NOTIFICATIONS، VIBRATE، WAKE_LOCK، RECEIVE_BOOT_COMPLETED، FOREGROUND_SERVICE |
| نکتهٔ امنیتی manifest | `usesCleartextTraffic = False` (فقط HTTPS) · `allowBackup = False` |
| کتابخانه‌ها | Jetpack Compose، Navigation، Room، WorkManager، Retrofit/OkHttp، Gson، **ML Kit barcode scanning** (مدل‌ها همراه بسته)، CameraX |
| معماری‌های نیتیو | arm64-v8a، armeabi-v7a، x86، x86_64 |
| dex | ۲ فایل (۴٬۸۷۸٬۳۰۰ + ۲۷۰٬۳۷۲ بایت) |

### امضا

| مورد | نتیجه |
|---|---|
| طرح امضا | **v2 موجود** · **v1 (JAR) ندارد** · **v3 ندارد** |
| گواهی | `CN = Android Debug, O = Android, C = US` — سریال `01`، صادر ۲۰۲۶-۰۹-۱۶ |
| اثر انگشت SHA-256 | `9E:40:2B:46:4B:13:F5:8E:90:BC:C8:17:F6:8D:0D:40:2F:66:9D:8A:FE:5A:27:46:E1:EE:2C:86:74:23:B0:08` |

**معنی این امضا:** فایل با **کلید Di­bug** اندروید امضا شده، نه با کلید انتشار. برای نصب دستی روی گوشی
کار می‌کند (چون v2 روی اندروید ۷+ کافی است و `minSdk` هم ۲۴ است)، ولی:
* نسخه‌های بعدی اگر با کلید دیگری امضا شوند، **روی همین نصب به‌روزرسانی نمی‌شوند** (باید اول حذف شود)؛
* این فایل به‌عنوان «نسخهٔ انتشار» قابل اتکا نیست (هر کسی که کلید دیباگ را داشته باشد می‌تواند
  نسخه‌ای با همان هویت امضا کند).

## ۳) برنامه چه کاری می‌کند (از داخل dex)

* **معماری: کلاینت REST** — `VizitorApiService` با Retrofit/OkHttp و پوشش `ApiEnvelope`؛ مدل‌های
  `LoginRequest/LoginUserDto`، `ProductDto`، `CustomerDto`، `SalMaliRowDto`، `InvoiceHeaderRequest`،
  `InvoiceLineRequest`، `SubmitInvoiceResponse`، `NewCustomerRequest/Response`، `PingDto`.
* **نام سرویس‌های API که در کد دیده می‌شود:** `ping`، `login`، `logout`، `getProducts`،
  `getCustomers`، `getInvoices`، `submitInvoice`، `getSalMali`، `getServerTime`، `health`، `register`.
* **پایگاه‌دادهٔ محلی (Room، حالت آفلاین):** جدول‌های `products`، `customers`، `invoices`،
  `invoice_items`، `cart_items`، `chat_messages`، `sal_mali_history`.
* **همگام‌سازی پس‌زمینه:** `SyncWorker` (WorkManager) با کلید `vizitor_periodic_sync`.
* **گفتگوی هوش مصنوعی:** مدل `gemini-1.5-flash` از طریق یک پروکسی، با آدرس **جای‌نگهدار**:
  `https://vizitor-ai-proxy.YOUR-SUBDOMAIN.workers.dev` → تا وقتی این آدرس واقعی نشود، بخش چت کار نمی‌کند.
* **امکانات دیگر از کلیدواژه‌ها:** چک/چک‌های پیگیری (`ChequeFollowUp`، `vizitor_cheques`)، سبد خرید،
  تنظیمات و تم (`vizitor_settings`، `vizitor_theme_prefs`)، احراز هویت (`vizitor_auth`).
* **رابط کاربری فارسی:** ۴۸۰ رشتهٔ فارسی داخل dex (فاکتور، همگام‌سازی، گفتگو، دستیار، …).
* **کلید/توکن هارد‌کدشده:** بررسی شد — هیچ (نه `AIza…`، نه `sk-…`، نه `Bearer`، نه `AKIA…`) ✓
* **فونت همراه بسته:** ندارد (هیچ فایل فونتی داخل APK نیست) → فارسی با فونت سیستم نمایش داده می‌شود.

## ۴) مهم‌ترین یافته: این برنامه به SQL Server وصل **نمی‌شود**

* در کل dex **هیچ نشانه‌ای از درایور SQL Server نیست**: نه `jdbc:sqlserver`، نه `net.sourceforge.jtds`،
  نه `mssql-jdbc`، و هیچ اشاره‌ای به جدول‌هایی مثل `sys_users` / `sys_cus` / `visitors` یا دیتابیس
  `atiran2` / `Meelano` وجود ندارد.
* در عوض، تمام ارتباط از طریق **HTTP(S) + Retrofit/OkHttp** انجام می‌شود؛ یعنی این برنامه همان
  معماری قدیمی **«اندروید → API → SQL Server»** را می‌خواهد.
* نصب‌کننده‌ای که الان داریم (طبق درخواست خودتان: «نیاز به IIS و API نیست، بخش‌های اضافی حذف شوند»)
  **هیچ سرویس API نصب نمی‌کند**؛ فقط پورت ۱۴۳۳ را برای اتصال مستقیم دیتابیس باز می‌کند و کارت اتصال می‌سازد.
  → اگر این APK را روی همان سرور نصب کنید، چیزی برای حرف زدن ندارد: ورود و همگام‌سازی خطا می‌دهند.
* چون `usesCleartextTraffic=False` است، سرویس API (اگر برگردد) باید **HTTPS با گواهی معتبر** باشد؛
  `http://` ساده رد می‌شود.

## ۵) تفاوت با نسخهٔ اندرویدی که خودمان ساختیم (`vizitor-direct`)

| مورد | `VizitorDirect-1.0.0.apk` (کار ما) | `Vizitor-v2.17.1-release.apk` (فایل جدید) |
|---|---|---|
| معماری | **مستقیم به SQL Server، پورت ۱۴۳۳** | REST API (نیازمند IIS/PHP یا سرویس مشابه) |
| درایورهای SQL | mssql-jdbc + jTDS داخل بسته | ندارد |
| ورود | جدول واقعی `dbo.sys_users` ERP | `login` روی سرویس API |
| امضا | کلید انتشار CI (`CN=Vizitor Direct, O=Meelano`) | کلید **Di­bug** اندروید |
| حجم | ۷٬۶۲۳٬۴۱۵ بایت | ۲۶٬۲۷۵٬۰۱۱ بایت |
| `applicationId` | `ir.atiran.vizitor.direct` | `ir.atiran.vizitor` |
| هم‌زیستی | — | چون شناسه فرق دارد، **هر دو با هم روی یک گوشی نصب می‌شوند** |
| امکانات | ویزیتور + پیش‌فاکتور مستقیم روی SQL | چت هوش مصنوعی، چک، سبد خرید، حالت آفلاین |

## ۶) کد منبع این نسخه در مخزن نیست

تنها فایل منتشرشده، خودِ APK است. در شاخه‌های دیگر مخزن، نسخه‌های **قدیمی‌ترِ** همین خانواده دیده می‌شود
(`versionName = 1.6.0` روی `arena/01a09dd8-vizitor` و `2.13.5` روی `arena/01a09f68-vizitor`)،
ولی **۲.۱۷.۱ در تاریخ مخزن سورس ندارد** → بدون سورس، نه می‌توان آن را بازبینی کرد و نه اشکالش را رفع.

## ۷) نتیجهٔ کوتاه

| پرسش | پاسخ |
|---|---|
| فایل سالم و نصب‌شدنی است؟ | ✅ بله (اندروید ۷+، امضای v2، سالم) |
| با نصب‌کنندهٔ فعلی ما (اتصال مستقیم SQL، بدون IIS) کار می‌کند؟ | ❌ **نه** — به سرویس HTTP(S) API نیاز دارد |
| با سرور فعلی و پورت ۱۴۳۳ حرف می‌زند؟ | ❌ نه؛ هیچ درایور/کد SQL داخلی ندارد |
| برای انتشار نهایی مناسب است؟ | ⚠️ با کلید Di­bug امضا شده؛ برای انتشار واقعی باید با کلید انتشار امضا شود |
| چت هوش مصنوعی کار می‌کند؟ | ❌ آدرس پروکسی جای‌نگهدار است (`YOUR-SUBDOMAIN`) |
| کد منبعش در مخزن هست؟ | ❌ نه |
| با APK خودمان تعارض دارد؟ | ✅ نه؛ هر دو با هم نصب می‌شوند (`…vizitor` ≠ `…vizitor.direct`) |

## ۸) برای اینکه این نسخه قابل استفاده شود، سه چیز لازم است

1. **کد منبع ۲.۱۷.۱** (تا بازبینی و رفع اشکال ممکن باشد) — یا تصمیم بگیریم نسخهٔ `vizitor-direct` خودمان مبنا باشد.
2. **سرویس API روی سرور** + گواهی HTTPS (یعنی برگشتن همان اجزای IIS/API که قبلاً حذف شدند)؛
   در غیر این صورت این برنامه روی سرور فعلی هیچ کاری نمی‌تواند بکند.
3. **کلید انتشار واقعی** برای امضا (تا به‌روزرسانی نسخه‌های بعدی ممکن باشد) و اصلاح آدرس پروکسی چت.

---

## به‌روزرسانی (نتیجهٔ نهایی)

از آن‌جا که سورس ۲٫۱۷٫۱ در اختیار نبود، **نسخهٔ نهایی** از آخرین سورس موجود در مخزن (۲٫۱۳٫۵) ساخته شد
و کل مسیر «اتصال مستقیم روی پورت ۱۴۳۳» (خواندن جدول و ستون‌های ویزیتورها + هماهنگی با کارت اتصال
نصب‌کننده) به آن اضافه شد؛ همچنین یک **کلید امضای ثابت** تنظیم شد تا نسخه‌های بعدی روی همین نصب
به‌روزرسانی شوند. جزئیات و لینک فایل نصب: [`ANDROID-DIRECT-HANDOVER.md`](ANDROID-DIRECT-HANDOVER.md)

طراحی و برنامه‌نویسی: **میلاد یقوبی** — گروه نرم‌افزاری **Meelano Studio Design**
