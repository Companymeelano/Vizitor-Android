# تحویل نسخهٔ ۲٫۱۵٫۰ برنامهٔ اندروید «آتیران ویزیتور» (اتصال مستقیم SQL Server)

**تاریخ:** ۲۰۲۶-۰۹-۲۲ · **سازندهٔ بیلد:** GitHub Actions (run `35701434982` — موفق) · **کامیت پایهٔ بیلد:** `fd66cd8`

---

## ۱) فایل نصبی (همین را روی گوشی نصب کنید)

| فایل | حجم | sha256 |
|---|---|---|
| `vizitor-app/dist/Vizitor-2.15.0-direct.apk` | ۴۱٬۷۳۱٬۷۴۶ بایت (~۳۹٫۸ مگابایت) | `dc95ef311f66c76d6ed5c6039efc8bece0ec9ac0b6710a0ea46f86f039dbfeac` |
| `Vizitor-2.15.0-direct-debug.apk` (نسخهٔ آزمون) | ۵۲٬۷۰۶٬۸۵۴ بایت | `84092a239ab7d5de1e7c9ab076d359515af300ef52cb0ad4cd6820ae816b0fe3` |

* **لینک ریلیز (دانلود مستقیم):** https://github.com/Companymeelano/Vizitor-Android/releases/tag/vizitor-app-direct-2.15.0
* **شناسهٔ برنامه:** `ir.atiran.vizitor` · **نام نسخه:** `2.15.0-direct` · **شمارهٔ ساخت:** `21811` (بالاتر از ۲۱۸۱۰ ⇒ روی گوشی‌های موجود **درجا به‌روزرسانی** می‌شود)
* **امضا:** APK Signature Scheme **v2** تأیید شد (`apksigner`)، گواهی:
  `5C:CA:D3:B6:01:49:6E:69:8B:AE:05:5A:07:29:E1:EF:0B:E5:1F:BA:86:C5:DB:07:54:CA:D0:55:D4:A6:AC:5A`
  (`CN=Vizitor App Direct, O=Meelano, C=IR`) — **همان کلید ۲٫۱۴٫۰**، پس نصب روی نسخهٔ قبلی بدون حذف داده انجام می‌شود.
* جزئیات بازرسی بیلد: `vizitor-app/apk-report.txt` (۵ فایل dex، حضور درایورهای mssql-jdbc و jTDS، متادیتای aapt2).

---

## ۲) خواستهٔ اول — «ورود ویزیتورها از جدول `dbo.sys_vis`، بدون پرسیدن نام کاربری/رمز آتیران»

**قبل:** برنامه اول نام کاربری و کلمهٔ عبور کاربر را در `dbo.sys_users` چک می‌کرد و بعد رابطهٔ او با ویزیتور را از `sys_vis` پیدا می‌کرد.
**حالا:** مرحلهٔ اول کاملاً حذف شده است؛ برنامه فهرست **ویزیتورهای واقعی** را می‌خواند و کاربر فقط ویزیتورش را انتخاب می‌کند.

* فایل تازه: `sqldirect/VisitorLogin.kt`
  * کوئری (پارامتری و فقط `SELECT`، بدون هیچ ستون رمزی):
    `dbo.sys_vis` ⋈ `dbo.visitors` روی `vis_rdf = shvis` — با نام، موبایل، منطقه/شهر و وضعیت فعال.
  * برای هر ویزیتور، تعداد مجوزهای واقعی‌اش هم شمرده می‌شود:
    مشتری از `dbo.sys_cus`، کالا از `dbo.sys_kal`، انبار از `dbo.sys_anb` (همه با `UserID` و `SysID`).
  * جست‌وجوی محلی روی نام/موبایل/کد ویزیتور — بدون کوئری اضافه.
* `DirectSqlViewModel`:
  * `loadVisitorsForLogin()` — خواندن فهرست (اگر اتصال برقرار نباشد، با همان تنظیمات ذخیره‌شده وصل می‌شود؛ **هیچ رمزی پرسیده نمی‌شود**).
  * `enterAsVisitor(option)` — ورود با `UserID` (کلید مجوزها) + `SysID` (شرکت) + `shvis` (کلید فاکتورها) و سپس همگام‌سازی کامل کالا/مشتری/فاکتور.
  * ویزیتور انتخاب‌شده در **حافظهٔ امن AES-GCM + Keystore** ذخیره می‌شود (`SecureDbStore.saveVisitor/loadVisitor/clearVisitor`) ⇒ اجرای بعدی «ورود سریع» یک‌ضربه‌ای.
  * بعد از اتصال موفق، فهرست ویزیتورها **خودکار** خوانده می‌شود؛ و بعد از انتخاب ویزیتور، **خودکار** وارد پنل می‌شوید.
* `DirectSqlScreen` — کارت «گام ۳» بازنویسی شد: کاشی انتخاب ویزیتور با جست‌وجو (نام، موبایل، کد ویزیتور، تعداد مشتری/کالای مجاز) + هشدار اگر ویزیتور غیرفعال باشد (`visitors.active = 'f'`).
* **مسیر قدیمی حذف نشد، اختیاری شد:** «ورود دستی با نام کاربری آتیران (اختیاری)» برای کاربران مدیریتی هنوز کار می‌کند.

> ⚠️ **نکتهٔ امنیتی که باید بدانید:** طبق درخواست شما، انتخاب ویزیتور در این مسیر **بدون احراز هویت شخصی** است؛ یعنی هر کسی که رمز «کاربر محدود دیتابیس» روی گوشی را داشته باشد می‌تواند خودش را ویزیتور دیگری معرفی کند. اگر لازم شد، می‌توانم یک «رمز چهاررقمی ویزیتور» (ذخیره‌شده در همان حافظهٔ رمزنگاری‌شده) روی این مرحله اضافه کنم.

---

## ۳) خواستهٔ دوم — ویترین حرفه‌ای: تصویر، فونت/جدول/دکمه، قیمت مصرف‌کنندهٔ پیش‌فرض، میانگین قیمت هر قلم

### الف) تصویر کالا بر اساس «نام کالا» (فهرست پیش‌فرض تصاویر)
* فایل تازه: `ui/catalog/ProductVisuals.kt` — جدول نگاشت نام ⇒ تصویر (ترکیب‌های خاص قبل از واژه‌های عام: «بادام هندی» قبل از «بادام»).
* **۱۱ تصویر تازه** در `res/drawable-nodpi/` (عکس استودیویی ۶۴۰×۶۴۰ سبک‌سازی‌شده، ~۶۵ کیلوبایت هر کدام؛ روی زمینهٔ اسلیت تیره با نور طلایی — هم‌رنگ تم برنامه):

| تصویر | کلیدواژه‌های نام کالا |
|---|---|
| `goods_pistachio` | پسته |
| `goods_almond` | بادام |
| `goods_cashew` | بادام هندی، کاشو، فندق، نخودچی، چلغوز |
| `goods_walnut` | گردو |
| `goods_raisin` | کشمش، مویز، توت خشک |
| `goods_date` | خرما، رطب |
| `goods_fig` | انجیر، برگه، قیسی، آلو، زردآلو، خشکبار (+ تنقلات و شیرینی: نزدیک‌ترین عکس) |
| `goods_seeds` | تخمه، آفتابگردان، کدو، شاهدانه، هندوانه، ژاپنی |
| `goods_saffron` | زعفران، هل، دارچین، زردچوبه، فلفل، ادویه، سماق، زنجبیل |
| `goods_honey` | عسل، ارده، حلوا، کره، شیره (+ نوشیدنی: نزدیک‌ترین عکس) |
| `goods_mixed` | آجیل، مغز، مخلوط — و پیش‌فرض همهٔ کالاهای دیگر |

* تصویر کالا در **کارت ویترین**، **دیالوگ بزرگ‌نمایی** و کنار نام در دیالوگ «افزودن به سبد» نمایش داده می‌شود (تصویر + هالهٔ تیرهٔ ملایم تا نشان‌های موجودی/VIP کاملاً خوانا بمانند).
* حجم کل تصاویر تازه ≈ **۶۷۰ کیلوبایت** (APK از ۴۰٬۹۲۷٬۳۹۰ به ۴۱٬۷۳۱٬۷۴۶ بایت رسید).

### ب) قیمت مصرف‌کننده (پیش‌فرض بر اساس نام کالا) + میانگین قیمت هر قلم
* فایل تازه: `sqldirect/ProductDefaults.kt`
  * **قیمت مصرف‌کننده:** اول از سرور (`forosh_price.forosh3`). اگر صفر/خالی بود، از روی **قیمت پایه × حاشیهٔ سود دستهٔ همان نام کالا** ساخته می‌شود و در UI با برچسب **«پیش‌فرض ٪۲۰»** نشان داده می‌شود تا با عدد واقعی سرور اشتباه نشود.
    حاشیه‌ها: پسته/بادام‌های سخت ٪۱۸ · آجیل و مغزها ٪۲۰ · خشکبار ٪۲۲ · تخمه‌جات ٪۲۵ · ادویه ٪۳۰ · خواروبار ٪۲۵ · تنقلات ٪۲۸ · نوشیدنی ٪۲۰ · سایر ٪۲۰.
  * **میانگین قیمت هر قلم:** میانگین **سطح‌های غیرصفر** `forosh1..forosh5` همان کالا؛ اگر همه صفر بودند، میانگین `MinPrice` و `MaxPrice`. (هیچ عدد ساختگی داخل میانگین نمی‌رود.)
  * **دستهٔ کالا از نام** (آجیل و مغزها، خشکبار، تخمه‌جات، ادویه، خواروبار، تنقلات، نوشیدنی، سایر).
* جدول قیمت کارت کالا اکنون کامل است: فروش ۱، فروش ۲، قیمت مصرف‌کننده، **میانگین قیمت هر قلم**، و در صورت وجود: فروش ۴، فروش ۵، کمینهٔ مجاز، بیشینهٔ مجاز — همه با `PriceRow` (سطر جدول با نوار رنگی معنا و اعداد فارسی).
* نوار دسته‌ها در بالای ویترین (فیلتر با شمارش) + کاشی‌های آماری (کالا در ویترین / موجود / قلم در سبد) + سرستون جدول.
* پایگاه‌دادهٔ محلی: `products` با ستون‌های تازهٔ `price4, price5, minPrice, maxPrice, avgPrice, consumerIsDefault, category` — **مهاجرت دستی ۶→۷** نوشته شد تا سبد خرید، فاکتورهای در انتظار و گفتگوی ویزیتورها از دست نرود.

---

## ۴) خواستهٔ سوم — چرا «نام مشتریان» و «گزارشات» هنگام ورود درست بارگذاری نمی‌شد؟ (تشخیص + رفع)

دو ایراد واقعی در لایهٔ داده پیدا و رفع شد:

1. **نام مشتری روی فاکتورها خالی می‌ماند.** فاکتورهای سرور **شمارهٔ مشتری (`shmo`)** را برمی‌گردانند، ولی نقشهٔ نام مشتریان فقط با کلید **`code`** ساخته می‌شد؛ نتیجه: در «گزارشات»، سطر فاکتور همیشه «کد مشتری ۱۲۳۴» نشان می‌داد.
   **رفع:** نقشهٔ نام اکنون **هم با `shmo` و هم با `code`** پر می‌شود (`SqldirectSync`).
2. **فهرست مشتریان می‌تواند کاملاً خالی بماند.** مسیر اصلی مشتریان، جدول مجوز `dbo.sys_cus` است؛ اگر برای آن کاربر ردیفی نبود (یا `SysID` با شرکت مطابق نبود)، فهرست خالی می‌شد و همان هشدار «فهرست مشتریان مجاز این کاربر خالی است» ثبت می‌شد.
   **رفع:** تابع پشتیبان `customersForVisitor()` روی ستون واقعی **`CUSTOMERS.vis_rdf`** (مشتریان تخصیص‌یافته به همان ویزیتور، فقط `active = 't'`) اضافه شد و در صورت خالی‌بودن `sys_cus` به‌طور خودکار استفاده می‌شود.
3. **پیام‌های وضعیت گویا شدند** (به‌جای سکوت): «برای این ویزیتور مشتری مجازی در sys_cus و CUSTOMERS.vis_rdf پیدا نشد»، «برای این ویزیتور فاکتور یا پیش‌فاکتوری در سرور ثبت نشده»، «نام مشتری روی فاکتورها پیدا نشد (کلید shmo)».
4. **گزارشات** هم مثل بقیهٔ بخش‌ها از همان جریان `server_invoices` تغذیه می‌شود؛ حالا با کاشی‌های آماری (تعداد/جمع مبلغ/جمع تخفیف/میانگین هر فاکتور) و سرستون جدول — تا معلوم باشد داده آمده یا نه.

---

## ۵) خواستهٔ چهارم — ارتقای همهٔ بخش‌ها

* **مجموعهٔ تازهٔ UI** در `ui/components/AutoFit.kt`:
  * `AutoFitText` — متن هر دکمه/چیپ/سرستون **به‌صورت خودکار** تا اندازه‌ای می‌رسد که کامل جا شود (پله‌ای و تثبیت‌شده؛ بدون نوسان). پس هیچ برچسبی نمی‌بُرد.
  * `IdealButton` — دکمهٔ سه‌بعدی با لبهٔ عمق، گرادیان، فشرده‌شدن هنگام لمس و برچسب خودتنظیم.
  * `PriceRow` / `TableHeader` / `MiniStat` / `GoodsImage` — سطر جدول قیمت، سرستون جدول، کاشی آماری و تصویر کالا (همه قابل استفادهٔ مجدد در همهٔ بخش‌ها).
* **ویترین:** کاشی‌های آماری، فیلتر دسته، سرستون جدول، تصویر کالا، جدول قیمت کامل، دیالوگ بزرگ‌نمایی تصویری با دستهٔ کالا.
* **مشتریان:** کاشی‌های «کل مشتریان / دارای بدهی / جمع بدهی (ریال)» + سرستون جدول (نام‌ها و اعداد فارسی).
* **گزارشات:** کاشی‌های آماری + سرستون جدول + شمارش رکوردها.
* **ورود/تنظیمات:** کارت تازهٔ انتخاب ویزیتور + نشان‌گرهای پیشرفت + جدول مجوزهای هر ویزیتور.
* قواعد تازهٔ ویترین روی دادهٔ نمونهٔ اولیه (`SeedData`) هم اعمال شد تا پیش از اتصال به سرور، ویترین کامل دیده شود.

---

## ۶) تأیید فنی بیلد (مستقل از ادعا)

* GitHub Actions run `35701434982` → **موفق** (~۷ دقیقه، JDK 17، `:app:assembleDebug :app:assembleRelease`، امضای release با همان کلید P12 داخل مخزن).
* `apksigner verify` ⇒ `Verifies` + `Verified using v2 scheme: true`؛ گواهی مطابق ۲٫۱۴٫۰ (به‌روزرسانی درجا).
* `aapt2` ⇒ `versionCode='21811' versionName='2.15.0-direct'`، `application-label:'آتیران ویزیتور'`، `targetSdkVersion 35`.
* ۵ فایل dex + حضور `SQLServerDriver` و `jTDS Driver` + کلاس‌های تازهٔ `VisitorRepository`/`DirectSqlScreen` در dex.
* sha256 فایل ریلیز با sha256 مخزن **دقیقاً یکسان** است (همین عدد در جدول بالای این سند).

---

## ۷) نصب / به‌روزرسانی

1. APK را از ریلیز دانلود کنید (لینک بالای سند).
2. روی گوشی باز کنید ⇒ «نصب»؛ چون امضا و شناسه یکی است، **روی نسخهٔ ۲٫۱۴٫۰ به‌روزرسانی می‌شود** (دادهٔ سبد/فاکتورهای در انتظار توسط مهاجرت ۶→۷ حفظ می‌شود).
3. صفحهٔ اول ⇒ «مامور فروش / ویزیتور سیار» ⇒ اگر تنظیمات اتصال روی گوشی هست، فهرست ویزیتورها خودکار خوانده می‌شود؛ ویزیتور خودتان را بزنید ⇒ ورود به پنل.
4. اجرای بعدی: همان دکمهٔ «ورود سریع» کافی است (ویزیتور ذخیره‌شده، بدون پرسیدن هیچ رمزی).

---

## ۸) نکات صادقانه (شفافیت کامل)

* **تست روی گوشی/سرور واقعی انجام نشده است** (بیلد و امضا در CI انجام شد، اما من به سرور آتیران و گوشی دسترسی ندارم). اگر جایی رفتار غیرمنتظره دیدید، متن `build-failure.txt`/`apk-report.txt` و پیام وضعیت داخل خود برنامه دقیقاً می‌گویند مشکل کجاست.
* تصویر «تنقلات/شیرینی» و «نوشیدنی» فعلاً به نزدیک‌ترین عکس محصول نگاشت شده‌اند (سینی میوهٔ خشک و شیشهٔ مایع طلایی)؛ افزودن دو عکس اختصاصی فقط یک ردیف در `ProductVisuals.kt` است.
* قیمت مصرف‌کننده‌ای که از نام کالا ساخته می‌شود **عدد واقعی سرور نیست** و همیشه با برچسب «پیش‌فرض ٪آ» نشان داده می‌شود؛ اگر ERP شما `forosh3` را پر کند، خودکار جای پیش‌فرض را می‌گیرد.
* ورود از `sys_vis` بدون احراز هویت شخصی است (توضیح و راه‌حل پیشنهادی در بخش ۲).
* APK بدون `R8` فشرده نشده (۳۹٫۸ مگابایت) — همان وضعیت نسخه‌های قبلی؛ در صورت نیاز می‌توانم کوچک‌سازی و تست آن را در نسخهٔ بعد اضافه کنم.

---

**طراحی و برنامه‌نویسی:** میلاد یقوبی — **Meelano Studio Design**
