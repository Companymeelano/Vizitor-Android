# تحویل APK — «آتیران ویزیتور ۲٫۱۴٫۰» (نسخه‌ای که در صفحهٔ اول **انتخاب نقش** دارد)

**تاریخ:** ۲۰۲۶-۰۹-۲۲ • **شاخه:** `arena/01a0c76f-vizitor-android` • **مخزن:** `Companymeelano/Vizitor-Android`
**پروژه:** `vizitor-app/` — همان «آتیران ویزیتور» با اتصال مستقیم SQL Server روی پورت ۱۴۳۳

> ⚠️ این **نسخهٔ درست** است: در صفحهٔ اول، پنل «گام ۱ — اتصال به سرور آتیران» و سپس
> «گام ۲ — انتخاب نقش ورود» با کاشی‌های نقش (مامور فروش/ویزیتور سیار، مدیریت، مدیر فروش،
> حسابداری، انبار و پخش، کاربر فروشگاه) نمایش داده می‌شود.
> نسخهٔ پیشین که در `android-app/` ساخته شد («ویزیتور مستقیم» ۱٫۰٫۰) برنامهٔ کوچک‌تری بدون این صفحهٔ نقش‌ها بود.

---

## ۱) فایل نصب

| مورد | مقدار |
|---|---|
| فایل | **`Vizitor-2.14.0-direct.apk`** |
| حجم | **۴۰٬۹۲۷٬۳۹۰ بایت** (۳۹ مگابایت) |
| SHA-256 | `bf08c3980bdcf198a1a7595f82f4b49424b1283d0042e82ca8ca3ba48484b6e0` |
| امضا | `apksigner verify` → **Verifies** • طرح **v2** • یک امضاکننده |
| گواهی امضا | `C=IR, L=Tehran, O=Meelano, OU=Meelano Studio Design, CN=Vizitor App Direct` |
| اثر انگشت گواهی (SHA-256) | `5C:CA:D3:B6:01:49:6E:69:8B:AE:05:5A:07:29:E1:EF:0B:E5:1F:BA:86:C5:DB:07:54:CA:D0:55:D4:A6:AC:5A` |
| کلید امضا | `vizitor-app/vizitor-app-signing.p12` (alias `vizitorapp`، معتبر تا ۲۰۵۶) — **ثابت بین بیلدها** |
| بستهٔ برنامه | `ir.atiran.vizitor` — نام روی گوشی: **آتیران ویزیتور** |
| نسخه | `versionName = 2.14.0-direct` • `versionCode = 21810` |
| اندروید | `minSdk 24` (اندروید ۷ و بالاتر) • `targetSdk / compileSdk = 35` |
| معماری‌ها | `arm64-v8a`, `armeabi-v7a`, `x86`, `x86_64` |
| تعداد dex | ۵ |

**فایل‌های همراه در ریلیز/مخزن**

| فایل | حجم | SHA-256 | توضیح |
|---|---|---|---|
| `Vizitor-2.14.0-direct.apk` | 40,927,390 | `bf08c398…84b6e0` | نسخهٔ اصلی — همین را روی گوشی نصب کنید |
| `Vizitor-2.14.0-direct-debug.apk` | 51,868,747 | `146dfda4…1c6a38` | نسخهٔ آزمون (بستهٔ `…vizitor.debug`، کنار نسخهٔ اصلی نصب می‌شود) |
| `Vizitor-2.14.0-direct.apk.sha256` | 92 | — | فقط سطر همین APK |
| `SHA256SUMS.txt` | 190 | — | هش هر دو فایل |

**دانلود (۲ راه):**
1. ریلیز: <https://github.com/Companymeelano/Vizitor-Android/releases/tag/vizitor-app-direct-2.14.0>
2. داخل مخزن (اگر بخش ریلیز/Artifacts در شبکه‌تان بسته است): [`vizitor-app/dist/Vizitor-2.14.0-direct.apk`](vizitor-app/dist/Vizitor-2.14.0-direct.apk) همراه `BUILD-INFO.txt`

**آزمون‌های تأییدشده (روی همین فایل، نه روی کاغذ):**
* تأیید امضا با `apksigner` → `Verifies`، طرح v2، یک امضاکننده.
* گواهی داخل APK را جداگانه استخراج و هش کردم → **دقیقاً همان کلید `vizitor-app-signing.p12`** ⇒ نسخه‌های بعدی روی همین نصب به‌روزرسانی می‌شوند (بدون حذف برنامه).
* حضور کلاس‌های کلیدی در dex: `SQLServerDriver` (۱)، `jtds.Driver` (۱)، `VisitorRepository`، `SqlConnectionManager`، `SqlDiagnostics`، `VisitRepository`، `VisitViewModel`، `DirectSqlScreen`، `VisitScreen`.
* سالم‌بودن بستهٔ zip و هم‌خوانی هش فایل/`SHA256SUMS`/گزارش CI.

---

## ۲) نصب روی گوشی

1. `Vizitor-2.14.0-direct.apk` را دانلود و به گوشی منتقل کنید (کابل/بلوتوث/تلگرام — یا همان لینک را روی گوشی باز کنید).
2. فایل را باز کنید؛ اگر اجازه نخواست: **تنظیمات → نصب برنامه‌های ناشناس** برای برنامهٔ فایل‌منیجر را روشن کنید.
3. «نصب» → نام برنامه روی گوشی: **آتیران ویزیتور**.
4. اندروید ۷ (API 24) و بالاتر.

---

## ۳) مسیر کار در برنامه (همان صفحهٔ اول با انتخاب نقش)

**صفحهٔ اول = `SplashScreen.kt` (۷۱۳ خط)** با ساختار دو گامی:

| گام | در برنامه | چه می‌کند |
|---|---|---|
| **گام ۱** | «اتصال به سرور آتیران» | ورود کارت اتصال نصب‌کننده (QR / `android-connect.json` / متن) → پر شدن نشانی داخلی (`h`)، آی‌پی اختصاصی (`H`)، پورت ۱۴۳۳ (`p`)، دیتابیس (`d`)، کاربر محدود (`u`) → وارد کردن رمز کاربر محدود SQL → «تست اتصال و فهرست دیتابیس‌ها» → انتخاب/تایپ دیتابیس → «اتصال به دیتابیس» (فهرست `sys.databases` واقعی + تیک‌های سلامت مسیر پیش‌فاکتور) |
| **گام ۲** | «انتخاب نقش ورود» | کاشی نقش‌ها با گوی سه‌بعدی آواتار و رینگ طلایی: **مامور فروش / ویزیتور سیار (فعال)** • مدیریت • مدیر فروش • حسابداری • انبار و پخش • کاربر فروشگاه (بقیه «به‌زودی») → سپس ورود با **نام کاربری و کلمهٔ عبور خودِ ویزیتور** از جدول واقعی `dbo.sys_users` |

بعد از ورود، همهٔ بخش‌ها فعال می‌شوند (نوار پایین): **پیشخوان • ویترین • ویزیت • مشتری • گزارشات • تنظیمات**
(+ سبد خرید → پیش‌فاکتور، گفتگوی ویزیتورها، اسکنر بارکد).

**دو اعتبارنامهٔ جدا (مثل خودِ سامانه):**
1. کاربر محدود دیتابیس (`vizitor_android`) — فقط برای باز کردن اتصال؛ رمز با **AES-GCM + Android Keystore** روی گوشی ذخیره می‌شود.
2. کاربر ویزیتور در `dbo.sys_users` — برای ورود به سامانه؛ رمز ذخیره **نمی‌شود**.

---

## ۴) تازه‌های نسخهٔ ۲٫۱۴٫۰ (چرا این نسخه)

* **تب «ویزیت»** — ثبت مراجعه در جدول واقعی `dbo.Visit` (مشتری مجاز از `dbo.sys_cus`، مدت، توضیح، موقعیت GPS اختیاری، تاریخ شمسی + ساعت به قالب ERP).
* **ویزیت‌های اخیر + شمارش «امروز»** مستقیم از `dbo.Visit` (نام مشتری با JOIN روی `dbo.CUSTOMERS`).
* **کارت «دسترسی سریع»** در پیشخوان: ثبت ویزیت + گفتگوی ویزیتورها.
* **فعال‌شدن همهٔ بخش‌ها بعد از ورود** (پیشخوان، ویترین با ۵ سطح قیمت و موجودی، ویزیت، مشتری، سبد → پیش‌فاکتور با `dbo.add_sail_pish`، گزارشات از `sailfact`/`sailfact_pish`، تنظیمات).
* **عیب‌یابی اتصال (۶ بررسی)** داخل برنامه: نشانی → DNS → دسترسی به پورت ۱۴۳۳ → هر دو نشانی → بارگذاری درایورها → ورود واقعی → `HAS_DBACCESS`، با گزارش فارسی و دکمهٔ «کپی گزارش (بدون رمز)».
* رفع خطاهای کامپایل نسخهٔ لاکچری (Luxury2/Premium) که بیلد ۲٫۱۳٫۸ را شکسته بود.

---

## ۵) نقشهٔ فنی پروژه

| بخش | مقدار |
|---|---|
| کل Kotlin | **۵۹ فایل / ۱۹٬۸۴۰ خط** |
| صفحهٔ اول با نقش‌ها | `ui/screens/SplashScreen.kt` (۷۱۳) + `ui/components/SplashKit.kt` (۶۸۱) + `ui/components/Premium.kt` (۱٬۰۳۲) |
| لایهٔ اتصال مستقیم SQL | `sqldirect/`: `MeelanoDataSource.kt` (۹۱۳)، `DirectSqlViewModel.kt`، `SqlConnectionManager.kt`، `SqlDiagnostics.kt`، `SecureDbStore.kt`، `VisitRepository.kt`، `VisitorRepository.kt`، `ConnectCards.kt`، `SqldirectSync.kt` |
| صفحه‌های اصلی | `ui/screens/`: ویترین ۱٬۰۳۲ • گفتگو ۱٬۰۳۱ • مشتری ۹۷۰ • اتصال SQL ۹۱۸ • پیشخوان ۸۵۷ • ویزیت ۷۴۷ • سبد ۶۶۲ • گزارشات • تنظیمات • اسکنر |
| لایهٔ محلی/شبکه | Room (AppDatabase/Daos/Entities)، Retrofit + OkHttp، WorkManager (همگام‌سازی)، DataStore |
| زنجیرهٔ بیلد | Gradle 8.x + AGP 8.7 + Kotlin 2.x + KSP، Compose BOM 2024.09.03، JDK 17، SDK 35 |
| درایورهای SQL | `mssql-jdbc 9.4.1.jre8` + `jtds 1.3.1` (ترتیب: jTDS اول، بعد درایور رسمی) |
| دوربین/موقعیت | CameraX (اسکن)، ACCESS_FINE/COARSE_LOCATION (موقعیت ویزیت) |

منبع کد: مخزن `Companymeelano/Vizitor`، شاخهٔ `arena/01a0c36c-vizitor` (کامیت `999f748`) —تمام ۱۴۷ فایل پروژه به‌جز
پوشه‌های `artwork/` و `design/` (تصاویر مادر طراحی، ~۲۱ مگابایت که در ساخت استفاده نمی‌شوند) به این مخزن منتقل شد.

---

## ۶) بیلد و انتشار (شواهد)

```
ورک‌فلو : .github/workflows/build-vizitor-app.yml   (Build Vizitor Android app (direct SQL))
اجرای نهایی: 35694817596 — موفق، ورودی: کامیت 3b6a3c8
گام‌ها   : بررسی کد → JDK 17 → کلید پایدار p12 → gradlew :app:assembleDebug :app:assembleRelease (۶ دقیقه و ۵۴ ثانیه)
          → dist/ → گزارش apk-report.txt → ثبت APK در مخزن → artifact → ریلیز vizitor-app-direct-2.14.0
```

**اصلاحاتی که برای رسیدن به این بیلد لازم بود:**

1. **محرّک ورک‌فلو** فقط روی دو شاخهٔ قدیمی (`arena/01a0a7c6-vizitor`, `arena/01a0c36c-vizitor`) بود → به `main` و `arena/**` تغییر کرد (وگرنه روی این مخزن هرگز اجرا نمی‌شد).
2. **اشکال واقعی در ترتیب گام‌ها:** گام «گزارش بازرسی» با `git reset --hard` پوشهٔ `dist/` را به نسخهٔ داخل گیت برمی‌گرداند؛ پس گام بعدی همان APK **بیلد قبلی** را می‌خواند و artifact/ریلیز هم همان را منتشر می‌کرد، در حالی که گزارش هش بیلد تازه را نشان می‌داد. حالا مبنای ثبت/انتشار، **خروجی خودِ Gradle** است و `dist/` در پایان هم‌تراز می‌شود. (با این اصلاح، هش گزارش = هش فایل در مخزن = هش دارایی ریلیز.)
3. **فایل چک‌سام و BUILD-INFO** قبلاً به سطر نسخهٔ debug اشاره می‌کردند → حالا فقط نسخهٔ اصلی + `SHA256SUMS.txt` کامل.

---

## ۷) نکته‌های صادقانه

* **حجم ۳۹ مگابایت:** برنامهٔ کامل است (Compose، Room، Retrofit، CameraX، نمودارها، انیمیشن‌های لاکچری، درایورهای JDBC) و برای سازگاری کامل، کوچک‌سازی/فشرده‌سازی (R8/شیرینک) خاموش است. اگر حجم مهم است، می‌توان با روشن‌کردن R8 و قواعد نگه‌داری درایور، این عدد را پایین آورد.
* **کلید امضا در مخزن عمومی است** (`vizitor-app-signing.p12`، رمز پیش‌فرض CI) — همان رویهٔ قبلی پروژه. برای انتشار رسمی/فروشگاهی، کلید خصوصی خودتان را بگذارید یا Secretهای `VIZITOR_APP_KEY_B64`/`VIZITOR_APP_KEY_PASS` را در مخزن تنظیم کنید (ورک‌فلو اینها را ترجیح می‌دهد).
* **`mssql-jdbc` رسمی برای اندروید نیست:** پروژه این را با jTDS پشتیبان و انتخاب خودکار درایور مدیریت کرده؛ روی بعضی گوشی‌ها ممکن است درایور رسمی بار نشود و برنامه به jTDS سوییچ کند.
* **پورت ۱۴۳۳ روی اینترنت = سطح حمله:** یا VPN/شبکهٔ داخلی، یا محدودکردن آی‌پی‌های مجاز روی فایروال؛ کاربر دیتابیس باید همان `vizitor_android` محدود بماند (نه `sa`).
* **نقش‌های غیر از «مامور فروش»** در صفحهٔ اول با برچسب «به‌زودی» هستند — این طراحی خودِ برنامه است، نه اشکال بیلد.
* **آزمون روی گوشی واقعی** را CI نمی‌تواند انجام دهد؛ نصب و اتصال را روی یک گوشی باید یک‌بار دستی تأیید کنید. آزمون‌های «اتصال به SQL Server واقعی» به‌صورت خودکار در ورک‌فلوی جدا اجرا می‌شوند.
* پوشه‌های `artwork/` و `design/` (تصاویر مادر) به این مخزن منتقل نشدند تا حجم مخزن بی‌دلیل بالا نرود؛ در مخزن اصلی موجودند.

---

## ۸) ساخت دوباره و نسخهٔ بعدی

* هر تغییر در `vizitor-app/app/**` یا فایل‌های Gradle روی `main`/`arena/**` خودکار APK می‌سازد، گزارش را در `vizitor-app/apk-report.txt` می‌نویسد، APK امضاشده را در `vizitor-app/dist/` ثبت و ریلیز `vizitor-app-direct-2.14.0` را به‌روز می‌کند.
* برای نسخهٔ بالاتر: `versionCode` و `versionName` را در `vizitor-app/app/build.gradle.kts` بالا ببرید و نام فایل‌های `dist/` و تگ ریلیز را هم به‌روز کنید (با **همان کلید پایدار** → به‌روزرسانی روی نصب قبلی).

---

*طراحی و برنامه‌نویسی: **میلاد یقوبی** — گروه نرم‌افزاری **Meelano Studio Design** (برنامهٔ آتیران ویزیتور).*
