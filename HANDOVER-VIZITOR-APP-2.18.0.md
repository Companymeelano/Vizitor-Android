# تحویل نسخهٔ ۲٫۱۸٫۰ — آتیران ویزیتور (اتصال و فراخوانی کامل + اطلاع‌رسانی اولیه + منو/چارت/جدول سه‌بعدی)

**تاریخ:** ۲۰۲۶‑۰۹‑۲۲ · **نسخه:** `2.18.0-direct` · **شمارهٔ ساخت (versionCode):** ۲۱۸۱۴
**صفحهٔ ریلیز:** <https://github.com/Companymeelano/Vizitor-Android/releases/tag/vizitor-app-direct-2.18.0>

> این دور، اول از همه **تحلیل کدبه‌کد** بود؛ نتیجهٔ آن در سند جداگانه
> `TAHLIL-KOD-BE-KOD-VIZITOR-2.18.0.md` آمده است. خلاصه‌اش: برنامه دو مسیر فراخوانی موازی داشت
> (اتصال مستقیم SQL و API قدیمی PHP) و دکمه‌های «همگام‌سازی»، «تست اتصال» و «صدور سند» به مسیر
> قدیمی می‌رفتند که در نصب فعلی وجود ندارد — پس هیچ‌وقت نتیجه نمی‌دادند. حالا همه از **یک دروازهٔ
> واحد روی اتصال مستقیم SQL** عبور می‌کنند.

---

## ۱) فایل نصب

| فایل | حجم | SHA‑256 |
|---|---|---|
| `vizitor-app/dist/Vizitor-2.18.0-direct.apk` (امضاشده) | در صفحهٔ ریلیز و فایل `.sha256` | در صفحهٔ ریلیز و فایل `.sha256` |
| `Vizitor-2.18.0-direct-debug.apk` (نسخهٔ آزمون) | — | — |

- **کلید امضا تغییر نکرده:** `SHA-256 = 5ccad3b601496e698bae055a0729e1ef0be51fba86c5db0754cad055d4a6ac5a`
  (CN=Vizitor App Direct, O=Meelano, C=IR) → **به‌روزرسانی درجا** روی نسخهٔ نصب‌شده (۲٫۱۷ / ۲٫۱۶ / …).
- گزارش کامل بیلد و امضا: `vizitor-app/apk-report.txt`.

---

## ۲) «اتصال» — چه چیزی درست شد

| ایراد در ۲٫۱۷ | اصلاح در ۲٫۱۸ |
|---|---|
| بعد از بسته‌شدن برنامه، هر کوئری خطای «قبل از هر کوئری باید اتصال برقرار باشد» می‌داد | `SqlConnectionManager.ensureConnected()` — هر بخش، اتصال را از تنظیمات **رمزنگاری‌شدهٔ همین گوشی** برقرار می‌کند |
| هر فراخوانی اتصال، اتصال سالم را می‌بست و از نو می‌ساخت | مسیر سریع: اگر با همین تنظیمات وصل هستیم، همان اتصال استفاده می‌شود |
| فراخوانی هم‌زمان، پیام گنگ «خطای نامشخص» می‌داد | اگر اتصالی در جریان باشد، برنامه منتظر نتیجهٔ همان می‌ماند (تا ۴ ثانیه) |
| تغییر سرور، اتصال‌های کهنه را در استخر نگه می‌داشت | با تغییر سرور/دیتابیس، استخر کامل بسته و درایور فعال صفر می‌شود |

## ۳) «فراخوانی» — چه چیزی درست شد

| دکمه/عمل | مسیر قبلی (بی‌نتیجه) | مسیر جدید |
|---|---|---|
| همگام‌سازی | `VizitorRepository.syncAll()` → api.php | `VizitorGateway.syncAll()` → SQL مستقیم (کالا، مشتری، فاکتور، ویزیت، مسیر، سهمیه) |
| تست اتصال | `VizitorRepository.testConnection()` → ping api.php | `VizitorGateway.probe()` → SELECT 1 + مشخصات سرور و درایور فعال |
| صدور سند | فقط فاکتور محلی + تلاش api.php | ثبت محلی **+ فراخوانی مسیر واقعی پیش‌فاکتور ERP** |
| ثبت مشتری جدید | ادعای ارسال به آتیران (نادرست) | ثبت محلی در «انتظار تأیید حسابداری» با پیام شفاف |

**مسیر واقعی پیش‌فاکتور (همان مسیر خودِ ERP):**
`EXEC dbo.add_sail_pish` (۲۶ پارامتر موضعی، `@id_en` = شمارهٔ پیش‌فاکتور) →
درج اقلام در `dbo.subsailtemp_pish` با `mod = 1` (۲۵ ستون، همه پارامتر؛ تریگر
`trig_sst_pish` آن را در `dbo.subsailfact_pish` می‌نویسد) → **بازخوانی تأییدی** از
`subsailfact_pish ⋈ sailfact_pish` روی `(shfacfo, rdf__)` → در شکست نیمه‌کاره،
کنار گذاشتن سند با `active='f', ismodify='t'` (همان روش `Edit_sail_pish`).

⚠️ **قاعدهٔ ایمنی که رعایت شده:** پنج مقدار سربرگ (`ted_rooz`, `mod_darsad_vis`,
`rdf_sarbarg`, `gainall` و `ph_kh`) در هیچ دیتابیسی نمونهٔ واقعی ندارند
(`sailfact_pish` خالی است). برنامه **آن‌ها را حدس نمی‌زند**: تا وقتی یک پیش‌فاکتور مرجع
در خودِ ERP ثبت نشود، دکمهٔ «ثبت واقعی» غیرفعال است و فقط «پیش‌نمایش سند» ساخته می‌شود.

---

## ۴) «اطلاع‌رسانی اولیه به ویزیتور» (صفحهٔ تازه)

یک‌بار پس از نخستین ورود، خودکار باز می‌شود (و از پیشخوان هم همیشه در دسترس است):

* **وضعیت زندهٔ اتصال:** درایور فعال (jTDS/Microsoft)، تأخیر، نام دیتابیس، نسخهٔ SQL Server.
* **اختیارات خودِ ویزیتور:** تعداد مشتری/کالا/انبار مجاز، سهمیهٔ فاکتور باقی‌مانده
  (`visitors.TedadFactorMojazMande`)، اعتبار (`eteb`)، درصد نقدی و چکی.
* **دادهٔ آمادهٔ روی گوشی:** تعداد کالا، مشتری و فاکتور همگام‌شده.
* **ترکیب مشتریان:** دونات سه‌بعدی (خوش‌حساب/بدهکار) + سهم مشتریان خوش‌حساب.
* **مسیرهای رسمی شما:** از `dbo.masir` (شماره، نام، منطقه).
* **مسیر ثبت سند:** روشن/خاموش بودن «ثبت واقعی» و دلیل دقیقش.
* **منوی سه‌بعدی شروع سریع + دکمهٔ «شروع کار».**

## ۵) «همهٔ فعالیت‌های ویزیتور» (صفحهٔ تازه)

منوی سه‌بعدی ۱۲ فعالیت (ثبت ویزیت، مشتریان، ویترین، سبد و سند، گزارش‌ها، اطلاع‌رسانی،
اسکنر بارکد، گفتگوی ویزیتورها، مشتری جدید، همگام‌سازی، مسیرها، تنظیم اتصال) به‌همراه:

* نمودار ستونی **سه‌بعدی** «ویزیت‌های هفته» از تاریخ‌های واقعی `dbo.Visit`؛
* جدول‌های سه‌بعدی «آخرین ویزیت‌ها»، «مطالبات در خطر» و «مسیرها»؛
* کارت «آخرین سند» با اقلام، مشتری، تاریخ و مبلغ سند صادرشده.

## ۶) ظاهر: منو، چارت و جدول **کاملاً سه‌بعدی**

کیت تازهٔ `Lux3D` (۹۴۳ خط، فقط با Canvas خودِ Compose — بدون کتابخانهٔ بیرونی و بدون سنگین‌شدن APK):

| جزء | هندسهٔ سه‌بعدی |
|---|---|
| `Lux3DStage` | صحنهٔ با کف پرسپکتیوی، خطوط گریز و نور طلایی از بالا |
| `Lux3DTile` / `Lux3DMenu` | کاشی‌های منو با **ضخامت واقعی** + چرخش ۷ درجه و جمع‌شدن هنگام لمس |
| `Lux3DBarChart` | ستون‌های ایزومتریک: وجه روبرو، وجه بالا، وجه کنار، سایهٔ بیضی، بازتاب کف و درخشش نوک |
| `Lux3DDonut` | حلقهٔ ضخیم با لبهٔ تیرهٔ زیرین، وجه رنگی، برش‌های گرادیانی و نگین |
| `Lux3DStatOrb` | گوی‌های آماره: کره با گرادیان شعاعی، نور نقطه‌ای، سایهٔ پایه و حلقهٔ طلایی |
| `Lux3DTable` | سربرگ طلایی برجسته + ردیف‌های ضخامت‌دار با سایه |
| `Lux3DProgress` | نوار فرورفته + ستون برآمده + خط نور |

زبانِ طراحی «گزارش طلایی» (نسخهٔ ۲٫۱۷) و هر پنج تم قبلی دست‌نخورده باقی مانده‌اند.

---

## ۷) صادقانه — نکات مهم (بدون ادعای اضافه)

1. **آزمون روی گوشی واقعی انجام نشده است.** ساخت، امضا و بازرسی محتوای APK در CI تأیید می‌شود
   (نام بسته، شمارهٔ ساخت، رشته‌ها و کلاس‌های تازه)، ولی رفتار روی دستگاه و روی دیتابیس واقعی را
   باید خودتان یک‌بار ببینید.
2. **«ثبت واقعی پیش‌فاکتور» فقط پس از ثبت یک پیش‌فاکتور مرجع در خودِ ERP فعال می‌شود**
   (توضیح کامل در بند ۳). پیش از آن، صدور سند «پیش‌نمایش» می‌دهد و در سرور هیچ چیزی نمی‌نویسد —
   این عمدی است، چون ثبت سند با مقادیر حدسی می‌تواند سند نادرست در حساب‌داری بسازد.
3. **ثبت مشتری جدید** در این نسخه فقط در «فهرست در انتظار تأیید حسابداری» روی گوشی ثبت می‌شود؛
   نوشتن در `dbo.CUSTOMERS`/`dbo.new_cust` فعال نشده است (امضای پروسیجر روی سرور تأیید نشده).
4. **مسیر ورود ویزیتور** همان `dbo.sys_vis` است که خودتان در نسخهٔ ۲٫۱۵ خواستید: انتخاب ویزیتور
   بدون کاربری/رمز شخصی. (برای مدیر، ورود با نام کاربری و رمز خودِ مدیر در `dbo.sys_users` انجام می‌شود.)
5. در `dist/` فقط خروجی همین نسخه نگه داشته می‌شود؛ نسخه‌های قبلی روی صفحه‌های ریلیز خودشان
   دست‌نخورده‌اند (۱٫۰٫۰ / ۲٫۱۴٫۰ / ۲٫۱۵٫۰ / ۲٫۱۶٫۰ / ۲٫۱۷٫۰).
6. تصاویر «شیرینی» و «نوشیدنی» همان‌طور که پیش‌تر گفته شد به دو تصویر موجود نگاشت شده‌اند
   (کمبود تصویر اختصاصی در پروژه).

---

## ۸) مسیر به‌روزرسانی

۱) `Vizitor-2.18.0-direct.apk` را روی همان گوشیِ نسخهٔ قبلی نصب کنید (بدون حذف برنامه).
۲) چون کلید امضا یکی است، اندروید به‌روزرسانی درجا را می‌پذیرد و داده‌های محلی می‌مانند.
۳) نخستین اجرا ⇒ نقش را انتخاب کنید ⇒ نام کاربری/رمز ⇒ **صفحهٔ «اطلاع‌رسانی اولیه»** می‌آید
   ⇒ «شروع کار».
۴) اگر روی گوشی «[اتصال برقرار نیست]» دیدید، یک‌بار از «تنظیم اتصال» سرور/دیتابیس را ذخیره کنید؛
   از آن به بعد همهٔ بخش‌ها خودشان وصل می‌شوند.

---

Meelano Studio Design — Milad Yaghoobi · آتیران ویزیتور ۲٫۱۸٫۰
