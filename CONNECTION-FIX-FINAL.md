# رفع مشکل «وصل نشدن اپ ویزیتور به سرور» + نسخهٔ نهایی

این سند دقیقاً پاسخ سؤال شماست: چرا با اینکه پورت ۱۴۳۳ باز و سبز بود، اپ وصل نمی‌شد و
امکان ورود/فراخوانی داده نداشت — و حالا چه چیزی درست شد، با چه فایلی و با چه گام‌هایی.

---

## ۱) علت واقعی (چهار اشکال پیدا شد — سه‌تا در نصب‌کننده، یکی در اپ)

**الف) در نصب‌کننده، مسیر رجیستری SQL Server ناقص ساخته می‌شد.**
کد به‌جای
`HKLM:\SOFTWARE\Microsoft\Microsoft SQL Server\MSSQL15.MSSQLSERVER\MSSQLServer`
دنبال `HKLM:\MSSQL15.MSSQLSERVER` می‌رفت. نتیجه: `Test-Path` همیشه شکست می‌خورد و
**فعال‌سازی خودکار TCP/IP و حالت احراز هویت هرگز اجرا نمی‌شد** (کد مرده بود).
✅ حالا مسیر کامل ساخته می‌شود، هم برای نمونهٔ پیش‌فرض و هم نمونهٔ نام‌دار
(`SQLEXPRESS` و مانند آن)، و بعد از نوشتن، مقدار برگشتی **دوباره خوانده و تأیید** می‌شود.

**ب) در میان‌بر «اتصال مستقیم SQL» (حالت `-AndroidPrepOnly`)، تابع شناسایی نمونهٔ SQL
هنوز اجرا نشده بود.** پس حتی وقتی شما تأیید می‌کردید، کلید رجیستری معلوم نبود و عملاً
هیچ تغییری اعمال نمی‌شد.
✅ حالا همان تابع در ابتدای آماده‌سازی خودش صدا زده می‌شود.

**ج) در اجرای خودکار (حالت پیش‌فرض)، پاسخ همهٔ پرسش‌ها «نه» می‌شد.** یعنی همان تغییر حیاتی
— «SQL Server and Windows Authentication mode» — و ری‌استارت لازم برای اعمال آن،
هرگز انجام نمی‌شد. به همین دلیل: **پورت باز بود (فایروال و TCP سالم)، ولی SQL Server
کاربر SQL را رد می‌کرد** (خطای 18456) و اپ پیام «نام کاربری یا رمز درست نیست» می‌گرفت.
✅ حالا چون «اتصال مستقیم اندروید» صریحاً درخواست شده، این مراحل **خودکار اعمال و ری‌استارت
می‌شوند** (با پیام واضح در لاگ). اگر مدیر سرور بخواهد ری‌استارت را خودش در ساعت غیرکاری انجام دهد،
کافی است نصب را با `-NoSqlRestart` اجرا کند.

**د) در اپ، فقط «یک مرحله اعتبارنامه» وجود داشت.** کاربر محدود دیتابیس که نصب‌کننده می‌سازد
(`vizitor_android`) یک کاربر SQL است و ردیفی در جدول `dbo.sys_users` ندارد؛ از طرف دیگر
کاربر ویزیتور در سامانه، کاربر SQL نیست. پس با یک نام کاربری/رمز هرگز نمی‌شد هم وصل شد و هم وارد.
✅ حالا **دو مرحلهٔ اعتبارنامه** دارد، دقیقاً مثل خودِ سامانه و مثل برنامهٔ آزمایش‌شدهٔ «ویزیتور مستقیم»:
۱) رمز کاربر محدود دیتابیس (باز کردن اتصال) → ۲) نام کاربری/رمز خودِ ویزیتور (`dbo.sys_users`).

> خلاصهٔ ساده: پورت باز بودن فقط نیمی از راه است؛ آن نیمهٔ دوم — «اجازه دادن سرور به ورود کاربر SQL» —
> در نصب‌کننده هیچ‌وقت اجرا نمی‌شد. حالا می‌شود و خودش هم نتیجه‌اش را تأیید می‌کند.

---

## ۲) آنچه در اپ زیبا و ساده شد (طبق خواستهٔ شما)

* **پنل اتصال روی صفحهٔ اول (انتخاب نقش):** بالای کارت‌های نقش، یک پنل هم‌رنگ تم با آیکن سرور،
  چیپ وضعیت («تنظیم نشده / آمادهٔ اتصال / وصل به دیتابیس / متصل ✅»)، نام سرور و دیتابیس،
  و دکمهٔ اصلی. کاربر **اول** اتصال را تنظیم می‌کند، **بعد** نقش خود را می‌زند.
* **ورود سریع با یک ضربه:** کلید «به‌خاطر سپردن» نام کاربری و رمز ویزیتور را با
  AES-GCM + Android Keystore ذخیره می‌کند؛ اجرای بعدی = یک ضربه روی نقش یا «ورود سریع» و
  ورود + همگام‌سازی خودکار و ورود به پنل. (هیچ رمزی بدون انتخاب خودِ کاربر ذخیره نمی‌شود.)
* **صفحهٔ اتصال دیگر شلوغ نیست:** یک کارت خلاصهٔ وضعیت + سه کارت **بازشو** (کارت اتصال نصب‌کننده /
  سرور و دیتابیس / ورود ویزیتور) و یک بازشوی «جزئیات» برای فهرست ویزیتورها و ستون‌ها.
  مرحلهٔ فنی پیش‌فرض بسته است تا کاربر گم نشود.
* **یک مسیر، نه دو مسیر:** بخش REST («آدرس API، کلید، همگام‌سازی وب‌سرویس») که در حالت
  «اتصال مستقیم، بدون IIS/API» هیچ‌وقت کار نمی‌کرد **حذف شد**. حالا هم در صفحهٔ اول و هم در
  تنظیمات، فقط **یک** بخش وجود دارد: «اتصال به سرور آتیران» + «همگام‌سازی و سرویس‌های داده».
* **همه‌چیز با اجزای خود تم:** قاب شیشه‌ای (`GlassCard`)، دکمه‌های نئونی بنفش/سبز، برچسب‌های
  وضعیت، آیکن‌های `Dns / Storage / Person / Sync / QrCodeScanner / Key / Login` و رنگ طلایی تم.

---

## ۳) همگام‌سازی، فراخوانی سرویس‌ها و گزارش‌ها (حالا فعال)

بعد از ورود، اپ **خودش** سرویس‌های داده را از SQL Server می‌خواند و در دیتابیس داخلی گوشی می‌ریزد:

| چه چیزی | از کجا | نتیجه در اپ |
|---|---|---|
| کالاها + ۵ سطح قیمت + موجودی زنده | `dbo.inventory` + `dbo.forosh_price` (+ نام گروه از `dbo.kagroup`) | ویترین، سبد، جست‌وجو و بارکد با دادهٔ واقعی |
| مشتریان مجاز همین ویزیتور | `dbo.sys_cus` ⋈ `dbo.CUSTOMERS` | تب مشتریان، انتخاب مشتری در صدور پیش‌فاکتور |
| ویزیتورها + ستون‌های واقعی + دامنهٔ دسترسی | `dbo.sys_vis` ⋈ `dbo.visitors`، `sys_cus/sys_kal/sys_anb`، `sys.columns` | کارت «جزئیات دادهٔ ویزیتورها» |
| **فاکتور و پیش‌فاکتورهای خودِ ویزیتور** | `dbo.sailfact` و `dbo.sailfact_pish` (فقط ستون‌های تأییدشده) | **بخش جدید «فاکتورهای سامانه (از سرور آتیران)» در تب گزارشات** |

* دکمهٔ «همگام‌سازی» در صفحهٔ اتصال، در تنظیمات و بعد از ورود خودکار اجرا می‌شود.
* همهٔ کوئری‌ها **پارامتری و فقط خواندنی** هستند، روی `Dispatchers.IO` اجرا می‌شوند و
  در صورت خطا، پیام فارسی روشن می‌گیرید (هیچ رمزی چاپ نمی‌شود).
* داده روی گوشی نگه داشته می‌شود تا در قطعی شبکه هم برنامه بی‌داده نماند و بعد از اتصال،
  وضعیت «آخرین همگام‌سازی» همیشه در نوار وضعیت دیده می‌شود.

---

## ۴) فایل‌های تحویل

### الف) برنامهٔ اندروید (نسخهٔ نهایی)
| مورد | مقدار |
|---|---|
| صفحهٔ دانلود | **https://github.com/Companymeelano/Vizitor/releases/tag/vizitor-app-direct-2.13.5** |
| فایل نصب | `Vizitor-2.13.5-direct.apk` — **۳۹٬۲۰۴٬۱۶۵ بایت** |
| SHA-256 | `bc74cd6c5c69e2f907ae3ebecf08286e0a3a25ac323b239063aa5bf128af6539` |
| نسخه | `2.13.5-direct` / ساخت ۲۱۸۰۰ |
| امضا | کلید ثابت `CN=Vizitor App Direct, OU=Meelano Studio Design` (اثر انگشت `5CCAD3B6…AC5A`) |

### ب) برنامهٔ آماده‌سازی سرور (ویندوز)
| مورد | مقدار |
|---|---|
| صفحهٔ دانلود | **https://github.com/Companymeelano/Vizitor/releases/tag/vizitor-setup-v1.0.0** |
| فایل | `Vizitor-Setup-1.0.0.exe` — **۹۷۷٬۹۶۳ بایت** |
| SHA-256 | `b4b55aea8a3a60e0b3136f98d15c1db6f146c66603bc911fa78b65bdeaade073` |
| تأیید | محتوای exe با فایل مخزن **بایت‌به‌بایت** مقابله شد: `install.ps1` = `8f204304…` ✅ |

---

## ۵) گام‌به‌گام روی سرور و گوشی (همین ترتیب را بروید)

**روی سرور (ویندوز):**
۱. فایل `Vizitor-Setup-1.0.0.exe` را (همین نسخهٔ جدید) نصب/اجرا کنید تا فایل‌های `C:\Vizitor`
   به‌روز شوند.
۲. روی دسکتاپ، میان‌بر **«اتصال مستقیم SQL»** را به‌عنوان Administrator اجرا کنید.
۳. حالا این پیام‌ها را باید ببینید (برای اولین بار، سرور SQL یک‌بار کوتاه ری‌استارت می‌شود):
   * `حالت احراز هویت سرور به «SQL Server and Windows» تغییر کرد (LoginMode: 1 → 2)`
   * `سرویس MSSQL… ری‌استارت شد` (یا پیام `net`)
   * `خودآزمایی ورود: کاربر «vizitor_android» با موفقیت وارد دیتابیس [خرما/atiran2/…] شد ✓`
   * `حالا SQL Server روی پورت ۱۴۳۳ گوش می‌دهد و اتصال اندروید آماده است ✓`
۴. اگر پیام **خودآزمایی** سبز نبود، متن همان خط را برای من بفرستید (بدون رمز).

**روی گوشی:**
۵. اگر نسخهٔ قبلی (APK آپلودی ۲٫۱۷٫۱) نصب است، **یک‌بار حذفش کنید** (امضای متفاوت).
۶. `Vizitor-2.13.5-direct.apk` را نصب کنید.
۷. در صفحهٔ اول، پنل «اتصال به سرور آتیران» → **«تنظیم اتصال»**:
   * متن `C:\Vizitor\setup\android-connect.txt` را بچسبانید → «خواندن کارت اتصال»
   * رمز کاربر محدود دیتابیس را وارد کنید (در فایل `C:\ProgramData\Vizitor\android_app_password.txt` است)
   * «تست اتصال» (فهرست دیتابیس‌ها می‌آید) → دیتابیس حسابداری را انتخاب/تایپ کنید → «ذخیرهٔ امن»
   * نام کاربری و رمز **خودتان** در سامانه را بزنید، «به‌خاطر سپردن» را روشن کنید →
     **«ورود و همگام‌سازی»**
۸. از این پس: صفحهٔ اول → کارت «مامور فروش (ویزیتور)» → خودکار وارد پنل می‌شوید و همهٔ بخش‌ها
   (پیشخوان، ویترین، مشتریان، گزارشات) با دادهٔ واقعی سرور فعال‌اند.

---

## ۶) اگر باز هم وصل نشد (سه دستور تشخیصی در SSMS)

روی سرور، در SSMS (با حساب مدیر ویندوزی) این‌ها را اجرا و **جواب‌ها را برایم بفرستید**:

```sql
-- ۱) آیا سرور ورود کاربر SQL را می‌پذیرد؟  نتیجه باید 0 باشد
SELECT SERVERPROPERTY('IsIntegratedSecurityOnly') AS windows_only,
       SERVERPROPERTY('ProductVersion') AS version,
       SERVERPROPERTY('InstanceName') AS instance_name;

-- ۲) وضعیت کاربر محدود اپ (باید is_disabled = 0 باشد)
SELECT name, is_disabled, type_desc FROM sys.server_principals WHERE name = 'vizitor_android';

-- ۳) آیا حساب ویزیتور در سامانه فعال است؟
SELECT user_id, user_name, active, IsLocked FROM dbo.sys_users WHERE user_name = 'نام‌کاربری‌شما';
```

اگر `windows_only = 1` بود، یعنی تغییر حالت احراز هویت اعمال نشده؛ یا نصب را دوباره اجرا کنید
یا دستی از SSMS: راست‌کلیک سرور → Properties → Security → «SQL Server and Windows Authentication mode»
→ OK و بعد در سرویس‌ها سرویس SQL را ری‌استارت کنید.
اگر TCP خاموش است: SQL Server Configuration Manager → SQL Server Network Configuration →
Protocols for <instance> → TCP/IP = Enabled → IPAll → TCP Port = 1433 → ری‌استارت سرویس.

---

## ۷) صادقانه

* همهٔ کارها با **بازرسی خودکار در CI** انجام و ثبت شد (ساخت APK، بازرسی امضا و حضور درایورهای SQL،
  ساخت exe و مقابلهٔ بایت‌به‌بایت محتوایش، آزمون‌های اتصال SQL)، اما **روی گوشی و ویندوز واقعی
  از طرف من آزمایش نشده** — من به آن دستگاه‌ها دسترسی ندارم؛ به همین دلیل خودآزمایی داخل نصب‌کننده
  و پیام‌های دقیق داخل اپ گذاشته شد تا اگر جایی ایراد بود، سریع و بدون حدس رفع شود.
* هیچ جدول/ستون/کوئری‌ای حدس زده نشده: ساختمان داده و فاکتورها از ممیزی واقعی سرور و از
  ستون‌های تأییدشده استخراج شده‌اند. اگر سرور قدیمی با TLS مشکل داشت، در همان صفحه کلید
  «رمزنگاری TLS» را خاموش کنید.

طراحی و برنامه‌نویسی: **میلاد یقوبی** — گروه نرم‌افزاری **Meelano Studio Design**
