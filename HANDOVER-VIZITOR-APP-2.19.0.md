# تحویل نسخهٔ ۲٫۱۹٫۰ — «آتیران ویزیتور»
## اتصال ویزیتور و ارسال فاکتور، دقیقاً به سبک عکس‌های مرجع M•A Report

**تاریخ:** ۲۰۲۶‑۰۹‑۲۳ · **نسخه:** `2.19.0-direct` · **شمارهٔ ساخت (versionCode):** ۲۱۹۱۵
**صفحهٔ ریلیز:** <https://github.com/Companymeelano/Vizitor-Android/releases/tag/vizitor-app-direct-2.19.0>

> این دور دو کار انجام شد: **(۱)** کل پروژه خط‌به‌خط تحلیل شد (سند جداگانه:
> `TAHLIL-KOD-BE-KOD-VIZITOR-2.19.0.md`) و **(۲)** صفحهٔ نخست «اتصال ویزیتور» و
> صفحهٔ «ارسال فاکتور» طوری بازنویسی شدند که چیدمان‌شان **همان چیدمان عکس‌های
> پیوست** باشد: نوار بالا با سه کره، نوار شش بخش، تیتر قهرمان، جدول سه‌رنگ
> «نبض»، کارت طلایی بزرگ، کارت‌های شاخص، کارت سند MEELANO، کاشی‌های خروجی و
> برگهٔ «تنظیمات هوشمند».

---

## ۱) فایل نصب

| فایل | حجم | SHA‑256 |
|---|---|---|
| `vizitor-app/dist/Vizitor-2.19.0-direct.apk` (امضاشده) | ۴۲٫۰۱۰٫۲۷۸ بایت (≈۴۰٫۱ مبی‌بایت) | `d053e1c838bbe1540f3a8b81657098871a0ec89f568030abdf824be35b84822e` |
| `Vizitor-2.19.0-direct-debug.apk` (نسخهٔ آزمون) | ۵۳٫۱۸۲٫۰۴۹ بایت | — |

- کامیت بیلد (CI): `4b36487` — تاریخ بیلد: ۲۰۲۶‑۰۹‑۲۳ ۰۴:۴۹:۰۹ UTC · بیلد CI **موفق** (`35819504773`).
- **بازرسی محتوای همین APK:** نام بسته `ir.atiran.vizitor` · `versionCode=21815` ·
  `versionName=2.19.0-direct` · امضا **Verifies** با همان کلید قبلی؛ و وجود اجزای تازه
  (`MaConnectScreen`, `MaChromeKt`, `MaTopBar`, `MaNavStrip`, `MaGoldCta`, `MaDocCard`,
  `MaSmartSheet`, `MaSegmentPills`, `MaThinBar`, `MaOrbButton`, `MaMetricCard`,
  `askAiAssistant`, `SqlDiagnostics`, `buildReport`, `refreshVisitors`, `InvoiceShare`)
  و رشته‌های فارسی تازه («نبض اتصال ویزیتور»، «تنظیمات هوشمند»، «ارسال پیش‌فاکتور به آتیران»،
  «ثبت واقعی در آتیران»، «دستیار هوشمند فروش»، «اجرای تشخیص کامل»، «فهرست ویزیتورها»،
  «گزارش وضعیت اتصال ویزیتور»، «خروجی سند»، «سهمیه و اعتبار ویزیتور»،
  «پیش‌فاکتور فروش آتیران»، «مشتریان مجاز»، «سلامت مسیر سند»، «کم‌موجود»).

- **کلید امضا تغییر نکرده:** `SHA-256 = 5ccad3b601496e698bae055a0729e1ef0be51fba86c5db0754cad055d4a6ac5a`
  (CN=Vizitor App Direct, O=Meelano, C=IR) → **به‌روزرسانی درجا** روی نسخهٔ نصب‌شده (۲٫۱۴ تا ۲٫۱۸).
- گزارش کامل بیلد و امضا: `vizitor-app/apk-report.txt`.

---

## ۲) صفحهٔ نخست: «اتصال ویزیتور» (تازه)

مسیر: برنامه را که باز کنید، صفحهٔ آغاز → دکمهٔ اتصال → **این صفحه**.

۱. **نوار بالا:** عنوان طلایی «آتیران ویزیتور»، فلش بازگشت، و سه کرهٔ سمت چپ:
   ⚙ تنظیمات هوشمند · ⚠ هشدار (با شمارندهٔ خطا) · 🔍 جست‌وجو/فهرست ویزیتورها.
   کنار عنوان یک قرص وضعیت است: «وصل / در حال اتصال / قطع / وارد شده».
۲. **نوار شش بخش:** فعالیت‌ها · گزارش‌ها · خزانه (سبد) · کالاها · مشتریان · نمای کلی —
   هر شش مورد به صفحهٔ واقعی خودشان می‌روند (هیچ بخش نمایشی نیست).
۳. **تیتر قهرمان:** «آتیران ویزیتور» + «تجربهٔ هوشمند ویزیت و فروش آتیران».
۴. **نبض اتصال ویزیتور:** شش ردیف با نوار سه‌رنگ (سبز/کهربایی/قرمز) و توضیح:
   اتصال آتیران (با زمان پاسخ میلی‌ثانیه) · دیتابیس · ورود ویزیتور (کاربر و نام) ·
   اختیارات ویزیتور (مشتری/کالا/انبار مجاز) · سلامت مسیر سند · همه از **دادهٔ واقعی**.
۵. **کارت طلایی بزرگ:** «اتصال به سرور آتیران» — و بعد از اتصال، همین کارت به
   «همگام‌سازی داده‌های ویزیتور» تبدیل می‌شود (کالا، مشتری، فاکتور، ویزیت امروز).
۶. **چهار کارت شاخص:** مشتریان مجاز · کالاها · انبارها · ویزیتورهای `dbo.sys_vis`
   (با مقدار واقعی هر ویزیتور از `sys_cus` / `sys_kal` / `sys_anb`).
۷. **ورود ویزیتور:** نام کاربری و کلمهٔ عبور آتیران، «به‌خاطر سپردن»، دکمهٔ طلایی
   ورود، و مسیر جایگزین «فهرست ویزیتورها (sys_vis)» با جست‌وجو و انتخاب ویزیتور.
۸. **تنظیمات سرور (بازشو):** آدرس/پورت/آی‌پی عمومی/دیتابیس/کاربر محدود/رمز +
   تست اتصال، ذخیرهٔ امن، ورود سریع و پاک‌سازی رمزها.
۹. **سلامت مسیر سند:** فهرست سبز/قرمز جدول‌ها و اشیای `add_sail_pish`،
   `sailfact_pish`، `subsailfact_pish`، `subsailtemp_pish`، `trig_sst_pish`.
۱۰. **کارت سند MEELANO:** گزارش وضعیت اتصال (سرور، دیتابیس، کاربر، مشتریان مجاز،
    زمان پاسخ) + امضای «Meelano Studio Design · Milad Yaghoobi».
۱۱. **برگهٔ «تنظیمات هوشمند»:** گروه‌های بازشو با کاشی‌های رنگی — ورود سریع،
    تست و اطلاعات سرور، همگام‌سازی کامل، تنظیمات پیشرفته، ورود به سامانه،
    فهرست ویزیتورها، به‌روزرسانی ویزیتورها، ذخیرهٔ امن، قطع اتصال، خروج از حساب،
    پاک‌کردن رمزها.

## ۳) صفحهٔ «ارسال فاکتور» (بازسازی‌شده)

۱. **نوار بالا + قرص وضعیت** «پیش‌نمایش / ثبت واقعی» + هشدار تعداد اقلام کم‌موجود.
۲. **قرص‌های فیلتر:** همه (n) · موجودی کافی (n) · کم‌موجود (n).
۳. **کارت طلایی انتخاب مشتری** با گروه/شهر/کد و برچسب «بدهکار/خوش‌حساب».
۴. **یادداشت ویزیتور** (به پیش‌فاکتور آتیران می‌رود).
۵. **اقلام سبد:** هر قلم با موجودی، کنترل −/+، ویرایش تعداد و مبلغ قلم.
۶. **خلاصهٔ عددی:** تعداد اقلام، جمع اقلام، میانگین هر قلم، وضعیت امضا.
۷. **سهمیه و اعتبار:** نوارهای «سهمیهٔ باقی‌ماندهٔ اسناد»، «اعتبار مجاز» و
   «مشتریان همگام‌شده» + وضعیت آمادگی مسیر ثبت.
۸. **امضای دیجیتال مشتری** (Canvas لمسی) با دکمهٔ پاک‌کردن.
۹. **کلید «ثبت واقعی در آتیران»** + **دکمهٔ طلایی ارسال**؛ دکمهٔ دوم برای
   «ثبت محلی بدون ارسال به سرور».
۱۰. **کارت نتیجهٔ سند:** حالت (پیش‌نمایش امن / ثبت واقعی)، شمارهٔ سند `shfacfo`،
    تعداد اقلام نوشته‌شده، پیام سرویس، دکمهٔ «نمایش متن پیش‌نمایش».
۱۱. **کاشی‌های خروجی واقعی:** اشتراک متن · تصویر · PDF · Word · گزارش‌ها
    (فایل‌ها با `InvoiceShare` ساخته و به اشتراک گذاشته می‌شوند).
۱۲. **دستیار هوشمند فروش:** پیشنهاد کالای مکمل بر اساس مشتری و سابقهٔ خرید
    (اگر اینترنت/پراکسی نباشد، پیشنهاد کلاسیک داخلی نمایش داده می‌شود).
۱۳. **کارت سند MEELANO:** پیش‌فاکتور فروش آتیران با همهٔ ردیف‌ها.

---

## ۴) سه گام استفاده

**گام ۱ — اتصال:** کرهٔ ⚙ (تنظیمات هوشمند) یا کارت طلایی → آدرس `192.168.1.150`،
پورت `1433`، دیتابیس `Meelano`، کاربر `vizitor_android` و رمز → «تست اتصال» →
«ذخیرهٔ امن». انتظار: همهٔ ردیف‌های نبض سبز و «زمان پاسخ سرور» عددی می‌شود.

**گام ۲ — ورود ویزیتور:** «فهرست ویزیتورها (sys_vis)» → جست‌وجو → انتخاب ویزیتور
(یا ورود با نام کاربری/رمز آتیران). پس از ورود، خودکار به پنل می‌روید؛ صفحهٔ
«اطلاع‌رسانی اولیه» هم در نخستین ورود نشان داده می‌شود.

**گام ۳ — ارسال فاکتور:** ویترین → افزودن کالا → سبد سفارش → انتخاب مشتری →
یادداشت و امضا → کلید «ثبت واقعی» (خاموش بگذارید اگر ERP هنوز پیش‌فاکتور مرجع
ندارد) → دکمهٔ طلایی. نتیجه در «کارت نتیجهٔ سند» نوشته می‌شود.

---

## ۵) چه چیزی نسبت به ۲٫۱۸٫۰ تغییر کرد

| موضوع | پیش از این | حالا (۲٫۱۹٫۰) |
|---|---|---|
| صفحهٔ اتصال | `DirectSqlScreen` سه‌گامی ساده | `MaConnectScreen` مطابق عکس‌های مرجع (نوار بالا/بخش‌ها، نبض، کارت طلایی، شاخص‌ها، برگهٔ هوشمند) |
| صفحهٔ ارسال فاکتور | لیست ساده با تم قدیمی | چیدمان مرجع + خلاصهٔ سهمیه/اعتبار + کارت نتیجهٔ سند + کاشی‌های خروجی واقعی |
| دستیار هوشمند | کد بود، اما هیچ دکمه‌ای صدایش نمی‌زد | بخش «دستیار هوشمند فروش» با دکمهٔ واقعی |
| تشخیص عیب | فقط در کد | دکمهٔ «اجرای تشخیص کامل» در دیالوگ هشدار |
| فهرست ویزیتورها | فقط خواندن یک‌باره | + کاشی «به‌روزرسانی ویزیتورها» |
| کارت‌های شاخص | بدون مصرف | گرید ۲×۲ روی صفحهٔ اتصال |
| اشکال چیدمان | دو محل با دو فرزند در یک `item` | هر دو داخل `Column` رفتند |
| مسیر ثبت سند | دست‌نخورده | همان `VizitorGateway.submitCart` → `add_sail_pish` (بدون تغییر رفتار) |

---

## ۶) نکات صادقانه
* من روی گوشی و شبکهٔ داخلی شما آزمون نکرده‌ام؛ بیلد در CI و بازرسی محتوای APK
  انجام می‌شود و آزمون عملی با شماست.
* «ثبت واقعی» تا وقتی در `dbo.sailfact_pish` پیش‌فاکتور مرجعی نباشد، عمداً کار
  نمی‌کند (برای اینکه هیچ سند نیمه‌کاره/حدسی در ERP ساخته نشود).
* کسورات/تخفیف در این نسخه صفر است (تصمیم قبلی کارفرما).
* ثبت مشتری جدید همچنان محلی و «در انتظار تأیید حسابداری» است.
* عکس شیرینی/نوشیدنی در ویترین، نگاشت نام است (نه تصویر اختصاصی).

طراحی و برنامه‌نویسی: **میلاد یقوبی** — **Meelano Studio Design**
