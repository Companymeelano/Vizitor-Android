# گزارش ممیزی فنی Vizitor Android — Direct SQL 1433
تاریخ: 2026-09-21

## نتیجهٔ اجرایی

سورس پروژه به‌صورت فایل‌به‌فایل روی لایه‌های اتصال SQL، احراز هویت، دامنهٔ دسترسی ویزیتور، همگام‌سازی، دادهٔ محلی، مسیر نوشتن پیش‌فاکتور و UI بررسی شد.

### وضعیت فعلی

- معماری اتصال مستقیم Android → SQL Server TCP/1433 در پروژه وجود دارد.
- انتخاب دیتابیس از `master/sys.databases` وجود دارد.
- اعتبارنامهٔ SQL Server با AES-GCM + Android Keystore ذخیره می‌شود.
- ورود ویزیتور از `dbo.sys_users` با قرارداد واقعی ممیزی‌شده انجام می‌شود.
- ارتباط ویزیتور با `sys_vis` و دامنهٔ مشتری/کالا/انبار از `sys_cus/sys_kal/sys_anb` قابل استخراج است.
- UI اتصال سه‌مرحله‌ای و پنج تم لاکچری دارد.
- مسیرهای قدیمی HTTP/API و دادهٔ دمو از مسیر فعال برنامه خارج شدند.
- فاکتور نهایی از ویزیتور عمداً غیرفعال شد؛ این اپ باید فقط پیش‌فاکتور ایجاد کند و حسابداری آن را تبدیل کند.
- مسیر نوشتن پیش‌فاکتور در DataSource پیاده‌سازی شده، اما **تا وقتی یک پیش‌فاکتور واقعی توسط خود آتیران ساخته و نمونه‌برداری نشود، از UI فعال نمی‌شود**. علت این تصمیم جلوگیری از ساخت سند اشتباه در ERP است؛ چند مقدار سربرگ در ممیزی قبلی هنوز نمونهٔ واقعی نداشتند.

## اصلاحات اعمال‌شده

### 1. Pool اتصال و تغییر سرور/دیتابیس
مشکل: هنگام تغییر host/database/user، اتصال‌های قبلی ممکن بود داخل pool باقی بمانند و کوئری بعدی به اتصال قبلی برسد.

اصلاح:
- قبل از هر `connect()`، pool تخلیه می‌شود.
- `settings` و `activeDriver` قبلی پاک می‌شوند.
- فقط اتصال ساخته‌شده با تنظیمات جدید وارد pool می‌شود.

### 2. دامنهٔ کالاها
مشکل مهم: `products()` قبلاً تمام کالاهای فعال `inventory` را می‌خواند و scope ویزیتور `sys_kal` را اعمال نمی‌کرد.

اصلاح:
- کالا فقط وقتی برمی‌گردد که `sys_kal.shka` برای همان `UserID` وجود داشته باشد.
- `SysID` نیز در صورت وجود company filter بررسی می‌شود.

### 3. دامنهٔ موجودی انبار
مشکل: `stock()` موجودی تمام انبارها را می‌خواند.

اصلاح:
- کالا باید در `sys_kal` مجاز باشد.
- انبار باید در `sys_anb` مجاز باشد.
- `UserID` و `SysID` هر دو در صورت نیاز اعمال می‌شوند.

### 4. فاکتور/پیش‌فاکتور در گزارش
فقط اسناد فعال (`active='t'`) خوانده می‌شوند تا نسخه‌های بازنشستهٔ ERP در گزارش ویزیتور ظاهر نشوند.

### 5. حذف دادهٔ دمو
این موارد از مسیر اجرایی حذف شدند:
- seed اولیهٔ کالا/مشتری/سال مالی
- seed پیام‌های گفت‌وگو
- fallback ساختگی Top Products

در اجرای واقعی، نبود داده به معنی نبود داده است؛ برنامه دیگر دادهٔ ساختگی تولید نمی‌کند.

### 6. حذف مسیر API قدیمی از عملیات واقعی
مسیرهای write قدیمی HTTP/API دیگر از اپ استفاده نمی‌شوند.
همگام‌سازی فعال از `SqldirectSync` و SQL Server انجام می‌شود.

### 7. مشتری جدید
چون قرارداد «Pending Approval» واقعی در ERP موجود نیست و `new_cust` مشتری را مستقیماً وارد `CUSTOMERS` می‌کند، ثبت مستقیم آن بدون قرارداد تأیید حسابداری خطرناک است.
بنابراین فعلاً فقط رکورد محلی `pendingApproval` ایجاد می‌شود و برنامه ادعا نمی‌کند که آن را به ERP فرستاده است.

### 8. فاکتور نهایی
متد قدیمی `issueInvoice()` دیگر فاکتور نهایی را به API نمی‌فرستد.
صدور فاکتور نهایی از ویزیتور مسدود است و تبدیل پیش‌فاکتور به فاکتور باید توسط حسابداری انجام شود.

### 9. Cleartext HTTP
`usesCleartextTraffic` از `true` به `false` تغییر داده شد.
اتصال SQL مستقل از HTTP است و مسیر مستقیم از TCP/JDBC استفاده می‌کند.

## مسیر اتصال صحیح

1. کاربر host/IP را وارد می‌کند.
2. پورت پیش‌فرض 1433 است و قابل تغییر است.
3. نام کاربری و رمز SQL Server وارد می‌شود.
4. برنامه با همان حساب به `master` متصل می‌شود.
5. `sys.databases` خوانده می‌شود.
6. کاربر دیتابیس حسابداری را انتخاب می‌کند.
7. اتصال به همان دیتابیس برقرار می‌شود.
8. `DB_NAME()` و نسخه SQL Server و تعداد مشتریان بررسی می‌شود.
9. کاربر ویزیتور با `dbo.sys_users` احراز می‌شود.
10. `sys_vis` و `visitors` هویت ویزیتور را تعیین می‌کنند.
11. `sys_cus`, `sys_kal`, `sys_anb` دامنهٔ دسترسی را تعیین می‌کنند.
12. همگام‌سازی مستقیم کالا، مشتری، موجودی و اسناد فعال انجام می‌شود.

## مسیر پیش‌فاکتور

کد موجود در `MeelanoDataSource` طبق ممیزی ERP این مسیر را دارد:

1. `dbo.add_sail_pish`
2. دریافت `shfacfo`
3. دریافت `rdf__` سربرگ فعال
4. برای هر خط، `INSERT` پارامتری در `dbo.subsailtemp_pish`
5. تریگر `dbo.trig_sst_pish` خط را به `dbo.subsailfact_pish` منتقل می‌کند.
6. خواندن مجدد با join صحیح `(shfacfo, rdf__)`.

### چرا فعلاً دکمهٔ ثبت واقعی فعال نشده؟
طبق مستندات موجود، `sailfact_pish` در دیتابیس‌های ممیزی‌شده رکورد واقعی نداشت. چند پارامتر مهم سربرگ از جمله `ted_rooz`, `ph_kh`, `mod_darsad_vis`, `rdf_sarbarg` و `gainall` نمونهٔ واقعی قطعی نداشتند.

پر کردن این مقادیر با حدس برای ERP حسابداری قابل قبول نیست.

### گام لازم برای فعال‌سازی نهایی
یک بار در خود آتیران، با همان کاربر ویزیتور، یک پیش‌فاکتور واقعی ایجاد کنید و فقط آن را ذخیره کنید؛ تبدیل به فاکتور نکنید. سپس این دو کوئری اجرا شوند:

```sql
SELECT TOP 5 *
FROM dbo.sailfact_pish
ORDER BY shfacfo DESC;

SELECT *
FROM dbo.subsailfact_pish
WHERE shfacfo = (SELECT MAX(shfacfo) FROM dbo.sailfact_pish)
ORDER BY rdf__ DESC, RDF;
```

این نمونه، قرارداد نهایی سربرگ و خط را مشخص می‌کند. بعد از آن می‌توان UI ثبت پیش‌فاکتور را بدون حدس و با همان مقادیر واقعی ERP فعال کرد.

## نکات امنیتی سرور

اتصال مستقیم TCP/1433 از نظر سطح حمله حساس است. توصیهٔ فنی:
- ترجیحاً VPN یا شبکهٔ خصوصی.
- اگر اینترنتی است، firewall فقط IPهای مجاز را بپذیرد.
- کاربر SQL اختصاصی و کم‌دسترسی استفاده شود.
- دسترسی DDL مثل ALTER/DROP داده نشود.
- رمز SQL و رمز ویزیتور در Log چاپ نشوند.
- اگر TLS واقعی با گواهی معتبر فراهم شد، `trustServerCertificate` بهتر است false باشد.

## محدودیت تست ساخت

Build نهایی در این محیط قابل اجرا نبود، چون Gradle Wrapper برای دریافت Gradle 8.9 به اینترنت نیاز داشت و محیط اجرای بررسی دسترسی شبکهٔ خروجی نداشت:

`UnknownHostException: services.gradle.org`

بنابراین این گزارش **ادعا نمی‌کند APK نهایی در همین محیط build شده است**. بررسی سورس، ساختار Gradle، وابستگی‌ها، مسیرهای SQL و اصلاحات استاتیک انجام شده‌اند؛ build نهایی باید روی محیط Android/Gradle دارای dependency cache یا اینترنت اجرا شود.

## فایل‌های مهم بررسی‌شده

- `SqlConnectionManager.kt`
- `DirectSql.kt`
- `SecureDbStore.kt`
- `MeelanoDataSource.kt`
- `VisitorRepository.kt`
- `SqldirectSync.kt`
- `DirectSqlViewModel.kt`
- `VizitorViewModel.kt`
- `VizitorRepository.kt`
- `VizitorApp.kt`
- `CartScreen.kt`
- `DirectSqlScreen.kt`
- `SettingsScreen.kt`
- `VizitorNavigation.kt`
- `Themes.kt`
- `Theme.kt`
- `AndroidManifest.xml`
- مستندات `android-sql-direct/docs/write-path/*`
- `ATIRAN-SCHEMA-EXTRACTED.md`
- `AUDIT-CURRENT-ARCHITECTURE.md`

