# بازطراحی لاکچری اپ اندروید — نسخهٔ ۲٫۱۳٫۶ (اتصال مستقیم SQL)

**طراحی و برنامه‌نویسی: میلاد یقوبی — گروه نرم‌افزاری Meelano Studio Design**

این سند، تحویل **نسخهٔ ۲٫۱۳٫۶** است: همان اپ کامل «آتیران ویزیتور» با اتصال مستقیم به SQL Server
روی پورت ۱۴۳۳، این بار با **صفحهٔ اول و دکمه‌های کاملاً بازطراحی‌شده**، **بخش تنظیمات ورود زیباتر**،
**آیکن سه‌بعدی تازه** و **فونت فارسی وزیرمتن روی تمام بخش‌های برنامه**.

---

## ۱) فایل نصب (اندروید)

| مورد | مقدار |
|---|---|
| صفحهٔ دانلود | **https://github.com/Companymeelano/Vizitor/releases/tag/vizitor-app-direct-2.13.6** |
| فایل نصب | `Vizitor-2.13.6-direct.apk` |
| حجم | **۳۹٬۴۹۲٬۷۴۵ بایت** |
| SHA-256 | `8acc2eab8cb18b371730d2bffa96b6526f739d6323c7761e3ee6e4e15704dfe8` |
| فایل آزمون (کنار اصلی نصب می‌شود) | `Vizitor-2.13.6-direct-debug.apk` — ۴۷٬۷۱۶٬۷۷۷ بایت<br>`fed1d708fda004805c3122b50422fbf490c8ace6ab02f2723500ad29e590d237` |
| نام/شمارهٔ نسخه | `2.13.6-direct` / شمارهٔ ساخت **۲۱۸۰۱** |
| نام برنامه روی گوشی | آتیران ویزیتور |
| پکیج | `ir.atiran.vizitor` (همان پکیج برنامهٔ خودتان) |
| کلید امضا | `CN=Vizitor App Direct, OU=Meelano Studio Design` — `5CCAD3B601496E698BAE055A0729E1EF0BE51FBA86C5DB0754CAD055D4A6AC5A` |
| هویت بیلد | GitHub Actions run **35512928252** — موفق ✓ — گزارش: `vizitor-app/apk-report.txt` |

> 🔑 کلید امضا **همان کلید نسخهٔ ۲٫۱۳٫۵** است؛ یعنی روی نصب قبلی به‌سادگی **به‌روزرسانی** می‌شود
> (بدون حذف و نصب دوباره، و تنظیمات/رمز ذخیره‌شدهٔ اتصال هم از بین نمی‌رود).

### نصب گام‌به‌گام

۱. اگر روی گوشی نسخهٔ ۲٫۱۳٫۵ نصب است: همین فایل جدید را نصب کنید (به‌روزرسانی می‌شود).
۲. اگر همان APK قدیمی ۲٫۱۷٫۱ (آپلودی) نصب است: **یک‌بار حذفش کنید** (امضایش متفاوت است)، سپس این فایل را نصب کنید.
۳. اگر پیام آمد، «نصب از منابع نامشخص» را اجازه دهید.
۴. **بدون نیاز به هیچ تغییر سروری** — نصب‌کنندهٔ ویندوز `Vizitor-Setup-1.0.0.exe` همان فایل قبلی است و
   آماده‌سازی سرور (پورت ۱۴۳۳ + کاربر محدود + دیتابیس) تغییری نکرده.

---

## ۲) چه چیزی در این نسخه بازطراحی شد؟

### الف) صفحهٔ اول (انتخاب نقش) — کاملاً تازه

* **سرصفحهٔ برند:** نشان سه‌بعدی M + مغز پسته در «گوی جواهر» بدون پلاک (مارک تازهٔ برنامه)،
  کنار آن نام «آتیران ویزیتور» با تیتر گرادیانی طلایی و سایهٔ عمق، و گوی آیکن «سپر امنیت» در کنار.
* **کارت معرفی (هیرو):** «پخش عمدهٔ آجیل و خشکبار درخشان» با تیتر گرادیانی + «با مدیریت سرکار خانم حمدانی»
  در قابی با حاشیهٔ نور و شیشهٔ تم.
* **پنل «اتصال به سرور آتیران» به‌عنوان گام اول ورود:** چیپ وضعیت نورانی (تنظیم‌نشده / آمادهٔ ورود /
  وصل به دیتابیس / متصل و آماده)، نام سرور و دیتابیس، و سه شمارندهٔ کالا/مشتری/فاکتور پس از ورود.
  دکمهٔ اصلی هوشمند است: «تنظیم اتصال سرور» → «ورود سریع» → «ورود به پنل»؛ کنار آن دکمهٔ شیشه‌ای
  «تنظیمات/همگام‌سازی».
* **شش کاشی نقش با گوی‌های سه‌بعدی:** آواتار هر نقش داخل گوی گرادیانی با حاشیهٔ طلایی، درخشش شیشه‌ای،
  حلقهٔ نور پشت و **فرو رفتن سه‌بعدی هنگام لمس**؛ نقش فعال نشان «ورود» سبز و بقیه نشان قفل «به‌زودی».
* **ابعاد واکنشی (Responsive):** روی گوشی کوچک/اقتصادی فشرده‌تر و روی گوشی بزرگ/پرچمدار بازتر می‌شود؛
  صفحه اسکرول‌پذیر است تا هیچ عنصری در هیچ اندازه‌ای بریده نشود.
* **فوتر هویت‌محور:** جداکنندهٔ نورانی طلایی + نسخه + امضای «گروه فنی و مهندسی میلانو» و Milad Yaghoobi.

### ب) صفحهٔ «اتصال به سرور آتیران» — زیباتر و مرتب‌تر

* سرصفحه با تیتر گرادیانی، دکمهٔ بازگشت سه‌بعدی و نشان برند.
* کارت خلاصهٔ وضعیت: گوی آیکن وضعیت + نام کاربر وارد‌شده + چیپ وضعیت + جداکنندهٔ نورانی + چهار سطر اطلاعات
  (سرور، دیتابیس، کاربر سامانه، آخرین همگام‌سازی) + شمارنده‌های کالا/مشتری/فاکتور/ویزیتور.
* **همهٔ فیلدهای ورودی** به «فیلد شیشه‌ای تم» تبدیل شدند: قاب گرد، آیکن در گوی کوچک بنفش،
  برچسب طلایی در فوکوس، و آیکن چشم برای نمایش/پنهان رمز.
* سه کارت بازشوی مرتب (کارت اتصال نصب‌کننده، سرور و دیتابیس، ورود ویزیتور) با گوی‌های سه‌بعدی —
  صفحه دیگر شلوغ نیست.
* **بخش ورود ویزیتور (تنظیمات ورود):** نام کاربری و کلمهٔ عبور با گزینهٔ «به‌خاطر سپردن» (ورود سریع
  در اجرای بعدی)، دکمهٔ «ورود و همگام‌سازی»، و در حالت وارد‌شده «ورود به پنل» + «به‌روزرسانی
  ویزیتورها و ستون‌ها».
* دو مرحلهٔ اعتبارنامه دست‌نخورده و امن است: اول **کاربر محدود دیتابیس** (که نصب‌کننده می‌سازد)
  و بعد **نام کاربری/رمز خود ویزیتور** با جدول واقعی `dbo.sys_users`. رمزها فقط رمزنگاری‌شده
  (AES-GCM + Android Keystore) روی گوشی می‌مانند.

### ج) صفحهٔ تنظیمات

* کارت «اتصال سرور آتیران» با گوی سه‌بعدی و چیپ وضعیت تازه؛ همان **یک مسیر واحد** (صفحهٔ اول و
  تنظیمات، هر دو به یک صفحهٔ اتصال باز می‌شوند) و دکمه‌های «مدیریت اتصال» و «همگام‌سازی».

### د) فونت فارسی روی همهٔ بخش‌ها

* **وزیرمتن (Vazirmatn) نسخهٔ ۳۳٫۰۰۳** با **شش وزن واقعی** (Regular, Medium, SemiBold, Bold, ExtraBold, Black)
  داخل خود برنامه بسته‌بندی شد؛ حتی روی گوشی‌هایی که این فونت را ندارند، همه‌جا یکسان و زیبا دیده می‌شود.
* تایپوگرافی برای فارسی کالیبره شد: ارتفاع خط ۱٫۵۲–۱٫۶ برابر (که اعراب و کشیدگی حروف بریده نشود)،
  و **هیچ فاصله‌گذاری حرفی (letterSpacing) روی متن فارسی گذاشته نشد** (اتصال حروف دست‌نخورده).
* فونت روی **متن‌های گرافیکی** هم اعمال شد: برچسب‌های نمودار پیشخوان، شماره‌های نقشهٔ مسیر و
  تصویر/PDF فاکتور.

### ه) آیکن سه‌بعدی تازهٔ اندروید

* مارک **حرف M طلایی برجسته (سه‌بعدی)** با **مغز پسته** روی پلاک بنفش با هالهٔ نور — آماده‌شده در
  همهٔ چگالی‌ها (`mdpi 48` تا `xxxhdpi 192`) + **آیکن گرد** + **لایهٔ تطبیقی** (Adaptive Icon اندروید ۸+)
  + نشان درون‌برنامه‌ای (`brand_mark`) که در سرصفحهٔ صفحهٔ اول و صفحهٔ اتصال استفاده می‌شود.

---

## ۳) نصب‌کنندهٔ ویندوز (بدون تغییر)

| مورد | مقدار |
|---|---|
| صفحهٔ دانلود | https://github.com/Companymeelano/Vizitor/releases/tag/vizitor-setup-v1.0.0 |
| فایل | `Vizitor-Setup-1.0.0.exe` — ۹۷۷٬۹۶۳ بایت |
| SHA-256 | `b4b55aea8a3a60e0b3136f98d15c1db6f146c66603bc911fa78b65bdeaade073` |

اگر سرور را قبلاً با همین فایل آماده کرده‌اید، **کاری لازم نیست**. اگر سرور تازه است:
نصب‌کننده → میان‌بر **«اتصال مستقیم SQL»** (به‌عنوان Administrator) → تأیید تغییرات → خودآزمایی سبز →
کارت اتصال در `C:\Vizitor\setup\android-connect.txt` و رمز کاربر محدود در
`C:\ProgramData\Vizitor\android_app_password.txt`.

---

## ۴) مسیر کار روی گوشی (خیلی کوتاه)

۱. نصب APK جدید → ۲. صفحهٔ اول: **تنظیم اتصال سرور** (چسباندن کارت + رمز کاربر محدود + انتخاب/تایپ دیتابیس
+ ذخیرهٔ امن) → ۳. **نام کاربری و رمز خودتان** + «به‌خاطر سپردن» → **ورود و همگام‌سازی** →
۴. از این پس: صفحهٔ اول → کاشی «مامور فروش» → **ورود خودکار به پنل**.

جزئیات عیب‌یابی سرور (سه کوئری فقط‌خواندنی SSMS) در [`CONNECTION-FIX-FINAL.md`](CONNECTION-FIX-FINAL.md) §۶ آمده است.

---

## ۴.۵) تختهٔ طراحی (برای مرور سریع)

![تختهٔ طراحی ۲٫۱۳٫۶](vizitor-app/design/ui-design-board-2.13.6.png)

فایل: `vizitor-app/design/ui-design-board-2.13.6.png` — آیکن جدید در اندازه‌های مختلف، نشان درون‌برنامه‌ای،
پالت رنگ، اجزای رابط (دکمه‌ها، فیلد، چیپ وضعیت، کاشی نقش، کارت بازشو) و شش وزن فونت وزیرمتن.

> ⚠️ این تصویر یک **تختهٔ طراحی** است، نه اسکرین‌شات اجرای برنامه. تصویر واقعی را باید روی گوشی خودتان ببینید.

---

## ۵) صادقانه و شفاف

* ✅ این نسخه با **بیلد خودکار GitHub Actions** ساخته و بازرسی شده (نسخه/شمارهٔ ساخت، امضا، حضور درایورهای
  SQL، تعداد dex) — گزارش: `vizitor-app/apk-report.txt`.
* ✅ کلید امضا ثابت است ⇒ به‌روزرسانی روی همان نصب قبلی انجام می‌شود.
* ⚠️ زیبایی نهایی را باید **روی گوشی خودتان** ببینید؛ من به دستگاه شما دسترسی ندارم. اگر جایی از چیدمان
  روی گوشی خاصی ناخوشایند بود یا اندازه‌ای را می‌خواهید تغییر دهم، همان را بگویید تا دقیق تنظیم کنم.
* ⚠️ نصب‌کنندهٔ ویندوز در این مرحله **تغییری نکرده** (چون هیچ تغییر سروری لازم نبود) و همان فایل معتبر است.

طراحی و برنامه‌نویسی: **میلاد یقوبی** — گروه نرم‌افزاری **Meelano Studio Design**
